<!-- Topic file for `.claude/rules/key-decisions.md`. Numbering is global; do not renumber. -->

## UI — design tokens, forms, toasts, primitives

### KD #14

Aurora Vault palette in `app/globals.css`; raw values use `--av-*` prefix. `09-design-system.md`.

### KD #15

`@tanstack/react-table` canonical for filterable/sortable lists. Reference: `grading-data-table.tsx`.

### KD #20

Forms use RHF + zod + shadcn `Form`; autosave grids stay on raw state. Schemas in `lib/schemas/`.

### KD #21

Feedback = toasts + dialogs via `sonner` (which is a sileo facade per KD #58 — call sites still `import { toast } from 'sonner'`); `window.alert/confirm/prompt` banned. Locked-sheet dialogs use `components/grading/use-approval-reference.tsx`.

### KD #24

> **Reversed (2026-06-15) — TanStack Query v5 is now the client data layer.** The original rule ("client mutations = raw `fetch` + `toast.error`; no React Query") was deliberately dropped: as the codebase grew, the hand-rolled `useEffect`-fetch reads had no abort-on-unmount/race handling (the "unstable fetching" symptom) and every mutation re-implemented an `isSubmitting` flag. All **80** client components that called `fetch()` were migrated to TanStack Query.

**The standard:** all client→API calls route through **`lib/query/fetcher.ts::apiFetch`** (the single chokepoint — throws a typed `ApiError` with `.status`/`.body`/`.message` already resolved to the response body's `error` field; `jsonInit(method, payload)` builds JSON mutations). `useQuery` for reads, `useMutation` for writes. Provider: `components/providers/query-provider.tsx` mounted once in `app/layout.tsx` — query defaults `{ staleTime: 60s, retry: 2, refetchOnWindowFocus: false }`, **mutations `{ retry: 0 }`** (never auto-replay a non-idempotent POST/PATCH/DELETE). Typed query-key factory in `lib/query/keys.ts`. **`toast` (KD #21) is still the feedback mechanism** — it now fires from `onError`/`onSuccess`.

**Model A (RSC-first, binding):** the app loads most data in server components, so `useMutation.onSuccess` **still calls `router.refresh()`** (and relies on server `revalidateTag`, KD #80) — we do **not** move RSC-loaded data into the client cache. Reads that were lazy client `useEffect` fetches (drill sheets, lookups, palette, history) become `useQuery`.

**Drill-seed rule (the seed is a placeholder, not authoritative — 2026-06-15 fix):** drill sheets are seeded from a server-prefetched row array for instant paint, but the per-`(target, segment)` narrowing happens **server-side in the drill route** (KD #82). So a seed is a **`placeholderData`** (paint instantly, but the query **always fetches** the narrowed rows and replaces it) — NOT `initialData`. Using `initialData` + `initialDataUpdatedAt: Date.now()` marks the broad seed _fresh_ under the 60s `staleTime`, which **skips the narrowing fetch → every drill shows the same full unfiltered set** (the bug this rule prevents). The lone exception is the markbook drill, whose seed is **client-narrowed via `applyTargetFilterClient` to exactly equal the fetch** — only such a seed may use `initialData` (skip the fetch). Reproduction + regression test: `__tests__/query/drill-seed.test.tsx`. Non-drill reads (palette/search/history/lookup) gate on `enabled` and have no seed.

**Error preservation (hard rule of the migration):** routes return specific codes (422 `publish_blocked`/`comments_incomplete`, 409 `grading_lock_date_passed`/`pending_change_requests`, `body.warning`, …). `onError` reads `ApiError.message`/`.body`/`.status` to surface the **exact** route-specific message/dialog — never a generic toast.

**Optimistic tiers:** (1) inline toggle / queue-row actions whose value is local list state → optimistic `onMutate` snapshot-from-closure + rollback in `onError` (reference: `components/admissions/document-validation/validation-queue.tsx` — snapshot from the closure, NOT from inside a `setState` updater); (2) dialog→submit→close→refresh → pending-only (`isPending`); (3) **autosave grids** (`score-entry-grid`, `totals-editor`, `wide-grid`, `daily-entry`) keep their existing **local-state** optimism (KD #20 unchanged) — only the network call routes through `useMutation`. References: `components/grading/totals-editor.tsx` (Tier-2 mutation), `components/admissions/drills/admissions-drill-sheet.tsx` (read). Component tests live under `__tests__/` (`.test.tsx`, jsdom + `@testing-library/react`; helpers in `__tests__/_utils/`).

### KD #44

`DatePicker` + `DateTimePicker` are canonical (`components/ui/date-{picker,time-picker}.tsx`). Native `<input type="date">` / `datetime-local` / `time` are banned outside the primitives themselves.

### KD #84

Unified `<DataTable>` shell + extracted primitives consolidate the previously-scattered toolbar / pagination / bulk-action / status-badge / linkified-identifier patterns. Shell at `components/ui/data-table/index.tsx`; building blocks (sortable-header / facet-dropdown / filter-chip / pagination / bulk-action-footer / empty-state / csv) in the same folder. `<StatusBadge>` at `components/ui/status-badge.tsx` with 4 domain wrappers (`<ApplicationStatusBadge>` / `<DiscountCodeStatusBadge>` / `<DocumentStatusBadge>` / `<EnrollmentStatusBadge>`). `<IdentifierLink>` at `components/ui/identifier-link.tsx` applying KD #81 styling consistently. Two consolidation wrappers: `<CohortTable kind>` (4 cohort kinds → 1 file at `components/sis/cohorts/cohort-table.tsx`) and `<DocumentCompletenessTable module>` (admissions + p-files clones → 1 file at `components/shared/document-completeness-table.tsx`). Plain-English copy registry at `lib/copy/data-table.ts`. URL state via `useUrlState` hook with optional namespace prefix. KD #15 (TanStack canonical) stays valid; the shell is the canonical _consumer_ of TanStack now. Per-row overflow menus + net-new bulk API surface deferred — shell exposes the slots, populate per-table next sprint. **Sprint 37 updates:** (1) `<StatusBadge>` was unified with the loud `<Badge variant>` style — every tone now delegates to `Badge` (healthy→success, locked→blocked, info→default, warning→warning, muted→muted). Subdued wash variant retired; status pills speak with one visual voice (saturated brand gradient + white text + shadow) matching `<StageStatusBadge>`. (2) Flat → gradient sweep across 16 components: every state-bearing tint uses `bg-gradient-to-b from-X/N to-X/(N/3)` instead of flat `bg-X/N`. Neutral chrome (`secondary`/`outline`/`muted` Badge variants, table headers) intentionally left flat. (3) Per-row action dropdown pattern (`⋯ More` `<DropdownMenu>`) — applied where row has ≥3 actions OR mixed-priority actions. Currently `ay-setup-data-table` (6 actions collapsed; high-signal Dates chip + Early-bird Switch stay inline) and `discount-codes-data-table` (Edit + Expire) use the pattern. Tables with 1–2 actions stay inline — dropdown adds friction without payoff.

**Sprint 53 footgun (2026-06-03) — namespace the URL state.** `url={{ enabled: true }}` WITHOUT a `namespace` makes `useUrlState.read()` treat **every** non-reserved page query param as a phantom facet column-filter. On a page carrying its own param (e.g. `?term_id=`), the shell's per-tab count logic (`tabCountData`) mis-applies it — reads `row[param]`, which doesn't exist on the row, so it excludes **every** row → all status-tab counts read **0** — while TanStack ignores the unknown column so the table still shows rows; `write()` would also strip the param on any filter change. **Fix: pass a `namespace`** so the table only reads/writes its own `ns.*` params and leaves the page's params alone (surfaced + fixed on the evaluation sections list → `namespace: 'sections'`). **Deferred:** 6 consumers still use `url={{ enabled: true }}` namespace-less (`grading-data-table`, `change-requests-data-table`, `audit-log-data-table`, `all-publications-overview`, `cohort-table`, `feedback-table`) — each safe only while its page carries no non-facet param; audit + namespace next pass (notably grading, which now receives the KD #75 `?section=` deep-link).

**Section-list conversion (Sprint 59, 2026-06-08).** The three remaining pill/card section lists — `/sis/sections`, `/markbook/sections`, `/attendance/sections` — were converted to `<DataTable>` (mirroring the already-tabled `/evaluation/sections`, KD #106): Level facet + search + CSV + sortable + `url={{enabled:true, namespace:'sections'}}`. Each row gets a `⋯` **row-actions menu** via a new shared **`components/sections/section-row-actions.tsx`** (`module: 'sis'|'markbook'|'attendance'`): "Open" (roster/grading/daily) always; **Generate index + Generate sheets gated to registrar/school_admin/superadmin** (hidden for teachers, who can still view Markbook/Attendance). The Generate-index confirm dialog was extracted into a controlled **`GenerateIndexDialog`** (`components/sis/generate-index-button.tsx`) reused by the standalone button + the row-actions; `GenerateSheetsDialog` is mounted controlled (its trigger self-suppresses when `open` is set with no `children`). **Headline:** Generate-index is now reachable from the **Markbook** sections row-actions, so the registrar no longer trips into SIS Admin to renumber a class (completes the KD #136 discoverability gap). Per-module columns kept lean (current data, no new queries): SIS = Active+Withdrawn, Markbook = Students, Attendance = Active. Markbook's page gained a small terms read for the `termStarted` Generate-index warning; Attendance preserves its form-adviser scoping. Three new `components/{sis,markbook,attendance}/sections-data-table.tsx`. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-08-section-list-tables*`.

**Namespace footgun closed (Sprint 59, 2026-06-08).** The deferred namespace-less `url={{ enabled: true }}` consumers are now all namespaced — an in-session audit found the footgun had gone live because their pages carry non-facet params: **grading** (`?section=` publish-checklist deep-link, KD #75 — it was **blanking the grading list + zeroing tab counts**, the highest-impact one) → `namespace: 'grading'`; **change-requests** (`?req=/action=/sheet_id=`) → `'requests'`; **cohort-table** ×5 (`?ay=`) → `'cohort'`; **feedback** (`?ay=`) → `'feedback'`; **all-publications-overview** (`?section_id=`) → `'publications'` (audit-log already had `'al'`). `rg "url={{ enabled: true }}"` now returns nothing. The grading page's RSC also switched its `initialSearch` read to the namespaced `grading.q` key (+ pruned dead never-read filter-param declarations). Same audit also fixed: the **evaluation priority-panel "submitted" count** now requires submitted **+ non-empty** (KD #120 — it was count-only, mis-counting emptied-but-submitted write-ups; both teacher + registrar sites), and a **guard comment** on `POST /api/attendance/import` documenting it lacks the single-writer's `assertAdviserForSections` scope check (teachers are excluded today, so no bypass — but widen the role list and a teacher could import any section). The audit otherwise came back clean (no `grade_entries` phantom-column bugs, transfer-safe roster counts, count==drill aligned, live-state metrics today-anchored, no Next-16 server-only/cache/async-params violations). No migration.

**Deep-link + url-state completion sweep (Sprint 59, 2026-06-09).** Follow-on once the table url-state was namespaced: a deep-link's query params must match the **destination table's filter contract** — namespaced key + the cell **value** (a name/label), never a raw id. **(1) Grading regression fixed** — the KD #84 namespace rename had stranded grading's inbound deep-links on the old un-namespaced `?q/?status/?mine/?term` + a raw `?section=<id>` server-scope, so they stopped hydrating the table. Added an exact **Section facet** to the grading table and repointed every inbound link (sections CTA, drills, dashboard cards, sidebar "My sheets", publish-window panel + bulk dialog) to `grading.section=<name>` / `grading.*`; dropped the server-side `?section=<id>` scope (stats now AY-wide, table filters client-side). **(2) Full inbound-link audit (3 parallel agents, all modules):** no other id-instead-of-value or namespace-mismatch deep-links exist — every other inbound link is server-scope (page-RSC reads `?ay/?req/?sheet_id/?status/?expiring/?section_id`, un-namespaced + ids OK) or already correct. **(3) Four filterable tables had NO url-state** (filters lost on refresh, not shareable) → namespaced url-state added: `<StudentDataTable>` (`/records/students` + `/admissions/applications`) → `'students'`; `discount-codes-data-table` → `'discounts'`; the two p-files document-validation queues (same page) → `'awaiting'` + `'expiring'` (distinct so they don't collide). **(4) Completion — the (2)/(3) named-table audit was NOT exhaustive.** A systematic enumeration (31 `data-table` importers → 26 filterable → only 15 had url-state) found **11 more** filterable tables still lacking it; all namespaced: `ay-setup-data-table`→`aysetup`, the SIS + Markbook section-roster tables→`roster` (distinct routes), `movements-table`→`movements`, `approvers-data-table`→`approvers`, `users-admin-client`→`users`, `attendance-readonly-table`→`attn`, `outdated-applications-table`→`outdated`, admissions `validation-queue`→`validation`, `report-cards-roster-table`→`rcroster` (distinct from the co-located `publications` overview — they render mutually-exclusively), `my-requests-table`→`myreq`. **Every filterable `<DataTable>` now persists filter/search/tab state to the URL.** (Conceptual note: url-state is _persistence of client-side filtering_ — the DataTable always filters in-memory; namespacing only makes the active filters survive refresh + be shareable. Page-level **server-side** scoping via `?ay/?status/?expiring/?section_id` is a separate layer the page RSC owns.) The genuinely non-filterable / server-filtered surfaces (the attendance audit-log, the p-files dashboard's custom `<Table>`) are correctly left without table url-state. **(5) `status`-facet ↔ reserved-key collision fixed.** A facet whose `columnId` was literally `status` collided with the reserved `<ns>.status` status-TAB key, so that facet's selection never round-tripped. Fixed across **5 tables** (the 3 new — ay-setup/users/my-requests — plus the pre-existing change-requests/discount-codes) by giving the status column a distinct internal `id` (`ay_status`/`user_status`/`req_status`/`cr_status`/`dc_status`) and pointing the facet at it; `accessorKey:'status'` is kept (no display/data change) and the status TAB still uses `<ns>.status`. Net: filter + search + every facet (incl. status) + status tab all persist for every filterable table. No migration.

**Pagination fixes (2026-06-11).** Two shared-shell bugs affecting every `<DataTable>`. **(1) "Next page" reverted to page 1.** The url-state effect persists `?<ns>.page=` via `router.replace` (a real Next navigation) → the RSC re-runs → the table receives a new `data` array → TanStack's default `autoResetPageIndex: true` snapped `pageIndex` back to 0. Fix: `autoResetPageIndex: false` on the table; restore `pageIndex` from `?page=` in `initialState` (round-trips on refresh/share — `useUrlState.read()` already returns `page`); and a dedicated `useEffect` (with a `didMountRef` mount-skip so a deep-linked `?page=` survives) that calls `table.setPageIndex(0)` only when the **filter inputs** change (`[search, statusTab, mineActive, columnFilters]`) — so genuine filtering still resets to page 1 but data-identity re-renders (the url navigation) don't. Sorting / column-visibility deliberately don't reset (no row-count change). Known accepted tradeoff: a parent that _shrinks_ `data` (e.g. a server-side delete) can briefly strand the user on an empty high page (recoverable — "go to first" works). **(2) Rows-per-page Select rendered blank** when a consumer's `pageSize` wasn't in `pageSizeOptions` ([10,20,50,100]) — ~15 tables pass `pageSize={25}`, so `String(25)` matched no `<SelectItem>`. Fix in `pagination.tsx`: `sizeOptions = Array.from(new Set([...pageSizeOptions, pageSize])).sort((a,b)=>a-b)` so the active size always has a matching item. No migration.

**Filters + sorting remediation (2026-06-21).** A full audit of all **31** `<DataTable>` consumers (3 read-only sweeps + an opus whole-diff review). **(1) Sort-affordance contract (the systemic finding):** the shell renders headers via bare `flexRender(columnDef.header)` (`index.tsx`), so a column is user-sortable ONLY if its `header` is a `<SortableHeader>` — `enableSorting:true` / a `sortingFn` on a plain-string header sets sort STATE but renders no clickable UI (and `initialSort` then can't be toggled). Converted the meaningful date/number/name/urgency columns to `SortableHeader` (null-safe comparators sort nulls last; urgency cohorts default `initialSort` soonest-first) across `student-data-table`, `outdated-applications-table`, `feedback-table`, `cohorts/cohort-table` (promised + pass-expiry → `[daysUntil asc]`), `movements-table`, `ay-setup-data-table`, `section-roster-table`, `approvers-data-table`, `users-admin-client`, `discount-codes-data-table`, `staff-table`. **(2) `tabCountData` accessor fix (root-causes the Sprint-53/59 footgun):** the per-tab count loop now resolves each facet's value through the column's accessor (`columns.find(... c.id===f.id || accessorKey===f.id).accessorFn ?? row[accessorKey]`) instead of a raw `row[f.id]` — so accessor-backed facets (an `accessorFn` column, or one whose `id`≠`accessorKey`) no longer zero out the status-tab badges when active. The earlier per-table `id`-rename workaround stays valid but is no longer the only guard. **(3) Facets:** added a `schedule` facet (`sections-data-table`, accessorFn returns the label so vocabulary==valueOptions==`SCHEDULE_LABELS`); replaced movements' hand-rolled free-text reason `<Input>` with a `reason` facet (`WITHDRAWAL_REASON_LABELS`) + a `term` facet (and the movements **page** dropped its server-side `reasonSearch` param + filtering); dropped redundant status-as-both-facet-and-tabs (`cr_status`, `req_status`, expiring-queue `owner`); fixed the `outdated-applications` `status` facet→`app_status` id collision; dropped the broken numeric-accessor `feedbackConsent` facet (the "May contact" tab covers it). **(4) Status→tabs (consistency):** `users-admin-client` (Active/Disabled), `discount-codes` (lifecycle), `attendance-readonly-table` (Active/Late/Withdrawn — hidden `_status` column removed). **(5) Correctness:** `report-cards-roster-table` gained a reachable **Closed** tab (was unreachable); `unsynced-students-queue` gained `gapReason` statusTabs + Level facet + url-state (`namespace:'unsynced'`); the **markbook audit-log** moved action/actor filtering **server-side** (`audit-log-data-table` Selects write `?action=`/`?actor=` + reset page; the log is server-paginated so the old client facets only filtered the loaded page — `page.tsx` validates action against `MARKBOOK_AUDIT_ALLOWLIST`, adds `.eq('actor_email')`, runs a distinct-actor query for the Select). **(6) Off-shell:** `staff-table` migrated onto the shell (searchKeys + assignment statusTabs w/ `countOverride` re-applying `!disabled` + a `toolbarTrailing` show-disabled toggle that pre-filters `data` + SortableHeader on name/load); `document-completeness-table` stays bespoke (sticky col / dynamic slot-dot matrix / Level→Section cascade / `?status`+`?expiring` server branches make a shell rewrite high-risk) but gained in-place column sorting (name/level/%/status) applied **before** the pagination slice. No migration; no spec doc (direct remediation).

**Row links + per-row actions (2026-06-22).** Follow-on audit (3 sweeps) of every `<DataTable>` for linkified row values + per-row actions; all additions are **zero-backend** (reuse existing routes/dialogs). **Cross-module `⋯` menus** (via `RowActionsMenu` + `DropdownMenuItem`, pattern = the audit-log `open` column) added to the action-less tables, each item gated on the id it needs and the menu hidden when empty: `student-data-table` (Open in Records / Admissions / View attendance), `report-cards-roster` (Records / attendance), `attendance-readonly` (Records), `all-publications-overview` (Open roster), `evaluation/sections-list` (Open grading via `?grading.section=`+ Open roster), cohort `stp`/`medical`/`pre-course`/`pass-expiry` (shared `buildCrossModuleActionsColumn` → Records when enrolled+studentNumber, else Admissions). **Hidden actions surfaced:** change-requests "Open sheet" widened from approved-only to all rows; grading per-row **Open sheet** + **Lock sheet** (a `LockSheetMenuItem` reusing `POST /api/grading-sheets/bulk-lock` with `{ids:[id]}` behind an `AlertDialog`, gated `canLock && !is_locked`). **Per-row "Send reminder":** cohort `promised` + document-completeness (reuse `BulkNotifyDialog`/`openBulkNotify`, gated on non-empty targets); p-files **expiring-queue** gained a **Notify** button (reuse `POST /api/p-files/[enroleeNumber]/notify`, mirrors `notify-dialog.tsx`, handles `no_recipients`/cooldown). **`mailto:`** on outdated-applications parent emails + movements actor email. **IdentifierLink consolidation (KD #81):** feedback / cohort×5 / movements / document-completeness / section-roster swapped hand-rolled `<Link>` identifier wrappers → `<IdentifierLink>` (same hrefs). **Staff section links:** FCA badge + subject chips → `/sis/sections/[id]` (ids already on `StaffRow`; `e.stopPropagation()` so they don't trigger the row's assignment sheet). **Review fixes:** cohort `pass-expiry` reminder was **dead** (those rows carry no `toFollowSlots` so `BulkNotify` built zero items) → replaced with cross-module nav + removed the matching dead **bulk** reminder; doc-completeness per-row Remind gated on non-empty targets; **all-publications `status` facet → `pub_status` id** (was colliding with the reserved `<ns>.status` status-tab key, the KD #84 collision class — `initialSort` updated too). No migration.

### KD #161

Column **display names live in `meta.label`; `header` is presentation only** (2026-07-29; no migration). The shared shell's "Columns" visibility menu and the CSV export both derived a column's name as `typeof header === 'string' ? header : column.id`. Because ~every consumer writes `header: ({ column }) => (<SortableHeader column={column}>Section</SortableHeader>)` — a **function** — that fell through to the raw id on **140 columns across 33 files**, surfacing developer identifiers (`levelLabel`, `fcaName`, `daysUntilExpiry`, and generated ids like `writeups_<uuid>`) in a menu school admins read, and as the literal header row of every downloaded CSV.

- **Resolver** `components/ui/data-table/column-label.ts` — `resolveColumnLabel(column)` (live `Column`, for the menu) + `resolveColumnDefLabel(col)` (raw `ColumnDef`, for the export sheet, which never builds a table). Two exports over one private core deliberately: `Column` and `ColumnDef` overlap structurally, so a single permissive signature would silently accept the wrong shape. Fallback chain **`meta.label` → a plain NON-EMPTY string `header` → `humanizeFieldName(id)`**. The non-empty guard matters — several action columns use `header: ''`, which is a string and previously rendered a blank, unlabelled checkbox row.
- **`ColumnMeta` augmentation** (bottom of `components/ui/data-table/types.ts`) types `label` + the pre-existing `excludeFromExport`, replacing an inline cast. It is **global once that module is in the program** — a future `meta: { somethingElse }` anywhere becomes a compile error until declared here. Deliberate.
- **Auto-humanize is a safety net, not the plan.** Measured: 100 of the 140 columns humanize to text that differs from their header, ~40 into different WORDS (`owner` → "Owner" but its header reads "Level"; `sectionName` → "Section Name" vs header "Class"; `iso` → "Iso" vs "Date"). Shipping the fallback alone would have replaced a raw id with a confidently-wrong label. Hence explicit labels on all 140.
- **Labels normally equal the header, and deliberately exceed it twice:** glyph headers expand (`#` → "Index number", `%` → "Attendance %" / "Documents complete %" — the latter is a _document_ completeness column, not attendance), and the two dynamic-id columns take the fuller name (`writeups_${t.id}` → `t.label` "Term 2" not the grid's "T2"; `slot:${h.key}` → the full `h.label`, not the grid's abbreviation).
- **`enableHiding: false` does NOT exempt a column.** `isExportableColumn` gates only on `NON_DATA_COLUMN_IDS` + `excludeFromExport` — so a non-hidable identifier column is absent from the menu but still ships its label as a CSV header. `open` (the audit logs' row-action link menu) was added to `NON_DATA_COLUMN_IDS` alongside `select`/`actions`/`action`.
- **CSV headers changed** (`daysSinceUpdate` → "Days since update"). Accepted trade-off — anyone with a spreadsheet keyed on the old raw text re-maps once. The export sheet's column-search box filters on the label too.
- **No id changed**, so filters, facets, sorting, status tabs, and URL state are untouched — labels are display-only. Column visibility is plain `useState`, never persisted, which is also why making the 6 blank action columns `enableHiding: false` strands no saved state.
- **Drift guard** `__tests__/ui/data-table-column-label-coverage.test.ts` uses the TypeScript compiler API (not regex — a regex scan crosses object boundaries in `cohort-table.tsx`'s five builders) and **auto-discovers consumers**, so new tables are covered with no maintenance. It asserts: every function-header column reachable by either surface declares a label; a literal label matches its header (glyphs and an `INTENTIONAL_LABEL_DIVERGENCE` map excepted); no hidable blank-header column. Proven to bite by deliberately removing a label and renaming a header — two distinct, actionable failures.
- **Not duplicates** (checked, both by design): `cohort-table.tsx` repeats "Student"/"Level" once per its five independent builders; `my-requests-table.tsx` repeats "Filed"/"Reason"/"Status" between `excludeFromExport` on-screen columns and their `csv.extraColumns` equivalents — the two never appear in the same surface.

Cross-ref KD #84 (the shell), KD #15 (TanStack canonical). Out of scope: `components/dashboard/drill-down-sheet.tsx`, a separate bespoke implementation whose own visibility menus render `DrillDownColumnOption.label` from `DRILL_COLUMN_LABELS`.

**Grouped facets — `facetGroups` (2026-07-06, KD #152).** New optional `facetGroups?: FacetGroupConfig[]` prop, additive alongside `facets`. Problem: a table needing many facets (the admissions applications table's 9 per-stage status filters) would wrap the shared toolbar and spawn a chip per selection if all passed to `facets` directly — and `toolbarTrailing` can't help, since it's a static `ReactNode` with no access to `columnFilters`. Fix: `FacetGroupConfig = { label, facets: FacetConfig[] }` — each group renders as **one** outline `Popover` trigger (dashed border, active-count badge, matching `FacetDropdown`'s look) whose content stacks that group's facets (each a normal nested `FacetDropdown` popover — verified nested Radix Popovers compose fine). Implementation extracted the per-facet render body into a local `renderFacet(f: FacetConfig)` closure (identical logic, now shared by both the inline `facets.map` and every group), and `activeChips` resolves labels against `[...facets, ...facetGroups.flatMap(g => g.facets)]` so grouped selections still chip/clear/persist via the existing generic `columnFilters`-based `url` mechanism — no separate persistence path. **Non-regression:** every other consumer omits `facetGroups` (defaults `[]`) so nothing renders differently for them; verified via full `next build` + the existing suite. New test `__tests__/ui/data-table-facet-groups.test.tsx` covers both the no-op case and actual filtering through a grouped facet.
