---
name: key-decisions
description: 72 numbered architectural and behavioral decisions (KD #1–#75, gaps at 19/26/30). Read whenever a "KD #N" reference is cited in code or docs, when making a cross-cutting architectural choice, or when in doubt about module boundaries, roles, or conventions.
load: on-demand
---

<!-- Stable rule. NOT auto-loaded. Read via the Read tool when relevant. Numbering is load-bearing; downstream docs cite "KD #N". Edit only with explicit user approval. -->

## Key decisions

1. **Single Supabase project** for grading + admissions.
2. **Roles in `app_metadata.role`** (`teacher | registrar | school_admin | admin | superadmin | p-file`); no `user_roles` table. `p-file` is module-scoped; `school_admin`/`admin` are P-Files read-only. See KD #39 for the `admin` vs `school_admin` split.
3. **Teacher assignments** in `teacher_assignments(user, section, subject, role)` — `form_adviser` or `subject_teacher`. Gates grading-sheets list + section comments.
4. **Weights per `(subject × level × AY)`** in `subject_configs`; Primary 40/40/20, Secondary 30/50/20; never hardcoded.
5. **Max 5 WW + 5 PT slots per sheet; max 50 students per section.**
6. **Annual grade** = `T1×0.20 + T2×0.20 + T3×0.20 + T4×0.40`, 2dp. `lib/compute/annual.ts`.
7. **PDF generation deferred** — browser print covers current volume.
8. **RLS tightened**: JWT role gate + deny-writes on `authenticated` + per-teacher row scoping (migrations 004, 005).
9. **Generic `audit_log`** (migration 006); every mutating route logs via `lib/audit/log-action.ts`. Module-split render: `pfile.*` rows only on `/p-files/audit-log`.
10. **Publication windows** per `(section, term)` gate parent view (migration 007).
11. **Parents = null-role Supabase users**; `proxy.ts` routes them to `/parent/*` only. Linkage via admissions `motherEmail`/`fatherEmail`.
12. **Parent SSO handoff** via URL fragment at `/parent/enter` — `supabase.auth.setSession()` from hash tokens. `10-parent-portal.md`.
13. **Dynamic AY** via `lib/academic-year.ts`; never hardcode `'AY2026'`.
14. **Aurora Vault palette** in `app/globals.css`; raw values use `--av-*` prefix. `09-design-system.md`.
15. **`@tanstack/react-table` canonical** for filterable/sortable lists. Reference: `grading-data-table.tsx`.
16. **Email via Resend is best-effort**; no-op without `RESEND_API_KEY`; idempotent via DB flags (e.g. `report_card_publications.notified_at`).
17. **Admissions analytics consolidated into `/records`**; `/admin*` redirects there. Superadmin-only CSV at `/api/admissions/export`. `08-admission-dashboard.md`.
18. **Admissions read-only dashboard helpers** in `lib/admissions/dashboard.ts` — `unstable_cache` 10-min TTL, tag `admissions-dashboard:${ayCode}`.
20. **Forms use RHF + zod + shadcn `Form`**; autosave grids stay on raw state. Schemas in `lib/schemas/`.
21. **Feedback = toasts + dialogs** via sonner; `window.alert/confirm/prompt` banned. Locked-sheet dialogs use `components/grading/use-approval-reference.tsx`.
22. **Three Supabase clients, strict separation**: server (cookie-scoped, RLS-enforced), service (bypass, server-only), browser (rare — only `/parent/enter`).
23. **Request validation is mixed**: manual for simple mutations, zod `safeParse` for complex (every SIS PATCH). Don't migrate existing routes for uniformity.
24. **Client mutations = raw `fetch` + `toast.error`; no React Query.** Reference: `components/grading/totals-editor.tsx`.
25. **Locked-sheet edits** go through `grade_change_requests` workflow (migration 009); server derives `approval_reference`, rejects free-text values.
27. **Report card has interim (T1–T3) and final (T4) templates.** `ReportCardDocument` takes `viewingTermNumber`. `lib/compute/annual.ts` + `05-report-card.md`.
28. **Pre-publish readiness is a soft gate**, not a hard block. Registrar can always "Publish anyway".
29. **Dev email redirect** — outside production, all Resend emails rewrite `to` to a static dev address.
31. **P-Files is a repository, not a review queue.** `p-file`+`superadmin` write, `school_admin`+`admin` read. Never sets `'Rejected'` (KD #37). `12-p-files-module.md`.
32. **Dates: ISO 8601 UTC in storage/transit; local formatting at render** via `toLocaleString('en-SG')`. No `dayjs`/`date-fns`/`moment`.
33. **Module switcher** visible to `school_admin`/`admin`/`superadmin`/`registrar`; teachers + `p-file` users locked. `currentModule` type includes `null` for neutral pages.
34. **P-Files upload** = dual-table write + multi-PDF merge (`pdf-merger-js`) + archive-on-replace snapshot. 10MB/file, 30MB/request. `12-p-files-module.md`.
35. **Server-component auth uses `getSessionUser()`** (local JWT via `getClaims()`), not `getUser()`. API routes still use `requireRole()`. `11-performance-patterns.md` §1.
36. **P-Files revision history is append-only** (migration 011). `GET /api/p-files/[enroleeNumber]/revisions`. Hard Rule #6 applies.
37. **Records module writes admissions data**; sole writer of `'Rejected'`. Zod schemas + per-field audit diff + `revalidateTag('sis:${ayCode}')`. Stable IDs rejected from schemas. `13-sis-module.md`.
38. **This is an SIS; modules are surfaces, not apps.** Cross-module links resolve via `studentNumber` (Hard Rule #4). New per-student domains become another tab, not a silo.
39. **`admin` vs `school_admin`** — only the grade-change approval pool differs. `school_admin` + `superadmin` excluded from `listEligibleApproverCandidates`. HFSE mapping: Chandana+Tin = `admin`; office staff = `school_admin`; Joann = `registrar`; Amier+CEO = `superadmin`.
40. **AY rollover is a DB flag flip** — `rpc('create_academic_year')` from `/sis/ay-setup`. Superadmin delete is emptiness-guarded. DDL source of truth: `10a-parent-portal-ddl.md`. `18-ay-setup.md`.
41. **Approvers are per-flow + designated** (`approver_assignments`, migration 013). Teacher picks primary + secondary; only those two see the request. `superadmin` + `school_admin` excluded per KD #39.
42. **Records at `/records/*`; SIS Admin at `/sis/*`.** Internal identifiers (`lib/sis/*`, `sis.*` audit prefix, `sis:${ayCode}` cache tag) stay.
43. **Markbook at `/markbook/*`; `/` is a neutral peer-module picker.** `/` shows a centered tile picker (lifecycle order, scoped via `isRouteAllowed()` so each role only sees the modules they can open). Forced redirects only when the picker would be useless: `null` (parents) → `/parent`, `p-file` → `/p-files`, `admissions` → `/admissions`. Everyone else (teacher, registrar, school_admin, admin, superadmin) lands on the picker — teachers see 3 tiles (markbook, attendance, evaluation), superadmin sees all 7.
44. **`DatePicker` + `DateTimePicker` are canonical** (`components/ui/date-{picker,time-picker}.tsx`). Native `<input type="date">` / `datetime-local` / `time` are banned outside the primitives themselves.
45. **One consolidated dashboard per module; no cross-module data leaks.** `/records` absorbs admissions analytics because Records *is* the operational admissions surface.
46. **Each data dashboard owns `lib/<module>/dashboard.ts`** (SIS also owns `health.ts`). Cache-wrapper pattern: hoist `load*Uncached`, compose `unstable_cache` per-call for dynamic tags. Consistent hero + trust-strip shape.
47. **Attendance is sole writer of daily attendance** (Phase 1+1.1 shipped 2026-04-21). Markbook/Records/Parent are read-only consumers. Audit prefix `attendance.*`. `16-attendance-module.md`.
48. **SIS Admin (`/sis/*`) is the central config surface; operational modules consume config, don't define it.** School Calendar, Sections, Discount Codes, and Teacher Assignments moved to `/sis/*` in the 2026-04-22 consolidation sprint. Operational modules (Attendance, Markbook, Records) keep cross-module sidebar links to the relevant SIS surface for workflow continuity.
49. **Evaluation module owns the form-class-adviser write-up** (sole source for T1–T3 report card FCA comments). `evaluation_writeups` is the source of truth, read by `lib/report-card/build-report-card.ts`. `terms.virtue_theme` (SIS Admin → AY Setup → Dates) drives the parenthetical: "Form Class Adviser's Comments (HFSE Virtues: …)". `/markbook/sections/[id]/comments` and `/markbook/grading/advisory/[id]/comments` redirect to the Evaluation equivalents. T4 is excluded (no comment section on the final card). Grades come from Markbook, attendance from Attendance; Evaluation writeups are the only thing flowing from this module to the report card. `report_card_comments` is legacy — historical rows copied into `evaluation_writeups` by migration 018; no new writes land there.
50. **School calendar has five day-types** (`school_day`, `public_holiday`, `school_holiday`, `hbl`, `no_class`) per `school_calendar.day_type` (migration 019). `school_day` + `hbl` are **encodable** — attendance grid accepts writes, rollup counts them in `school_days`. The other three reject writes (409 on `/api/attendance/daily`). `is_holiday` is legacy, derived via BEFORE trigger from `day_type` — prefer `day_type` in new code. "Important dates / special events" stay on `calendar_events` as an overlay layer (primary-dot on the grid), not a day-type.
51. **Admissions is its own module; Records is enrolled-only.** `/admissions/*` hosts the pre-enrolment funnel (Inquiry → Applied → Interviewed → Offered → Accepted) and the applicant-detail profile/family/docs/enrollment tabs. `/records/*` lists only Enrolled / Conditional students and resolves cross-year history via `studentNumber` (Hard Rule #4) through `lib/sis/records-history.ts`. Dedicated `admissions` role added to `Role` union + `ROUTE_ACCESS`. Module switcher derives allowed modules from `ROUTE_ACCESS` — never hardcode the allowed list. The SharePoint inquiry sync from the original Sprint 7 Part B plan was dropped 2026-04-24; admissions tracks inquiries outside the SIS.
52. **Test environment = AY9999 + Environment switcher** (`/sis/admin/settings`, superadmin only). `POST /api/sis/admin/environment` flips `is_current` to a test AY (`ay_code ~ '^AY9'`); first-time switch creates AY9999 via `create_academic_year` + seeds structure (sections, subject_configs, terms with dates, school_calendar, school_config, grading_sheets) + 200 `TEST-%` students + populated data (grade entries, daily attendance + rollups, teacher_assignments, evaluation writeups, Enrolled admissions rows, pre-enrolment funnel, discount codes, publication window). `DELETE` runs the full destructive cascade + `delete_academic_year` RPC. All seeders are idempotent via skip-guards. `lib/sis/{environment,seeder}/*` owns the flow. `components/sis/test-mode-banner.tsx` shows when `ay_code` matches `^AY9`.
53. **AY admissions tables use 4-digit slugs: `ay{YYYY}_*`** (migration 026). Matches existing production `ay2026_*`. Validators on `create_ay_admissions_tables` / `drop_ay_admissions_tables` are `^ay[0-9]{4}$`; `create_academic_year` / `delete_academic_year` compute `'ay' || substring(v_code from 3)`. Read-side helpers (`lib/sis/queries.ts`, `lib/admissions/dashboard.ts`, `lib/p-files/*`, the `app/api/sis/students/*` routes) all inline `ay${ayCode.replace(/^AY/i, '').toLowerCase()}` — copy this pattern when adding a new call site.
54. **Dashboard framework** (Sprint 21, 2026-04-24) — every module dashboard composes from `lib/dashboard/*` + `components/dashboard/*`, not bespoke markup. Shared primitives: `DashboardHero`, `ComparisonToolbar` (AY + date-range + auto-computed prior-period comparison), `MetricCard` (with inline `SparklineChart`), `InsightsPanel` (7 per-module narrative generators in `lib/dashboard/insights.ts`), `ActionList`, plus recharts wrappers (`TrendChart` / `ComparisonBarChart` / `DonutChart` / `StackedAreaChart` / `Heatmap`). Per-module `lib/<module>/dashboard.ts` adds `*Range` sibling functions next to any existing AY-scoped helpers (KD #46 cache-wrapper pattern preserved). URL-param contract: `?ay=&from=&to=&cmpFrom=&cmpTo=` plus module-specific URL filters (`?status=`, `?level=`); **no dropdown UI for secondary filters**. "Target" = prior-period comparison (auto-computed back-to-back of equal length) — no stored `kpi_targets` table. Chart budget ≤ 8 per screen (sparklines inside MetricCards don't count). **Critical gotcha:** `lib/dashboard/windows.ts::loadTermsUncached` uses `createServiceClient()` — cookie-scoped `createClient()` is forbidden inside `unstable_cache` in Next 16. Full spec: `docs/superpowers/specs/2026-04-24-comprehensive-dashboard-redesign.md`.
55. **`/attendance` route split** (Sprint 21) — `/attendance` is the analytics dashboard (registrar+ only: KPIs + trend + mix + top-absent); `/attendance/sections` is the section picker (the daily-entry gateway, teachers land here); `/attendance/[sectionId]` remains the daily writer (unchanged). Teachers visiting `/attendance` redirect to `/attendance/sections`. Attendance sidebar's isActive logic exact-matches `/attendance` (Dashboard) and prefix-matches `/attendance/sections` so `/attendance/[sectionId]` still highlights "Sections".
56. **Drill-down framework** (Sprint 22, 2026-04-25, on `feat/dashboard-drilldowns`; perf-tightened Sprint 23 2026-04-26) — every aggregating dashboard surface is a clickable drill into the underlying rows. Per-module shape: `lib/<module>/drill.ts` (row shape(s) + `buildDrillRows` + `applyTargetFilter` + `defaultColumnsForTarget` + `drillHeaderForTarget`); `app/api/<module>/drill/[target]/route.ts` (unified GET, JSON+CSV with UTF-8 BOM); `components/<module>/drills/<module>-drill-sheet.tsx` (target-aware sheet); `components/<module>/drills/chart-drill-cards.tsx` (per-target `'use client'` wrappers — Server Components cannot pass functions as children to Client Components in Next 16, so render-prop children are forbidden — use named per-target wrappers). Shared infra in `components/dashboard/drill-down-sheet.tsx` — universal toolkit (range-scope `Tabs` / status multi-select / level multi-select / group-by / density / column visibility / CSV button) — and `MetricCard.drillSheet` slot. **Pre-fetch contract (Sprint 23 update)**: `buildAllRowSets()` returns rolled-up shapes only; raw row arrays (entry / writeup-level) lazy-fetch on drill open via the existing `useEffect` (no new code path) with `DrillSheetSkeleton` placeholder. Modules with bounded row counts (≤ 5,000 rows) keep current full pre-fetch — Evaluation and Admissions today. Range-scope toggle (`This range`/`Current AY`/`All time`) triggers re-fetch via raw `fetch` (KD #24). Auth: per-module `requireRole(...)` + teacher row-scoping where applicable (Markbook teachers see only assigned sections; Evaluation teachers see only `form_adviser` sections; Attendance is registrar+ only per KD #55). Cache: per-call `unstable_cache` with module-scoped tag, 60s revalidate. Shared `lib/auth/teacher-emails.ts::getTeacherEmailMap()` replaces ad-hoc `auth.admin.listUsers` calls. Modules wired: Admissions (12 targets) · Markbook (9) · Attendance (10) · Evaluation (7) · Records (8 + class-assignment-readiness card) · P-Files (8 + revisions-heatmap card) · SIS Admin (4 + activity-by-actor card; system-health strip panels click-to-drill). Records pipeline-stage rebuilt as recharts Sankey for stage-flow visualization (Sprint 24). Specs: `docs/superpowers/specs/2026-04-25-admissions-drill-downs-design.md` + `2026-04-25-drill-down-replication-design.md` + `2026-04-25-dashboard-perf-audit-design.md` + `2026-04-26-records-pfiles-sis-drill-downs-design.md`.
57. **Layout archetypes** (Sprint 25, 2026-04-26, on `feat/dashboard-drilldowns`) — every module dashboard is classified into one of three archetypes from `docs/context/20-dashboards.md` § Layout archetypes: **operational** (the user lands with a job to do — top-of-fold is `PriorityPanel`); **analytical** (the user lands to monitor trends — top-of-fold is the F-pattern KPI grid + InsightsPanel); **hub** (the user lands to navigate to config — top-of-fold is admin nav cards, KPIs only via opt-in `?view=audit` tab). New shared primitive `components/dashboard/priority-panel.tsx` + `lib/dashboard/priority.ts` (`PriorityPayload` type). Per-module priority computers live next to existing dashboard.ts loaders (e.g. `lib/p-files/dashboard.ts::getPFilesPriority`). **Role-aware composition**: where one URL serves both teachers and registrars (Markbook + Evaluation), the page RSC branches on `sessionUser.role` at SSR and renders different `PriorityPanel` payloads + (for teachers) hides school-wide analytical KPIs. Teachers stay at the same URL — never a per-role route. Module assignments: Markbook (operational, role-split) · Attendance (operational) · P-Files (operational) · Records (analytical, no change) · Admissions (analytical, no change) · Evaluation (analytical/registrar + operational/teacher, role-split) · SIS Admin (hub). KD #54 + #56 preserved (drill framework intact, KD #24 preserved — no React Query). Spec codified inline in `docs/context/20-dashboards.md` § Layout archetypes.
58. **Single shared `<ModuleSidebar>` primitive** (Sprint 26, 2026-04-26, on `feat/swap-sonner-sileo`) — the 8 per-module sidebars (`markbook-`, `attendance-`, `sis-`, `p-files-`, `records-`, `admissions-`, `evaluation-`, `parent-sidebar.tsx`) collapse into one `components/module-sidebar.tsx` consumed by all 8 layouts. Module identity moves into the sidebar header as a `Popover` switcher; the topbar `<ModuleSwitcher>` is removed (a leaner `<TopbarModuleSwitcher>` survives only on the neutral `/` + `/account` shells where there is no sidebar to host it). Active item adopts §9.3 non-flat (gradient wash + ring-inset, no left bar). Footer is a single profile pill that opens a popover for Account + Sign out. Generalized realtime-badge slot via `lib/sidebar/use-realtime-badges.ts` (extracted from the markbook-only `useRealtimeBadgeCount` hook, now deleted) — register a key in `SidebarBadgeKey` + add a case to `subscribeChannels` to add a new live badge. Per-role quick action above the nav tree, declarative in `lib/sidebar/registry.ts::SIDEBAR_REGISTRY[module].quickActionByRole`. **Toast shim (same pass)** — `sonner` removed from `package.json`, `sileo ^0.1.5` added; `tsconfig.json::compilerOptions.paths` rewires `'sonner'` → `components/ui/sonner.tsx`, which is a sonner-shaped facade over `sileo`. Zero call-site touches across ~154 `toast.X(...)` invocations in 55 files. Specs: `docs/superpowers/specs/2026-04-26-sidebar-redesign-design.md` + `2026-04-26-sonner-to-sileo-shim-design.md`.
59. **Two `applicationStatus` columns with different value spaces** (Sprint 27, 2026-04-27, on `feat/swap-sonner-sileo`). `ay{YY}_enrolment_applications.applicationStatus` is parent-portal-side (set by parent registration form, typical value `"Registered"`). `ay{YY}_enrolment_status.applicationStatus` is SIS-side workflow pipeline (canonical `Submitted | Ongoing Verification | Processing | Enrolled | Enrolled (Conditional) | Cancelled | Withdrawn` per `lib/schemas/sis.ts::STAGE_STATUS_OPTIONS.application`). Same column name, different tables, completely different value spaces. Drill loaders + dashboards + lifecycle widget read from the SIS-side (status) column. Seeder must write `'Registered'` to apps row + canonical SIS pipeline value to status row. See `docs/context/14-modules-overview.md` Cross-module data contract for the writer mapping.
60. **Document status workflow distinguishes expiring vs non-expiring slots** (Sprint 27, 2026-04-27). Non-expiring (idPicture, birthCert, educCert, medical, form12, icaPhoto, financialSupportDocs, vaccinationInformation): `null → 'Uploaded' → 'Valid'` (or `'Rejected'`); registrar manually flips Uploaded→Valid. Expiring (passport, pass, motherPassport, motherPass, fatherPassport, fatherPass, guardianPassport, guardianPass): `null → 'Valid' → 'Expired'` (auto-flip when expiry passes); the expiry date IS the validation evidence, no `'Uploaded'` intermediate. `'To follow'` = parent-acknowledged-pending (either type). `'Rejected'` + `'Expired'` both signal parent re-upload needed. Lifecycle aggregate has separate buckets: "Awaiting document validation" (Uploaded) vs "Awaiting document revalidation" (Rejected + Expired). Constants: `DOCUMENT_SLOTS` in `lib/sis/queries.ts` (16 entries; the `expiryCol?:` field is the type discriminator).
61. **STP application tracker (HFSE Edutrust Certified)** (Sprint 27, 2026-04-27). `stpApplicationType` on `ay{YY}_enrolment_applications` gates 3 conditional document slots: `icaPhoto`, `financialSupportDocs`, `vaccinationInformation` (exported as `STP_CONDITIONAL_SLOT_KEYS` from `lib/sis/queries.ts`). When set (typical value `"New Student Pass Application"`), the 3 slots are required and surfaces should expose them; when null, they stay NULL and surfaces hide them. `residenceHistory` JSON column on apps row pairs with the 3 slots (ICA requires past 5 years of residency). Context: HFSE became Edutrust Certified, enabling the school to sponsor Singapore Student Pass applications via ICA on behalf of foreign students. Full doc: `docs/context/21-stp-application.md`. Lifecycle widget today rolls STP slots into the same Uploaded/Rejected/Expired buckets as other slots; dedicated STP buckets are future work.
62. **`category` ↔ `enroleeType` mirror across applications + status; discount-codes catalog uses 6-value superset** (Sprint 27, 2026-04-27). `applications.category` and `status.enroleeType` always agree; both ∈ `{New, Current, VizSchool New, VizSchool Current}` (4 values, exported as `ENROLEE_CATEGORIES` from `lib/schemas/sis.ts`). `discount_codes.enroleeType` ∈ same 4 values + `Both` + `VizSchool Both` (6 values, exported as `DISCOUNT_ENROLEE_TYPES` from `lib/schemas/sis.ts`). The `Both` entries on discount codes apply to either New OR Current eligibility filters. Seeder must keep apps.category and status.enroleeType in sync per row.
63. **Parent-portal re-upload tracking via AFTER UPDATE trigger** (Sprint 28, 2026-04-29, migration 033). P-Files revision history previously only captured the SIS-officer upload route's writes; parents' direct portal re-uploads silently overwrote the URL column on `ay{YYYY}_enrolment_documents`. New `capture_doc_revision()` PL/pgSQL trigger fires AFTER UPDATE OF the 16 slot URL columns on each AY's docs table, gated on enrolled status (looks up `applicationStatus` from the matching `_status` table via dynamic EXECUTE; bails if not Enrolled/Enrolled-Conditional — KD #31 scope rule). Inserts one `p_file_revisions` row per changed slot; `source` discriminator (`'pfile-upload' | 'parent-portal' | 'sis-direct'`) derives from `auth.jwt()` presence (parent-portal writes via authenticated JWT; service-role writes via the SIS upload route insert their own `'pfile-upload'` row first). Partial unique index on `(ay_code, enrolee_number, slot_key, previous_url)` lets the route's explicit insert + the trigger's deferred insert dedupe via ON CONFLICT DO NOTHING. `attach_doc_revision_trigger(p_docs_table)` introspects `information_schema.columns` and only lists slot URLs that exist — handles AY2025 missing the 3 STP columns gracefully. Schema changes: `p_file_revisions.archived_url` + `archived_path` → nullable; new `previous_url` + `source` columns. File-content preservation (parent portal overwriting canonical paths) is out of scope — the trigger captures metadata only.
64. **P-Files renewal lifecycle** (Sprint 28, 2026-04-29, migration 034). Append-only `p_file_outreach` table (`kind ∈ {'reminder', 'promise'}`) backs two registrar actions on actionable `DocumentCard`s: **Notify parent** (`POST /api/p-files/[enroleeNumber]/notify` — Resend email per recipient, 24h cooldown via the latest `kind='reminder'` row, 6 failure reasons surfaced) and **Mark as promised by `<date>`** (`PATCH /api/p-files/[enroleeNumber]/promise` — flips `<slot>Status='To follow'` per KD #60, surfaces in existing chase-strip "promised" bucket, `revalidateTag('sis:${ayCode}')`). Bulk fan-out via `POST /api/p-files/notify/bulk` (cap 50; skipped/cooldown counts surfaced). Recipient resolution by slot prefix: `mother*`/`father*`/`guardian*` → matching email; student slots → mother + father (CC), fallback guardian. Email template at `lib/notifications/email-pfile-reminder.ts` mirrors `email-parents-publication.ts` (RESEND_API_KEY no-op fallback per KD #16, NODE_ENV dev-redirect per KD #29). 3 new audit actions: `pfile.reminder.sent`, `pfile.reminder.bulk`, `pfile.mark.promised`. Sidebar quicklinks `?expiring=30|60|90` plus the existing `?status=expired` flip the `CompletenessTable` into bulk-select mode (checkbox column + sticky `<BulkNotifyDialog>` footer). Out of scope (v2): auto-revert `'To follow'` when `promised_until` passes; auto-cadence reminders; Resend webhook open/click tracking. Closes the loop from "see expiring docs" (Sprint 21 chase strip) to "act + track from inside the module".
65. **Parent SSO via HMAC-signed parallel cookie** (Sprint 28, 2026-04-29). Replaces the prior Supabase `setSession`-based parent SSO (KD #12) with a parallel HMAC-signed `parent_session` cookie scoped to the SIS — the previous flow clobbered staff Supabase sessions in shared browsers because both writes targeted the same `sb-*` cookies. New flow: `/api/parent/handoff` verifies the inbound `access_token` via the service client (without `setSession`) and sets the signed cookie (2h TTL); `/api/parent/exit` clears it; `<ParentSessionWatcher>` clears via `navigator.sendBeacon` on `pagehide`. `/parent/enter` moved to its own `(parent-handoff)` route group so `proxy.ts` can bypass claim checks just for the handoff endpoint without making the rest of `/parent` permissive. `proxy.ts` now treats `/parent/*` as fully cookie-gated (skips Supabase claim checks) and bounces null-role JWTs outside `/parent` to `/login` rather than redirect-looping. Sign-out: `<SidebarProfile>` branches on role — parents call `/api/parent/exit` + `window.location` to the portal so a co-resident staff Supabase session is untouched. Cross-AY visibility: `getAllStudentsByParentEmail` walks every `academic_years` row and dedupes by `student_number` so report cards are visible across any AY the student is enrolled in — publication-window per KD #28 is the actual gate, not current AY. New env var `PARENT_HANDOFF_SECRET` (≥32 chars per `lib/parent/cookie.ts`); MUST differ per environment; rotating invalidates all live parent sessions. KD #12's HMAC handoff mechanism is replaced; KD #11's "parents = null-role Supabase users" pattern stays valid for fallback identity but is no longer the auth carrier inside the SIS.
66. **Master class template tables** (Sprint 28, 2026-04-29, migrations 031+032). Sections + subject weights configuration is now driven by two dedicated master tables (`template_sections` + `template_subject_configs`) decoupled from any specific AY, instead of being copy-forwarded from the most-recent prior AY at AY-setup time. Superadmin manages the master at `/sis/admin/template` (sections + weights matrix); the Apply panel propagates the master to one or many target AYs via `apply_template_to_ay(p_ay_slug, p_overwrite)` RPC — non-destructive UPSERT preserves operational data (grading sheets, attendance, evaluations) on already-populated AYs. Migration 032 seeds the master template from AY9999's current rows so existing config carries over. AY-setup wizard reads from the template tables when creating a new AY, no longer copies from AY9999 (replacing the pattern that pulled test-AY pollution into AY2026/AY2027). Migration 030 ships alongside as the idempotency upgrade for `create_academic_year` (safe to re-run on partial-state AYs; source-AY copy-forward filters out test AYs via `^AY9`). New audit actions: `template.section.{create,update,delete}`, `template.subject_config.update`, `template.apply` + 3 entity types (`template_section`, `template_subject_config`, `template_application`). Sidebar: superadmin-only quicklink at `/sis/admin/template`; matching `ROUTE_ACCESS` allowlist entry. KD #4's "weights per `(subject × level × AY)` in `subject_configs`" + KD #40's "AY rollover is a DB flag flip" stay valid — the template is the *source* the per-AY rows are *applied from*, not a replacement for the per-AY storage.
67. **Atomic mid-year section transfer** (Sprint 29, 2026-04-30). Reassigning an enrolled student to a new section within the same level happens through the dedicated `POST /api/sis/students/[enroleeNumber]/transfer-section` route, NOT the existing class-stage PATCH. The route's helper `lib/sis/section-transfer.ts::transferStudentSection` runs an atomic withdraw-old-row + insert-new-row pair against `section_students` (per Hard Rule #6: append-only) plus an admissions-side `classSection`/`classLevel`/`classStatus='Finished'` update on the same call. The class-stage PATCH at `app/api/sis/students/[enroleeNumber]/stage/[stageKey]/route.ts` rejects post-Enrolled `classSection` mutations with a 422 pointing to the new endpoint — closes the silent dual-section bug where `syncOneStudent`'s narrow scope (`lib/sync/students.ts:291-300`, "does NOT detect withdrawals from other sections") left the student active in BOTH sections until the next bulk sync. Pre-flight rejections: cross-level transfer (same-level only), target at 50-cap (Hard Rule #5), same-section, missing studentNumber. Term-of-transfer captured in audit context via shared `lib/sis/terms.ts::getTermForDate` lookup; `student.section.transfer` audit row records `{fromSection, fromLevel, toSection, toLevel, transferDate, termNumber, termLabel}`. UI: per-row "Move" action on the new SIS Admin section roster table at `/sis/sections/[id]` (`components/sis/section-roster-table.tsx` + `<SectionTransferDialog>`). Records detail page (`/records/students/[studentNumber]`) renders a "Section transfers" history card sourced from `lib/sis/section-history.ts` — audit-log-derived, scoped cross-AY by walking every `enroleeNumber` for the student.
68. **Late-enrollee term detection** (Sprint 29, 2026-04-30). When the registrar tags a student as `late_enrollee` via `<EnrolmentEditSheet>`, `app/api/sections/[id]/students/[enrolmentId]/route.ts` PATCH refreshes `enrollment_date=today` on the active→late_enrollee boundary (boundary-only — idempotent re-saves don't re-stamp the date) so the joining-term lookup against the `terms` table reflects the registrar's tag moment, not the row's original sync-creation date. Term resolved via shared `lib/sis/terms.ts::getTermForDate(date, ayCode)` helper which also powers the section-transfer route (KD #67) — single source for date→term lookup, no per-route inline query. Audit context captures `lateEnrolleeTermLabel`; route response carries `lateEnrolleeTerm` so the EnrolmentEditSheet's success toast confirms which term ("Tagged Juan Cruz as late enrollee · T2"). Records placement section's StatusBadge gets an inline "· T2" amber suffix for late-enrollee rows, derived at render time from a preloaded terms-by-AY map (`preloadTermsForAYs` + `termForDateInPreloaded` — bulk variant for the cross-AY records page). Falls back to "between terms" when the date sits outside any defined term window (e.g., enrolled during T2/T3 break).
69. **Documents-stage parent-email-conditional gate** (Sprint 29, 2026-04-30). Extends KD #61's STP-conditional pattern to parent slots: father slots (`fatherPassport`, `fatherPass`) are skipped from the documents-Verified/Finished gate when `fatherEmail` is empty/null on the apps row; guardian slots (`guardianPassport`, `guardianPass`) when `guardianEmail` is empty/null. Mother stays always-required (anchor parent). Apps-row select in `app/api/sis/students/[enroleeNumber]/stage/[stageKey]/route.ts:156` widened to fetch `fatherEmail` + `guardianEmail` alongside `stpApplicationType`. Gate skip-keys at line ~183 grew from 2 sets (optional + STP) to 4 (optional + STP + father + guardian). Closes the gap where single-mother households were blocked from progressing to Verified because father docs were null. Companion change: `OPTIONAL_DOCUMENT_SLOT_KEYS` (`lib/sis/queries.ts:627`) extended to include `'form12'` alongside `medical` + `educCert` — Form 12 is admissions-side optional. Both consumers (gate + `<DocumentsViewer>`) pick up the new entry automatically.
70. **Admissions chase scope split** (Sprint 29, 2026-04-30). Documents in two distinct workflows that share statuses but differ semantically: **chase** (parent owes us something) = `'To follow'` + `'Rejected'` + `'Expired'`; **awaiting validation** (we owe a review) = `'Uploaded'`. Admissions PriorityPanel (`lib/admissions/priority.ts`) ranks by chase-only signals (`toFollow + rejected + expired`) so headlines aren't inflated by routine pending-review queue items. New `expired: number` counter on `AdmissionsCompleteness` + `'expired'` member of `AdmissionsChaseStatusFilter` + STATUS_VIEW_META entry on `/admissions` + `?status=expired` sidebar quicklink + applications-list filter. `admissionsChaseInsights` gains `chaseExpired` narrative branch (severity `bad`, "chase before enrollment stalls"). Sidebar's "Document validation" entry split per module — admissions one routes to `/admissions?status=uploaded` (un-enrolled validation queue), P-Files one routes to `/p-files?status=expired` (enrolled renewal queue); each module owns its own validation surface, no cross-module link. P-Files's chase-strip + dashboard prune (KD #71) is the symmetric subtractive half. Phase-1 shared infra: notify/promise/bulk-notify routes accept `module: 'p-files' | 'admissions'`; email template `lib/notifications/email-pfile-reminder.ts` accepts `kind: 'renewal' | 'initial-chase'`; `getDocumentChaseQueueCounts(ayCode, module)` per-module bucket gating (admissions → revalidation = Rejected only, drops expiringSoon; p-files → revalidation = Expired only, drops promised + validation). New audit actions: `admissions.reminder.{sent,bulk}`, `admissions.mark.promised`.
71. **P-Files renewal-only scope guard** (Sprint 29, 2026-04-30). KD #31 said P-Files is enrolled-students-only; this pass enforces it. **Enrollment gate** on `/p-files/[enroleeNumber]` via new `lib/p-files/queries.ts::isStudentEnrolled` (whitelist: `applicationStatus IN ('Enrolled', 'Enrolled (Conditional)') AND classSection IS NOT NULL`) — pre-Enrolled applicants 404 instead of leaking through to the detail page. **Sidebar prune**: only `?status=expired` + `?expiring=30|60|90` quicklinks survive; `?status=missing|uploaded|complete` removed (those are admissions-territory per KD #70). **Dashboard prune**: drops Pending Review KPI (replaced with Expiring ≤30d), drops `<TopMissingDrillCard>`, simplifies `SlotStatusDrillCard` donut from 4 slices to 2 (On file + Expired), removes `pfilesInsights` `pendingReview` branch, replaces "Has Missing" summary card with "Expiring ≤90d" (added `expiringSoon90` to `DashboardSummary`). `<DocumentChaseQueueStrip>` mounts with `module="p-files"` so it surfaces only revalidation (Expired) + expiringSoon, dropping promised + validation tiles. Companion data-shape fix: `'to-follow'` promoted to first-class `DocumentStatus` member (`lib/p-files/document-config.ts`) + `resolveStatus` rewritten so `<slot>Status` column is the single source of truth (null rawStatus → 'missing' regardless of URL — closes the bug where legacy partial-write rows with URL but no status silently passed as Valid).
72. **Class template subject CRUD via 3-tab structure** (Sprint 29, 2026-04-30). `/sis/admin/template` editor expanded from 2 tabs to 3: **Sections** (existing) · **Subjects** (new — catalog management) · **Subject weights** (renamed from "Subjects"; matrix unchanged). Subject catalog tab (`<SubjectsCatalogTab>`) lists every `public.subjects` row with code badge / name / Non-exam badge / "Used at N levels" subtext + a per-row "Drop from all levels" bulk action when N > 0. Catalog supports **INSERT only** via `POST /api/sis/admin/subjects/catalog` (uppercase code regex from `lib/schemas/subject.ts::SubjectCreateSchema`, 409-on-duplicate with `existingId`); rename/delete remain SQL-only because subjects FK to historical grade entries via subject_configs (KD #4). New audit action `subject.create` + new `'subject'` entity type. (Subject × level) full CRUD on the matrix tab: dashed cells gone, replaced by a single "+ Add level" `DropdownMenu` per subject card listing every markbook-eligible level not yet enabled (preschool excluded automatically). New `POST /api/sis/admin/template/subject-configs` (with profile-default weight pre-fill — primary 40·40·20, secondary 30·50·20, max slots 5/5/30 matching `lib/sis/seeder/structural.ts:129`); new `DELETE /api/sis/admin/template/subject-configs/[id]` for "Remove from this level" (left-aligned destructive two-step confirm in the existing edit dialog); new bulk `DELETE /api/sis/admin/subjects/catalog/[id]/configs` for "Drop from all levels". Audit actions: `template.subject_config.create` + `.delete` + `.bulk_delete`. Empty cells hidden in BOTH the template Subject weights tab and the per-AY `/sis/admin/subjects` matrix — chip row only shows configured pairs; subject card shows "Not enabled at any level yet" empty state otherwise. KD #66 (template tables + UPSERT propagation) stays valid — these CRUD operations target `template_subject_configs` only; existing AYs untouched until the user clicks "Apply template."
73. **Parent surface is sidebar-less; layout never bounces parents to `/` or `/login`** (Sprint 30, 2026-05-03). The `/parent/*` route group is now a single-purpose avenue for viewing report cards — no module switcher, no module sidebar, no "Parent Portal" branding. `app/(parent)/layout.tsx` renders a thin print-hidden top header (HFSE identity tile + signed-in email + "Back to parent portal" button via `app/(parent)/parent-signout-button.tsx`) and a `<main>` wrapper. When the `parent_session` cookie is invalid, the layout sends real staff (`role !== null`) to `/` and **everyone else** — anonymous OR null-role Supabase JWT (a legacy `setSession` leftover from pre-KD-#65) — to `NEXT_PUBLIC_PARENT_PORTAL_URL`. This closes the path where stale null-role JWTs landed parents on `/login` (which then bounced via the proxy to the SIS module picker on `/`). Parent empty states are now polished dedicated pages with case-aware copy: `/parent` distinguishes `allScheduled` / `allExpired` / `revoked-or-never-published` via a `<EmptyStateCard>` helper; `/parent/report-cards/[studentId]` distinguishes `expired` / `scheduled` / `revoked` / generic via a `<UnavailableState>` helper. Both helpers use the gradient icon tile + serif title + mono-uppercase eyebrow recipe (matching design-system §8 T1 chip/CTA + §9.3 status palette: amber for time-bounded windows, destructive red for revoked). KD #11's "parents = null-role Supabase users" pattern is **deprecated for fresh logins** but still tolerated as fallback identity; KD #12 was already replaced by KD #65. KD #65's HMAC cookie remains the auth carrier; this KD codifies the layout chrome decision + the "never route parents to `/`" guarantee.
74. **Page-level role differentiation closes the read-only oversight gap** (Sprint 30, 2026-05-03). Three module dashboards previously treated all allowed roles as the same viewer; this pass adds an `isOperational` (or `isOfficer`) flag at the page RSC and gates the operational top-of-fold tiles behind it. **P-Files** (`/p-files`, `/p-files/[enroleeNumber]`): page-level role check fixed to admit `school_admin` (was a real bug — gate-allowed in `ROUTE_ACCESS` but blocked at the page level). New `isOfficer = role === 'p-file' || role === 'superadmin'` gates `<PriorityPanel>` + `<DocumentChaseQueueStrip>` + bulk-notify in focused-status views; admin/school_admin see hero copy reframed as "Read-only oversight" and the analytical KPIs+charts only. Detail page eyebrow flips to "Read-only oversight" when `canWrite=false`. **Records** (`/records`): new `isOperational = role === 'registrar'` gates `<DocumentChaseQueueStrip>` + the "Documents to collect" `ActionList` + `<ClassAssignmentReadinessCard>`; school_admin/admin/superadmin see the analytical surface only. **Admissions** (`/admissions`): new `isOperational = role === 'admissions' || role === 'registrar'` gates `<NewApplicationsPriority>` + chase strip + chase `<PriorityPanel>` + chaseInsights; oversight roles skip these but keep KPIs + drill cards. **SIS Admin** (`/sis`): hero copy now branches by access tier (`superadmin` = "System administration", `admin` = "Academic admin", `school_admin` = "School admin"); the Access + System sections (Approvers / School Config / Users / Settings — all superadmin-only) are now **hidden entirely** for non-superadmin instead of rendering as greyed-out cards. **Sidebar prose** (`lib/auth/roles.ts::PFILES_NAV`): "Expiring soon" quicklinks gated to `requiresRoles: ['p-file', 'superadmin']` (officer-only — those are bulk-remind launching pads); "Document validation" quicklink expanded to include `school_admin`. Audit summary in `~/.claude/plans/hmmm-im-thinking-cause-kind-crystal.md`. KD #57 (Layout archetypes) stays the umbrella — this KD records the four role-split implementations under it.
75. **Publishing checklist is also a navigation hub** (Sprint 30, 2026-05-03). `components/admin/publish-window-panel.tsx` `<AlertDialog>` rebuilt with a `<ChecklistRow>` helper per check (status-tinted gradient icon tile — mint→sky for passed, amber for warning — + serif title + mono eyebrow + Badge count + per-row deep-link button). Each failing row routes the registrar straight into the relevant module to fix the issue without losing dialog state: grading sheets → `/markbook/grading?section={sectionId}`; adviser comments → `/evaluation/sections/{sectionId}`; attendance → `/attendance/{sectionId}`; T4 unlocked terms + missing quarterly grades → `/markbook/grading?section={sectionId}`. Dialog widened (`max-w-2xl`) to fit per-row action buttons; T4 sub-section sits behind a centered horizontal-rule divider with mono-uppercase eyebrow. Closes the "warnings → action" loop on KD #28 (publish-readiness is a soft gate). Quick-link buttons render on every row (passing or failing) so the dialog also serves as a launch pad to verify work is in order.
