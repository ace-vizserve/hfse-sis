---
name: project-layout
description: Directory tree for routes, libs, components, migrations. Read when creating new files, moving code between modules, or deciding where a new route or lib lives.
load: on-demand
---

<!-- Stable rule. NOT auto-loaded. Read via the Read tool when relevant. Edit only with explicit user approval. -->

## Project layout

Single deployable at the repo root. App Router lives in `app/`; there is no wrapper subdirectory.

```
proxy.ts                         auth + role gate
app/(auth)/login/
app/(dashboard)/                 neutral shell: /, /account, redirect stubs
app/(markbook)/markbook/         grading, report-cards, sections, change-requests, audit-log; compare/ (multi-AY × multi-term KPI grid per KD #78)
app/(attendance)/attendance/     dashboard (analytics) + sections/ (picker, KD #55) + [sectionId] (daily writer, KD #47); compare/ (KD #78)
app/(evaluation)/evaluation/     form-class-adviser writeups (KD #49); virtue-themes/ (registrar+ per-term virtue-theme editor, KD #137); compare/ (KD #78)
app/(admissions)/admissions/     pre-enrolment funnel (KD #51) + applications/{,[enroleeNumber],closed} (closed = terminal-stage archive, KD #70) + cohorts/{stp,medical,promised} (lifecycle observation lenses; pass-expiry mirror moved to /records-only since pre-enrolment travel-doc lapses surface via ?status=expired per KD #70); compare/ (kind="month" per KD #78)
app/(attendance)/attendance/     dashboard + sections/ + [sectionId] (daily writer, KD #47/#55) + students/[studentNumber] (per-student attendance detail)
app/api/parent/v2/               stateless Bearer API for the external admissions portal SPA — students/route.ts (parent→student linkage + active publication check) + report-card/route.ts (full ReportCardPayload gated on active window) + levels/route.ts (level catalog + next-level progression for the accepting AY, KD #153 — NOTE getCurrent/UpcomingAcademicYear ignore their _client param, so this route queries academic_years directly on the service client). Auth: Bearer access_token verified via service.auth.getUser(). CORS via lib/cors.ts (ADMISSIONS_PORTAL_ORIGIN allowlist). Rate-limited via lib/rate-limit.ts. No in-SIS parent pages or cookies (KD #65/#73 removed — see parent.md correction note).
app/(p-files)/p-files/           documents repository — page-level role split (officer = p-file/superadmin sees chase strip + chase priority; admin/school_admin sees read-only oversight without those operational tiles); compare/ (kind="month" per KD #78)
app/(records)/records/           enrolled students (cross-year via studentNumber) + dashboard (registrar = operational, school_admin/admin/superadmin = oversight) + audit + cohorts/{stp,medical,pass-expiry} + movements/ (audit-log-derived enrolment movement feed per KD #83); compare/ (kind="month" per KD #78)
app/(sis)/sis/                   admin hub (status+launch only, KD #154): ay-setup, calendar (audience-filtered per KD #76), sections, audit-log (Overview|Log — hosts the audit BI dashboard, KD #154), admin/{levels (grade-level catalog + progression + per-AY offerings, KD #153),roles (superadmin-only role→capability grant editor, KD #166),staff (RBAC staff directory — assignments + accounts cuts, KD #154),approvers,discount-codes,school-config,settings,subjects,template} — notes: /sis/sync-students page removed (KD #154; auto-sync cron per KD #90), /sis/admin/users → redirect stub into staff?view=accounts, evaluation-checklists + /sis/admin/sow removed (KD #108)
app/(classroom)/classroom/       the section x term staff workspace (KD #160) — /classroom (scoped class list) + /classroom/[sectionId] hub with
                                 sub-routes overview/grades/students/attendance/write-ups/timeline/settings. Term axis on ?term_id=. Attendance +
                                 write-ups sub-routes are adviser/oversight only and each carries its OWN capability check (ROUTE_ACCESS gates only
                                 the /classroom prefix, so a typed sub-route URL is not covered by the layout's check).
app/api/                         one folder per resource
lib/                             auth, compute, audit, schemas, <module>/{queries,mutations,dashboard,drill,compare}.ts; lib/sis/{environment,seeder}/* for the test-AY flow (KD #52) — seeder/prior-year.ts provisions AY9998 (KD #78); seeder/movements.ts seeds synthetic enrolment-movement audit rows per KD #83; lib/sis/cohorts.ts (cross-module STP / medical / pass-expiry lifecycle observation queries); lib/sis/process.ts (lifecycle observability rollups); lib/sis/levels.ts (word-form labels per migration 029); lib/sis/terms.ts (date→term resolution per KD #67 + #68); lib/sis/current-term.ts (date-derived current/default term resolver per KD #116); lib/sis/enrolment-position.ts (late-enrollee position resolver per KD #117); lib/sis/early-bird.ts (single-select early-bird rule per KD #118); lib/sis/section-transfer.ts + section-history.ts (mid-year transfers per KD #67); lib/sis/movements.ts (audit-log-derived MovementEvent loader per KD #83); lib/sis/records-compare.ts (Records compare-mode loader per KD #78); lib/sis/readiness.ts (4-step AY readiness engine per KD #109 — SOW step removed by KD #110); lib/dashboard/* shared dashboard primitives — range.ts (preset resolution + delta math; presets t1/t2/t3/t4/lastWeek/last15d/thisMonth/lastMonth per KD #79), windows.ts (term/AY windows + cross-AY active-term fallback per KD #79, uses service client per KD #54), insights.ts (per-module narrative engines), priority.ts (PriorityPayload type for operational top-of-fold per KD #57), compare.ts (CompareInput / parseCompareParams / buildCompareCells per KD #78); lib/<module>/drill.ts owns DrillRow shape(s) per KD #56; lib/<module>/compare.ts is the per-module compare-mode fan-out per KD #78; lib/markbook/change-request-status.ts (shared status pill config); lib/sidebar/{registry,use-realtime-badges,use-change-request-count}.ts (shared module-sidebar config per KD #58; use-change-request-count.ts is the extracted per-instance realtime changeRequests-count hook shared by the sidebar badge + the header notification bell, KD #156); lib/schemas/{subject,grading-sheet}.ts (SubjectCreateSchema per KD #72; SlotMetaPatchSchema per KD #105); — note: lib/schemas/sow.ts removed with the SOW teardown (KD #108) lib/auth/staff-list.ts (getTeacherList; 5-min cache); lib/p-files/freshen-document-statuses.ts (bidirectional auto-flip per KD #60); lib/supabase/paginate.ts (fetchAllPages — walks past PostgREST 1000-row cap, required for cross-AY bulk fetches); lib/hooks/use-debounced-refresh.ts (coalesces a burst of autosave writes into ONE router.refresh() once entry goes idle — the attendance Term sheet and the grading score grid both use it; moved out of components/attendance/ 2026-07-31 when it gained its second consumer); lib/attendance/adviser-dashboard.ts + adviser-dashboard-queries.ts (KD #171 — the PURE half holds tallyToday / unmarkedSchoolDays / headlineFor; the queries half composes ALREADY section-scoped loaders, never the registrar's school-wide ones); lib/classroom/{scope,terms,queries,timeline}.ts (KD #160 — scope.ts is the PURE capability resolver adviser|subject|oversight mirroring RLS, and the single source for canReadAttendance/canReadWriteups; sectionIds null = all sections, [] = none, never conflate; terms.ts delegates to the canonical resolveCurrentTermId + sgToday); lib/sis/provisioning/{temp-password,credential-workbook}.ts (supports scripts/provision-staff-accounts.ts — bulk staff-account creation via the Auth Admin API + an .xlsx credential handout; a SCRIPT not a migration because accounts live only in GoTrue-owned auth.users, and its output dir scripts/provision-output/ is gitignored since it holds plaintext temporary passwords); lib/data/countries.ts (canonical country-NAME list — COUNTRY_NAMES + COUNTRY_NAME_SET, from the MIT `countries-list` pkg; backs the nationality combobox AND the schema refine so offer-set and accept-set can't drift, KD #157); lib/markbook/subject-teacher.ts (pure buildSubjectTeacherNameMap — live subject teacher from teacher_assignments, never the drifting grading_sheets.teacher_name column, KD #158); lib/cors.ts (CORS allowlist + corsHeaders() for /api/parent/v2/* — reads ADMISSIONS_PORTAL_ORIGIN env var); lib/rate-limit.ts (IP + per-user rate limiting for /api/parent/v2/*); lib/auth/{capabilities,permission-map,require-capability}.ts + lib/schemas/role-permissions.ts (KD #166 — capabilities.ts is the PURE, client-safe vocabulary + DEFAULT_ROLE_CAPABILITIES parity seed; permission-map.ts is the server-only cached read of `role_permissions` (service client + arrays, never cookie client or Sets, tag `permissions`) falling back to defaults on ANY failure; require-capability.ts is signature-identical to requireRole so a route migrates in one line — the remaining ~90 requireRole sites stay as they are)
components/                      ui (shadcn, including `command.tsx` cmdk wrapper) + per-module folders + shared `module-sidebar.tsx` + `module-sidebar/{sidebar-header,sidebar-nav-item,sidebar-profile,sidebar-quick-action}.tsx` (KD #58 — replaces 8 per-module sidebar files); `notifications/notification-bell.tsx` (cross-module changeRequests bell mounted in every module layout's own header `<header>` bar, NOT part of module-sidebar.tsx, KD #156); `topbar-module-switcher.tsx` (used only on `/` + `/account` shells); components/classroom/* (class list + roster tables, sub-nav, settings form — KD #160; classroom-timeline.tsx groups by SGT day and collapses consecutive same-action-same-actor runs, KD #172); components/attendance/adviser-dashboard.tsx (KD #171 — the today strip COLLAPSES to one line when every class is marked); components/sis/{sensitive-input,country-combobox}.tsx (masked-by-default passport/pass input with reveal toggle + constrained nationality picker; both forwardRef + spread rest props so FormControl's id/aria-* reach the control, KD #157); components/attendance/{sheet-legend,use-debounced-refresh}.tsx (always-visible Term-sheet legend reading the same STATUS_CELL_WASH / COLUMN_TAG_COLOR maps as the grid); components/sis/command-palette.tsx (Cmd+K palette); components/sis/ay-readiness-pill.tsx (floating 4-step AY readiness dialog per KD #109 — SOW step removed by KD #110); components/sis/cohorts/{cohort-page-shell,medical-cohort-table,pass-expiry-cohort-table,stp-cohort-table}.tsx; components/sis/{lifecycle-aggregate-card,lifecycle-aggregate-row,student-lifecycle-timeline,stp-application-card,residence-history-editor}.tsx; components/shared/{document-status-visuals.ts,document-completeness-strip.tsx} (KD #168 — the single colour map behind the completeness strip, its popover, the outstanding chips and the legend; DocumentAction = what to DO drives colour, DocumentStatus = what HAPPENED drives the word + icon, which is what keeps one meaning per swatch); components/sis/role-permissions-editor.tsx (KD #166); components/sis/{pipeline-strip,stage-bucket-visuals}.ts(x) (9-stage applications-table progress strip + the shared bucket→fill/label map also read by student-lifecycle-timeline, KD #152; the data-table shell's `facetGroups` prop — `components/ui/data-table/{types,index}.tsx` — collapses many facets behind one toolbar popover, same KD); `components/ui/data-table/export-payload.ts` (pure — builds the CSV field list shared by BOTH export surfaces: `buildScreenFields(columns, visibleColumnIds, extraColumns, rows)` + `fieldsToCsvColumns`; drops object-valued columns by scanning EVERY value, never sampling the first, KD #162); `components/ui/data-table/column-label.ts` (single source for a column's DISPLAY NAME — `meta.label` → non-empty string header → `humanizeFieldName(id)` — read by BOTH the Columns visibility menu and the CSV header row; a column whose `header` is a render function MUST declare `meta.label`, enforced by `__tests__/ui/data-table-column-label-coverage.test.ts`, KD #161); components/sis/{section-transfer-dialog,section-roster-table,ay-accepting-applications-toggle,generate-index-button}.tsx; components/sections/section-row-actions.tsx (shared section-list `⋯` row-actions menu — open/generate-index/generate-sheets, role-gated, KD #84) + components/{sis,markbook,attendance}/sections-data-table.tsx (per-module section-list DataTables, KD #84); components/evaluation/virtue-themes-editor.tsx (KD #137); components/admissions/completeness-table.tsx; components/dashboard/* shared dashboard UI — DashboardHero, ComparisonToolbar (passes `presets` shortlist per KD #79), MetricCard (`drillSheet?: () => ReactNode` render prop per KD #56 + #80), InsightsPanel, ActionList, PriorityPanel (KD #57), DrillDownSheet (virtualized via `@tanstack/react-virtual` spacer-row pattern; scrollEl tracked via callback-ref + useState so Sheet portal close→reopen forces _willUpdate to re-detect the new scroll container), ChartLegendChip; CompareToolbar + CompareGrid (KD #78); `lib/cache/invalidate-drill-tags.ts` (per-AY revalidateTag helper per KD #80); charts/{trend,comparison-bar,donut,sparkline}-chart.tsx + .client.tsx (next/dynamic split per KD #80, recharts loaded on demand) + grouped-bar-chart.tsx (paired/grouped bars, ≤5 distinct hues, muted→grey — the attendance term trend + markbook top-5 subject trend per the approved design, KD #142/#143 updates; multi-series-trend-chart was deleted 2026-07-11) + ay-comparison-line-chart.tsx (current-AY solid + dashed-grey prior-AY overlays, endpoint labels, auto zero-line; the Insights trend chart) + chart-primitives.ts (shared YFormat/formatterFor); chart-skeleton.tsx (layout-stable per-kind placeholders for the dynamic-import wrappers); components/dashboard/insights/{insights-section,building-history-card,recommendation-callout,trend-delta-caption,compare-ay-picker}.tsx + lib/dashboard/trend-delta.ts (summariseAyTrend w/ inProgressPeriod honesty option + summariseSeriesMovement — the Insights caption/delta engine, KD #140 update); components/<module>/drills/{<module>-drill-sheet,chart-drill-cards}.tsx for drill wrappers (primary identifier linkified per KD #81; lazy-fetch on mount; in-drill scope toggle removed per KD #56). `app/api/<module>/drill/[target]/route.ts` sets `Cache-Control: private, max-age=60, stale-while-revalidate=300` on JSON responses (KD #80).
supabase/migrations/             001 → 106 (027 skipped; 052 + 060 each have two files with the same prefix; all applied to dev DB). 035–049 grading/subjects/evaluation/attendance hardening (see sprint history). 050 STP doc slots removed (KD #96); 051 hbl_overlay (KD #98); 052a audit-log indexes; 052b sync_grading_sheets_from_config RPC (KD #99); 053 annual_letter_grade (KD #100); 054 letterhead (KD #101); 055 non-examinable score entry (KD #104); 056 slot_labels reset; 057 slot_meta {label,date,page} (KD #105); 058 sow_master_templates + sow_published_versions + sow_class_instances + sections.curriculum_track (KD #108); 059 curriculum_track on template_sections + apply_template_to_ay RPC fix (KD #108); 060a sow_class_instances.has_partial_rebaseline + create_grading_sheets_for_scopes RPC (KD #108); 060b sow_subject_scopes table + seed (KD #108); 061 teacher-owned SOW — reshape sow_class_instances + re-scope evaluation_checklist_items to section_id + drop sync_grading_sheets_from_sow RPC (KD #110); 062–066 SOW schema full teardown — drop sow_subject_scopes, curriculum_track columns, published_version_id, sow_published_versions, sow_master_templates, sow_class_instances, SOW provenance columns on evaluation_checklist_items + grading_sheets (KD #108 complete); 067 enrolment terminal reasons + late-enrollee term override (KD #111); 068 attendance late-enrollee proration (KD #113); 069 restore stpApplicationStatus dropped by 067's create_ay_admissions_tables re-emit, heals existing AY tables (KD #119); 070 align ex_reason to HFSE — remap school_activity→mc + tighten attendance_daily_ex_reason_chk to mc/compassionate/vacation (KD #94); 071 generate_section_index_numbers RPC — registrar alphabetical index numbering, withdrawn-retired (supersedes dormant 042, KD #136); 072 generate_section_index_numbers re-bucket by section-tenure date (transfers bottom-pin; supersedes 071's enum bucket, KD #136); 073 report_card_publications.published_with_gaps jsonb — publish-anyway override snapshot (KD #139); 074 sections + template_sections.schedule + official virtue-section template reset (KD #144); 075 ay{YYYY}_enrolment_status.enrolledAt write-once timestamptz (the actual moment a student reached Enrolled — stage route stamps only when NULL) — heals existing AY tables + re-emits create_ay_admissions_tables from its newest live body (KD #119 hazard); 076 payment preference + referral columns (preferredPaymentScheme / preferredPaymentMethod / marketingReferrerName) on ay{YYYY}_enrolment_applications — heals out-of-band parent-portal DDL on every AY table + adds them to create_ay_admissions_tables; 077 levels_label_unique constraint on public.levels.label — supports admissions-driven level reconciliation (KD #152); 094 classroom_notes — private per-teacher class note, RLS select-own-only with NO oversight bypass, writes denied to authenticated (service-role route only, teacher_user_id from the verified session) (KD #160); 095 current-AY atomic switch + partial unique index; 096 p_file_reminder claim; 097 transfer_student_section atomic (withdraw+insert in ONE transaction, KD #67); 098 + 099 discount-code unique index (099 wires it into create_ay_admissions_tables); 100 school_config_parent_read — parents (null-role) may SELECT the letterhead row, mirroring rcp_parent_read; fixes the blank letterhead on the external portal, which reads that table directly as the parent (KD #165); 101 + 102 role_permissions — the editable (role, capability) grant table behind the capability layer, seeded from DEFAULT_ROLE_CAPABILITIES so applying it changes nothing (102 adds the subject-weights grants) (KD #166); 103 + 104 lock_down_definer_rpcs — revoke `execute` on seven SECURITY DEFINER functions from `authenticated` (103) AND `anon` (104); `authenticated` includes PARENTS in this system, and SECURITY DEFINER bypasses RLS, so a parent could renumber a class or seed grade entries via PostgREST (KD #167); 105 role_permissions grants for the academic coordinator — subjects + academic_year, capped at school_admin (KD #169); 106 the document-validation reassignment — the FIRST seed migration to REVOKE, so the parity test replays migrations in order rather than unioning inserts (KD #166 update)
docs/handoff/                    briefs written FOR another repo's agent (currently the external parent portal) — the SIS-side contract, the traps, and acceptance criteria; not a spec for work in this repo
docs/superpowers/                specs/ + plans/ — brainstorming spec docs and implementation plans produced by superpowers skills (not binding; living design artefacts)
```
