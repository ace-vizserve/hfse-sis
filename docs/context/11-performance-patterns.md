# 11 — Performance Patterns

The playbook for making pages feel fast in this app. Apply to every new feature. Adopted 2026-04-18 after a pass that cut per-nav auth latency from ~200–500ms to ~1–5ms and cached/parallelized the dashboard + grading list. Extended 2026-04-17 with a second pass: migrated all server-component layouts + pages off `getUser()`, scoped the grading list's `grade_entries` fetch to visible sheets, deduplicated the report card API, eliminated a double-fetch on the P-Files detail page, replaced a 1000-row O(n log n) merge with an O(n) linear merge on the audit log, hoisted `unstable_cache` wrappers to module scope, and fixed a 550-component re-render storm in the grading grid. Extended 2026-07-08 with a third pass: found and fixed the exact legacy-HS256 caveat documented in §1 below (it had actually been live in production this whole time — the project was never migrated to asymmetric JWT signing keys), added a `cache()` dedupe on `getSessionUser()`, and ran a read-only RLS/index audit of the hot tables (§10 — one real backlog item found, everything else already sound). Extended 2026-07-08 (later the same day) with §7, cross-checking an external Next.js perf write-up against this codebase (font subsetting + heavy dynamic imports already covered; hover/touch prefetch for off-viewport links and thumbnail image optimization added as forward-looking guidance; ISR ruled out as inapplicable to a role-gated, RLS-scoped app). Extended 2026-07-09 with §9, cross-checking a second Next.js + Supabase perf write-up (relational-select joins and targeted realtime filters were already in use but undocumented; Suspense streaming added as forward-looking guidance; connection pooling and `fetch({cache:'force-cache'})` ruled out as inapplicable to this app's Supabase-JS-client architecture). Audited 2026-07-09 (later same day) against the doc's own checklist — 3 parallel Explore agents hunted the whole tree for violations, each finding adversarially verified against source before fixing. Fixed: two school-wide N+1 loops in the Evaluation dashboard's teacher/registrar priority panels (`lib/evaluation/dashboard.ts`, batched into a shared `loadSectionWriteupPending` helper — was 2-3 queries per section, now 2-3 queries total); a genuine caching **correctness** bug found incidentally (`lib/sis/staff.ts::loadFormAdvisersBySection`'s `unstable_cache` key omitted `sectionIds`, so two pages calling it with different section sets for the same AY could serve each other's cached adviser map within the 60s window — now keyed on a sorted section-id string too); two non-hoisted `unstable_cache` wrappers (`lib/auth/teacher-emails.ts`, `lib/auth/staff-list.ts`); a `concat+sort` in the audit-log CSV export that should have been the linear merge §3 already documents (`app/api/audit-log/export/route.ts`); two sequential-await chains collapsed into `Promise.all` (`lib/evaluation/dashboard.ts::loadWriteupsUncached`, `app/(attendance)/attendance/page.tsx`); and 19 missing `loading.tsx` route segments across the 4 module dashboards, the 4 Insights pages, and 11 secondary list/detail pages. §1 auth, §5 re-render storms, and the realtime half of §8 came back clean. `npx next build` verified clean after every fix. Extended 2026-07-23 with §11: found and fixed two raw `<a href>` internal-navigation links that forced hard browser reloads, and discovered the client Router Cache's `staleTimes.dynamic` default (`0`) was re-triggering `loading.tsx` on every revisit to an already-loaded page (nearly the whole app is dynamically-rendered per §1's `cookies()`-based auth check) — raised to 30s in `next.config.ts`. Also began scoping granular `<Suspense>` streaming (§8's forward-looking bullet) for the module dashboards + Insights pages that combine several fast loaders with one genuinely slower aggregate — design in progress, not yet implemented. **Amended 2026-08-30 (app-wide query pass, phase 7): the "hoist the `unstable_cache` wrapper to module scope" rule in §2 and §9 is WRONG and has been rewritten.** The code disagreed with the doc in 110 of 118 places, and the code was right: Next derives the cache key from `cb.toString()` + `keyParts` + args and never from the wrapper's identity, so a per-call composition hits the same entry — measured against the real helper in `__tests__/perf/unstable-cache-composition.test.ts`, not argued. Per-call composition is also the only form available to a loader with a per-AY `tag`, which is what the 110 are. The rule it replaces is the one that actually matters: **`keyParts` must name every variable the callback closes over**, or two callers collide — the earlier `loadFormAdvisersBySection` bug in this same header, re-read correctly. Note the two prior mentions of hoisting below (2026-04-17, 2026-07-09) are left as history of what was done, not as standing guidance. **Extended 2026-08-30 with §13 — the close-out of the app-wide query/write pass (7 phases, 50 commits on `perf/app-wide-query-pass`, not merged):** the measured before/after for seven budget surfaces, every defect class fixed, the exempt lists and where they live, two migrations written and **not applied**, everything examined and deliberately left with the number that would justify revisiting it, and the claims the measurements killed. It also actions §10's index gap (migration 132) and corrects §10's instructions for doing so.

## 1. Auth on the hot path

- **Middleware uses `getClaims()`, not `getUser()`.** `lib/supabase/middleware.ts` calls `supabase.auth.getClaims()`, which verifies the JWT signature locally against the cached JWKS — no network round-trip per navigation. `proxy.ts` reads role via `getRoleFromClaims(claims)` from `lib/auth/roles.ts`.
- **`proxy.ts` matcher excludes `/api/*`.** API routes authenticate themselves via `requireRole()` inside each handler. Running the proxy on them was pure overhead.
- **Page / server-component code: use `getSessionUser()` from `lib/supabase/server.ts`.** It wraps `supabase.auth.getClaims()` and returns a lightweight `{ id, email, role: Role | null }` — everything a page needs without the Supabase Auth round-trip. All 3 layouts and 12 nested pages migrated off `getUser()` in the 2026-04-17 pass — greenfield pages should follow suit.
  - References: `app/(dashboard)/layout.tsx`, `app/(p-files)/layout.tsx`, `app/(parent)/layout.tsx`, `app/(dashboard)/grading/[id]/page.tsx`, `app/(dashboard)/admin/audit-log/page.tsx`, `app/(p-files)/p-files/page.tsx`.
- **When you still need `createClient()` for DB queries**, import both: `import { createClient, getSessionUser } from '@/lib/supabase/server'`. Call `getSessionUser()` first for auth, then `createClient()` only if the page does cookie-scoped DB reads beyond what `createServiceClient()` covers.
- **`app/(dashboard)/grading/page.tsx` reads `getClaims()` directly** (via `supabase.auth.getClaims()`) because it needs the raw claims object. That's fine — `getSessionUser()` is sugar, not mandatory.
- **API routes use `requireRole()`** from `lib/auth/require-role.ts`. As of 2026-04-25 it also authenticates via `getClaims()` (no Supabase Auth round-trip) and returns `{ user: { id, email }, role }`. If a handler needs more than id+email+role, look the full Supabase user up via `createServiceClient().auth.admin.getUserById(id)`.
- **Tradeoff:** claims reflect token state at issue time. A role change in `app_metadata` lands after the next token refresh (~1h). Fine for this app — staff roles are effectively static.
- **Caveat — hit in production, fixed 2026-07-08.** `getClaims()` only performs local verification when the Supabase project uses asymmetric JWT signing keys. This project was still on legacy HS256 (confirmed via `GET /auth/v1/.well-known/jwks.json` returning `{"keys":[]}`), so every `getClaims()` call across the app — `proxy.ts`'s middleware call plus every `getSessionUser()` call in every layout/page (~84 call sites) — was silently falling back to a real network `getUser()` round-trip. A single navigation could pay for that 3-4x (middleware → root layout → module layout → page.tsx), none of it deduped. The actual root-cause fix: migrated the Supabase project's JWT signing keys from HS256 to asymmetric ES256 via Dashboard → Project Settings → JWT Signing Keys ("Migrate JWT secret" → swap `anon`/`service_role` for the new publishable/secret API keys → "Rotate keys"). Re-checking the JWKS endpoint afterward confirmed a live ES256 key — `getClaims()` now verifies locally everywhere, no more silent network fallback. A `cache()` wrapper on `getSessionUser()` (to dedupe the ~84 same-request call sites down to one) was also evaluated as a complementary layer on top of this — check `lib/supabase/server.ts` for its current form before assuming either way. If this app is ever pointed at a different/new Supabase project, re-verify the JWKS endpoint isn't empty — this regressed silently once already.

## 2. Server-component data fetching

- **Default to parallel.** Two sequential `await supabase.from(...)` calls in a page is a bug. Use `Promise.all([...])`. Dependent queries run in waves — fetch IDs first, then the dependent counts in a single second-wave `Promise.all`. References: `app/(dashboard)/page.tsx::loadStatsUncached`, `app/(dashboard)/grading/[id]/page.tsx` (sheet fetch → `Promise.all([changeRequests, entries])`).
- **Combine count + rows in one query** when both are needed: `.select('id', { count: 'exact' })` returns the rows _and_ the exact count in a single round-trip. Don't issue a separate `head: true` count query next to a rows query against the same table.
- **Avoid `select('*')` on wide tables.** When the caller only needs a subset of columns (status, expiry, etc.), enumerate them. The `ay{YY}_enrolment_documents` dashboard query lists exactly the columns needed per slot instead of selecting all document-URL text fields. Reference: `lib/p-files/queries.ts::loadRawDataUncached`.
- **Filter at the DB, not in JS.** If searchParams carry a filter the DB can apply, push it there before fetching 500 rows to filter down to 5. Reference: `app/(dashboard)/admin/audit-log/page.tsx` pushes `sheet_id` and `action` into `.contains()` / `.eq()` before capping to 500 rows.
- **Scope wide-table fetches to an id set you've already derived.** Fetch the sheet list first (RLS-scoped), then pass those IDs into an `.in('grading_sheet_id', sheetIds)` filter on the blanks query — don't re-scan the full `grade_entries` table. Reference: `app/(dashboard)/grading/page.tsx`.
- **Cache what's safe.** School-wide, service-client reads → wrap in `unstable_cache` with a short TTL and a descriptive tag. References:
  - `app/(dashboard)/page.tsx::loadStats` — 60s TTL, tag `dashboard-stats`.
  - `lib/admissions/dashboard.ts` — 10min TTL, tag `admissions-dashboard:${ayCode}`.
  - `lib/p-files/queries.ts::loadRawData` — 600s TTL, tags `p-files-dashboard`, `p-files-dashboard:${ayCode}`.
- **Composing the `unstable_cache` wrapper per call is fine. Name every captured variable in `keyParts`.** _(Amended 2026-08-30 — this bullet previously read "Hoist the `unstable_cache` wrapper to module scope", on the grounds that per-call composition "can break cache stability across Next.js versions". That was never measured, and it is wrong. It is recorded here rather than deleted because the codebase does the opposite in 110 of 118 places, and a future pass must not "fix" them back on the strength of a stale rule.)_
  - **What Next actually keys on** (next 16.2.10, `node_modules/next/dist/server/web/spec-extension/unstable-cache.js`): `fixedKey = cb.toString() + '-' + keyParts.join(',')`, then `invocationKey = fixedKey + '-' + JSON.stringify(args)`. The wrapper object's identity appears nowhere in it, and the store it reads is the shared `incrementalCache`, not per-wrapper state. A wrapper composed fresh on every call therefore produces a byte-identical key and hits the same entry.
  - **Measured, not reasoned.** `__tests__/perf/unstable-cache-composition.test.ts` runs the REAL helper: two separately-composed wrappers request the identical cache key and the second call is served from the store with the underlying loader running once. The hoisted form is proven to behave identically — the two are equivalent, not better and worse.
  - **Per-call composition is frequently the only option, not a shortcut.** `tags` are fixed at composition time, so any loader whose tag is per-AY (`sis:${ayCode}`, `admissions-dashboard:${ayCode}`) cannot be hoisted at all. That is why the split runs the way it does: all 8 hoisted loaders carry STATIC tags, and the ~110 composed-per-call ones carry per-AY tags. See the note at the top of `lib/sis/dashboard.ts` (KD #46), which stated this correctly before the doc caught up.
  - ⚠ **The real hazard is a cache COLLISION, and hoisting does not prevent it.** Because `cb.toString()` is the same literal text whatever the closure captured, a value captured by the closure and absent from both `keyParts` and the arguments is invisible to the key — two callers with different inputs then serve each other's answer, which is a wrong result and not merely a stale one. This has bitten this repo once already (`lib/sis/staff.ts::loadFormAdvisersBySection` omitted `sectionIds`; see this doc's 2026-07-09 header note). A value passed as an ARGUMENT is keyed automatically via `JSON.stringify(args)`; a value closed over is not. The collision is reproduced in the test file's last case.
  - References for the composed-per-call form: `lib/admissions/dashboard.ts::loadJoinedRows`, `app/(sis)/sis/page.tsx::loadUnassignedAdviserSectionsCached`, `lib/sis/staff.ts::loadFormAdvisersBySection` (the corrected keyParts). For the hoisted form: `lib/auth/teacher-emails.ts`, `lib/sis/health.ts`, `lib/dashboard/windows.ts`. ⚠ The two references this bullet used to carry were both stale: `app/(dashboard)/page.tsx::loadStats` no longer exists (that file contains no `unstable_cache` at all), and `lib/admissions/dashboard.ts` was cited as an example of hoisting when all 8 of its wrappers are composed per call.
- **Don't cache RLS-scoped queries.** Rows differ per user; caching creates either cross-contamination risk or degenerates into per-user cache slots with no real win. Just parallelize them. Reference: `app/(dashboard)/grading/page.tsx`.
- **Top-level parallelization.** Even cached calls should overlap with other fetches. Kick off multiple independent `Promise`s and `await Promise.all([...])` — don't pay for them serially. Reference: `app/(dashboard)/page.tsx` (`loadStats` + `getPipelineCounts` + `getOutdatedApplications` all in one `Promise.all`).
- **Deduplicate query pipelines.** If a page and an API route both need the same data shape, extract one canonical builder and have both call it. Reference: `lib/report-card/build-report-card.ts` is the single source; `app/(dashboard)/report-cards/[studentId]/page.tsx`, `app/(parent)/parent/report-cards/[studentId]/page.tsx`, and `app/api/report-card/[studentId]/route.ts` all consume it.
- **Return the raw row alongside the computed view** when the caller is going to need both. Forcing the consumer to re-fetch the same row doubles the round-trips. Reference: `lib/p-files/queries.ts::getStudentDocumentDetail` returns `{ ...completeness, rawDocRow }` so the detail page doesn't re-fetch `enrolment_documents` for the file URLs.
- **Tag invalidation (future):** when freshness matters more than 60s, call `revalidateTag('dashboard-stats')` from the mutating API route. Not wired yet — current TTL covers it.
- **`sis:${ayCode}` is ONE AY-wide tag over 49 loaders, and that is settled — do not split it.** _(Measured 2026-08-30, app-wide query pass phase 7. Re-measure with `npx tsx --conditions=react-server --env-file=.env.local scripts/probe-sis-tag-rebuild-cost.ts` before reopening; the `--conditions=react-server` flag is what lets a plain script import a `server-only` module.)_ 29 `revalidateTag(\`sis:${ayCode}\`)`call sites across 24 route files — plus`invalidateDrillTags('records', ay)`, which emits the same tag — bust a tag carried by **49 `unstable_cache`loaders across 21 files** (of 118 compositions app-wide;`lib/sis/dashboard.ts` alone holds 16). So any one field edit drops all 49. It still is not worth splitting, for three measured reasons:
  - **The sum is never paid.** The two pages that consume most of the 49 — `/sis` and `/records` — each fire their loaders inside ONE `Promise.all`, so a rebuild costs the SLOWEST loader, not the total. Cold: ~1.0s on Records, ~3.8s on the hub, against sums of ~4.2s and ~5.9s.
  - ⚠ **The loader that dominates that cold column rebuilds in ~1 ms after a `sis:` emit.** `getHubModuleOverview` (3.8s cold, 8-10× the next) fans out across all six modules, and those six children carry their OWN tags (`markbook:${ay}`, `admissions-dashboard:${ay}`, p-files, attendance, evaluation) — a `sis:` emit never evicts them, so the parent is pure composition over six cache hits. **Its multi-second figure is a cold deploy, not a field edit**, and reading it as the cost of invalidation is the trap this bullet exists to stop.
  - **What remains is ~0.5s on the hub and ~1s on Records, and a correct narrower tag would not remove it** — the loaders that set those numbers (`getRecordsKpisRange`, `getAyReadiness`, `getHubSnapshot`) are exactly the ones a Records/SIS field edit legitimately changes. Every entry also carries a 60-600s TTL, so the same rebuild happens on a timer regardless; the tag only moves it earlier, which is the freshness it exists to buy.
  - Figures are upper bounds — measured from a dev machine, while production runs closer to the database.

## 3. In-memory work in server components

- **Linear merge over sort() for pre-sorted arrays.** When two DB queries both return `ORDER BY X DESC` and you need to merge-cap to N, a standard linear merge is O(n) and allocates nothing; `concat().sort()` is O(n log n) and constructs a `Date` per comparison. Reference: `app/(dashboard)/admin/audit-log/page.tsx` (two-pointer merge of `audit_log` + legacy `grade_audit_log`, bounded by `merged.length < 500`).
- **Don't construct `Date` objects inside sort comparators.** ISO-8601 timestamp strings compare lexicographically, so `a.at >= b.at` works directly. Every `new Date(x).getTime()` allocation in a sort callback runs per comparison — with 1000 rows that's ~10k allocations.

## 4. Loading UX

- **Every route segment with >1 server fetch on first paint needs a `loading.tsx`.** Without one, Next.js sits on the previous page until the RSC work resolves. Navigation feels broken even when it isn't.
- **Reuse `Skeleton`** from `components/ui/skeleton.tsx` and wrap in `PageShell` so the sidebar frame doesn't jump.
- **Tailor the shape to the page** (card grid, table rows, document). A generic spinner is worse than a well-matched skeleton because the layout shifts when real content arrives.
- **Shared skeletons** live next to the component they mirror. Reference: `components/report-card/report-card-skeleton.tsx` is reused by both `app/(dashboard)/report-cards/[studentId]/loading.tsx` and `app/(parent)/parent/report-cards/[studentId]/loading.tsx`.
- **Skip `loading.tsx`** on static-ish pages (login, account, forms with no server fetching) — it adds visual noise for no gain.

## 5. Client-side rendering & state

- **Don't put mutable state in `useCallback` dependency arrays if a ref will do.** `ScoreEntryGrid::patchEntry` used to depend on `rows`, which meant every successful save recreated the callback, which changed the `onCommit` prop on 50×11 = 550 cells and re-rendered the whole grid. Fix: `const rowsRef = useRef(rows); rowsRef.current = rows;` and read `rowsRef.current.find(...)` inside the callback, with `rows` removed from the dep array. The state update path (`setRows(current => ...)` functional form) already works fine. Reference: `components/grading/score-entry-grid.tsx`.
- **Rule of thumb:** if a callback only _reads_ a piece of state to compose a toast or log message (not as part of the update itself), a ref is correct. If it uses the value to decide what to write, use the functional setter.

## 6. Client-side data (TanStack Query)

- **Not adopted app-wide.** RSC + `unstable_cache` handles almost everything this app does. Adding a client-side query lib to RSC pages gives you nothing and costs bundle size.
- **One legitimate candidate:** the grading grid autosave (`components/grading/score-entry-grid.tsx`). Optimistic updates, retry/rollback, and dedupe would be a genuine UX win there. Targeted refactor only — don't spread it to RSC pages.

## 7. Patterns from external review (2026-07-08)

Cross-checked against a public write-up of common Next.js wins ([5 Next.js Performance Patterns That Cut Our Load Time by 60%](https://dev.to/mzunain/5-nextjs-performance-patterns-that-cut-our-load-time-by-60-11gk)). That app is a public, unauthenticated content site — several of its patterns map cleanly here, others don't apply to an authenticated, RLS-scoped internal SIS. Verified against this codebase before writing any of it down:

- **Font subsetting — already handled.** `app/layout.tsx` loads `Inter` / `JetBrains_Mono` / `Source_Serif_4` via `next/font/google`, which self-hosts and subsets automatically at build time. No action needed; don't add a raw `@font-face` + manual `unicode-range` unless a _non-Google_ display font is introduced later, in which case subset it the same way.
- **Dynamic imports for heavy, minority-use components — already the pattern, keep extending it.** `next/dynamic({ ssr: false })` is already used for every recharts chart wrapper (KD #80, `components/dashboard/charts/*`). Apply the same treatment to any future component with a large third-party payload that most sessions never touch (e.g. a rich editor, a PDF viewer, the SheetJS-backed attendance export path) — but not to something every page render needs, where the dynamic-import overhead just adds a waterfall.
- **Image optimization pipeline — low relevance today, note for later.** The source article's win was compressing user-uploaded photos shown at thumbnail size. This app's uploads (P-Files document slots, `idPicture` etc.) are predominantly PDFs/scans handled through `pdf-merger-js`, not `<img>` thumbnails, and `next/image` usage in the repo is minimal. If a future feature adds photo thumbnails (staff avatars, a student photo grid), route them through `next/image` (auto WebP/AVIF + responsive `sizes`) instead of serving the raw upload — don't build this preemptively.
- **Intelligent prefetch on hover/touch — genuinely new, worth adopting selectively.** `next/link` already prefetches on-screen links automatically in the App Router, so this only helps links that never enter the viewport observer: rows inside a virtualized/lazy-mounted `DrillDownSheet` (KD #56) or a collapsed table body. For those, an explicit `router.prefetch(href)` fired on `onMouseEnter` / `onTouchStart` (from `next/navigation`'s `useRouter()`) can shave the click-through from ~1-2s to near-instant. Don't apply this blanket to every link — it's only a real win where the default viewport-based prefetch can't fire.
- **ISR (`getStaticProps` + `revalidate`) — does not apply.** ISR trades freshness for serving the same static HTML to every visitor; every page in this app is role-gated and often RLS-scoped per user, so static regeneration would either leak cross-user data or be a no-op. The correct analog already in use here is `unstable_cache` on service-client (non-RLS) reads (§2) — that's the pattern to reach for, not `export const revalidate`.

## 8. Patterns from a second external review (2026-07-09)

Cross-checked against a public write-up of Next.js + Supabase performance patterns (`iloveblogs.blog/post/nextjs-supabase-performance-optimization-2026`). Its headline numbers are from a different app and aren't verifiable here, so they're not repeated below — only the technique and whether it applies to this codebase. Verified against this codebase before writing any of it down:

- **Relational-select joins to avoid N+1 — already established, now documented.** Supabase's nested-select syntax (`table:related(cols)`, `!inner` for a required join) is already used throughout `lib/` to fetch a parent + its related rows in one round-trip instead of looping and re-querying per row. References: `lib/report-card/build-report-card.ts` (`section:sections!inner(id, name, form_class_adviser, academic_year_id, …)`), `lib/attendance/queries.ts`, `lib/markbook/masterfile.ts`, `lib/markbook/publish-readiness.ts`, `lib/markbook/grade-diff.ts`, `lib/markbook/academic-summary-scope.ts`, `lib/notifications/email-parents-publication.ts`, `lib/supabase/admissions.ts`. Follow this pattern for any new query that would otherwise fetch a list then fan out per-row lookups.
- **Strategic DB indexing — infra-level, not app-code.** Real, but it's a migration-authoring concern (foreign keys, `WHERE`/`ORDER BY` columns), not a pattern this doc's checklist can enforce from application code. When a new hot-path query filters or sorts on a column that isn't already indexed, add the index in the same migration — don't wait for a slow-query report.
- **Multi-layer caching — already covered, mapped to this app's actual stack.** The article's three layers translate directly to patterns already in §2: its "Next.js Data Cache (`fetch({cache:'force-cache'})`)" and "CDN/edge `Cache-Control` headers" are this app's `unstable_cache` (§2, per-route in `app/api/<module>/drill/[target]/route.ts` per KD #80) — `force-cache` itself doesn't apply because the Supabase JS client doesn't route through Next's patched `fetch` cache extension the way a raw `fetch()` call would. Its "React `cache()` for per-render dedup" **is** already in use here, separately from `unstable_cache`: `lib/dashboard/windows.ts`, `lib/academic-year.ts`, `lib/sis/approvers/queries.ts`, and `lib/dashboard/ay-id.ts` wrap a request-scoped loader in React's `cache()` so multiple call sites within one render don't re-issue the same query. Reach for `cache()` (not `unstable_cache`) when the goal is dedup-within-one-request rather than cross-request TTL caching.
- **Code splitting via dynamic imports — already covered.** See §7 (`next/dynamic({ ssr: false })` for chart wrappers, extend to future heavy/minority-use components).
- **Image optimization pipeline — already covered.** See §7 (low relevance today; route future photo-thumbnail surfaces through `next/image`).
- **Connection pooling (Supabase pooler, port 6543) — does not apply.** This app's only DB access is via the Supabase JS client (`@supabase/ssr` / `@supabase/supabase-js`), which talks to PostgREST over HTTPS — there's no direct Postgres connection for a serverless function to exhaust, so pooler configuration is a non-issue here. It would only become relevant if a future feature added a raw `postgres`/Prisma connection alongside the JS client.
- **Streaming with React Suspense — genuinely new, not yet adopted.** This app currently streams at the _route_ level only (`loading.tsx` per §4, which blocks the whole segment until every server fetch resolves). No page currently wraps an individual slow query in a `<Suspense fallback={...}>` boundary to let fast content paint while one slow query is still running. Worth adopting selectively on a page that combines one clearly-slow aggregate query with several fast ones — e.g. a dashboard where most cards resolve in <100ms but one drill-adjacent rollup is materially slower. Don't reach for this by default: route-level `loading.tsx` already covers the common case, and Suspense-per-query adds a component boundary + its own skeleton for each slow query it wraps. **Update (2026-07-23):** scoping started for the module dashboards + Insights pages, targeting whichever ones actually show a slow-vs-fast query imbalance (verified per-page, not assumed) — matching skeletons per §4, not generic spinners. Design pending; this note will be updated once a spec lands under `docs/superpowers/specs/`.
- **Targeted realtime subscriptions — already established, now documented.** `lib/sidebar/use-realtime-badges.ts` already subscribes to specific `INSERT`/`UPDATE` events (never `event: '*'`) and passes a server-side `filter` (e.g. `action=in.(...)` for the P-Files verification badge) rather than subscribing to a whole table and filtering client-side. Any new realtime badge/subscription should follow this same shape.
- **Parallel data fetching — already covered.** See §2 (`Promise.all`) and §7 (hover/touch `router.prefetch()` for off-viewport links).
- **Server Components by default — already the architecture.** This app is RSC-first (KD #24 Model A): pages load data in server components and only mark a component `'use client'` when it needs hooks, event handlers, or browser APIs. No action needed — this is the existing default, not a gap.

## 9. Checklist before shipping a new feature

Before marking any new page or feature done, walk through:

- [ ] No `getUser()` calls in middleware or server-component page code — use `getSessionUser()` (or `getClaims()` directly for the raw claims object).
- [ ] Any two sequential `await supabase.from(...)` collapsed into `Promise.all`.
- [ ] Combined `select('id', { count: 'exact' })` anywhere you need both rows and count against the same table.
- [ ] Explicit column list on wide tables — no gratuitous `select('*')` when only a subset is used.
- [ ] Searchparams / filter props pushed to the DB query, not applied in JS after a full fetch.
- [ ] Wide-table fetches scoped via `.in('x', ids)` against an already-known id set, not an unfiltered table scan.
- [ ] School-wide / service-client reads wrapped in `unstable_cache` with a descriptive tag. Composing the wrapper per call is fine and is usually forced by a per-AY tag — but **`keyParts` must name every variable the callback closes over**, or two callers collide and serve each other's answer (§2, measured in `__tests__/perf/unstable-cache-composition.test.ts`).
- [ ] Pre-sorted arrays merged with a linear two-pointer walk, not `concat().sort()`.
- [ ] No `new Date(...)` allocations inside sort comparators — compare ISO strings directly.
- [ ] `loading.tsx` added for any new route segment with real server-side fetching.
- [ ] Mutating API routes that touch cached data call `revalidateTag(...)` (when freshness matters more than the TTL).
- [ ] Any shared query pipeline (used by a page _and_ an API route) has exactly one canonical implementation.
- [ ] Per-cell autosave, optimistic updates, or retry logic? Consider TanStack Query; otherwise stay on raw state.
- [ ] `useCallback`/`useMemo` dependencies reviewed — mutable state only in deps if the callback's _write_ depends on it; otherwise read via `useRef`.
- [ ] Links inside a virtualized/lazy-mounted list (drill sheets, collapsed table bodies) get an explicit `router.prefetch(href)` on `onMouseEnter`/`onTouchStart` — the default viewport-based `next/link` prefetch can't reach them (§7).
- [ ] Any new uploaded-photo-as-thumbnail surface (not document/PDF slots) renders through `next/image`, not the raw upload (§7).
- [ ] A list-then-fan-out query pattern replaced with a Supabase relational select (`table:related(cols)` / `!inner`) instead of looping and re-querying per row (§8).
- [ ] New realtime subscriptions use a specific `INSERT`/`UPDATE` event + server-side `filter` — never `event: '*'` with client-side filtering (§8).
- [ ] Internal navigation uses `next/link`, never a raw `<a href>` — raw anchors force a full browser reload. Reserve `<a href>` for file downloads (`download` attribute) and `mailto:` links only (§11).

## 10. Supabase / Postgres backlog (audited 2026-07-08, not yet actioned)

A read-only RLS + index audit of every `CREATE POLICY` / `CREATE INDEX` across all `supabase/migrations/*.sql`, prompted by the §1 JWT fix above. Findings, so a future pass doesn't have to redo this work:

- **One real, actionable gap:** the dynamically-created per-AY admissions tables (`ay{YYYY}_enrolment_applications` / `_status` / `_documents` — DDL lives inside the `create_ay_admissions_tables` RPC body, base in `012_ay_setup_helpers.sql`, RLS added in `025_ay_tables_rls.sql`) have **no index beyond the `id` primary key** — no index on `"enroleeNumber"` or `"studentNumber"`, despite those being the columns the app filters/joins on constantly (`lib/sis/queries.ts`, `lib/sis/process.ts`, `lib/p-files/queries.ts`, `lib/sync/students.ts`, etc. — dozens of `.eq('enroleeNumber', …)` / `.eq('studentNumber', …)` call sites). Every subsequent migration (026, 029, 030, 031, 033, 050, 067, 069, 075, 076…) only adds/renames columns; none adds an index. **Fix, when prioritized:** add `create index ... on public.<slug>_enrolment_applications ("enroleeNumber")` (+ `"studentNumber"`) and matching indexes on `_status`/`_documents`, inside `create_ay_admissions_tables` itself (so future AY tables get them automatically) plus a one-time backfill loop over existing `ay26`/`ay27`/etc. tables (mirroring the `pg_tables` walk already in `025_ay_tables_rls.sql`). Not urgent at current row counts (~150-500 students/AY) — worth doing before a much larger enrolment volume or a bulk-sync-heavy AY rollover makes the sequential scans bite.
- **RLS policies on the named hot tables (`grade_entries`, `attendance_daily`, `section_students`, `audit_log`, `students`, `sections`) are all cheap — no action needed.** Every per-row subquery bottoms out in `teacher_assignments`, which is indexed on the exact filter column (`teacher_assignments_by_user`, `003_teacher_assignments.sql`); `audit_log`/registrar-gated tables use JWT-claim-only checks with zero subquery cost. `005_rls_teacher_scoping.sql:26-31` already flags this exact risk class in its own comment and proposes a composite `(teacher_user_id, section_id)` index as the fix _if_ query plans regress at higher volume — not needed yet.
- **All 5 RLS helper functions** (`current_user_role`, `is_registrar_or_above`, `is_teacher_for_section`, `is_adviser_for_section`, `is_teacher_for_sheet`) are correctly declared `stable security definer` — no volatility-related re-evaluation-per-row bug exists.
- **Not yet done:** an audit of server-component pages/layouts for sequential (non-`Promise.all`) `await` waterfalls beyond what §2 already documents — started but not completed in the 2026-07-08 pass. Worth a dedicated read-only sweep of the 6 module dashboard pages + all module `layout.tsx` files before assuming §2's guidance is fully applied everywhere.

## 11. Client-side navigation & router cache (2026-07-23)

A user-reported symptom — "every page hard-reloads / `loading.tsx` fires even on pages I already visited" — traced to two independent causes. Neither is a per-page bug; both are systemic and worth checking on any new page.

- **Internal navigation must use `next/link`, never a raw `<a href="/...">`.** A raw anchor forces a full document reload (fresh HTML request, full JS re-parse, full remount) instead of a client-side transition — the router cache, prefetching, and `staleTimes` below don't apply at all if the link itself bypasses the router. Found and fixed two production instances: `components/attendance/drills/chart-drill-cards.tsx` ("Mark today's attendance" → `/attendance/sections`) and `components/markbook/academic-summary/comments-view.tsx` ("Open Evaluation Module" → `/evaluation`). Audited every `<a href=` in the codebase to confirm no others — the remaining ones are legitimately `<a>` (CSV/XLSX exports needing the `download` attribute, `mailto:` links, and server-rendered HTML email templates outside the Next.js app).
- **`experimental.staleTimes.dynamic` defaults to `0`.** Because nearly every page here calls `getSessionUser()` (reads `cookies()`, §1), essentially the whole app is dynamically-rendered — and Next's client Router Cache treats a `dynamic: 0` route as instantly stale, so revisiting _any_ already-loaded page (sidebar nav, browser back/forward) always refetches from the server and re-shows that route's `loading.tsx`, even seconds after first paint. Set in `next.config.ts`:
  ```ts
  experimental: {
    staleTimes: {
      dynamic: 30;
    }
  }
  ```
  A `router.refresh()` call (KD #24, fired after most mutations) still forces a fresh fetch regardless of this window — raising it only smooths passive back-and-forth navigation, it does not risk showing stale data after a save.
- **Config changes require a full process restart.** `next.config.ts` is read once at server startup — a running `next dev`/`next start` process will not pick up a config edit no matter how many times the browser is refreshed. Restart the process after any `next.config.ts` change before concluding a config fix "didn't work."
- **Multi-checkout gotcha (worktrees):** if working in an isolated git worktree, verify which checkout the live dev/build server is actually running from before debugging "the fix didn't apply" — edits in one working directory are invisible to a server running from another, even on related branches.

---

## 12. The write path (2026-08-17, KD #186)

Everything above concerns reads. These are the write-side rules, learned by measuring — and in two cases by measuring and discovering the estimate was wrong by an order of magnitude.

### No serial per-row loops in a mutation handler

Attendance Submit wrote every roster row in a serial loop: insert + rollup + audit, per student. **`3N` sequential round-trips — about 90 for a class of 30.** It now uses `writeDailyBatch` (`lib/attendance/mutations.ts`): one insert for the whole class, rollups in bounded-concurrency waves of 8, audit rows in one parallel wave via `logActions`. **`1 + ceil(N/8) + 1` — 90 → 6.**

- **The wave bound is not arbitrary.** `writeDailyBulk` runs its rollups strictly sequentially because an import can carry 1,500+ pairs and firing those at once exhausts the connection pool. A class-sized batch is two orders of magnitude smaller and can afford concurrency, capped so that failure cannot reappear.
- **Validate the whole batch before writing any of it.** The route used to validate-and-write one at a time, so a rejection partway through left the earlier students marked and reported how many got through. A submit is now all-or-nothing.

### ⚠ `after()` is not for audit rows

Moving audit-log inserts into `after()` was tried and **reverted**. An audit row is a compliance record; if post-response work is ever dropped the trail gains a hole and nothing reports it — the same "failure that reads as good news" shape this codebase guards against elsewhere. Parallelising the inserts takes ~30 sequential round-trips to about one, which is nearly all of the win at none of that risk.

### The refresh is part of the write, and it must be awaited

`router.refresh()` fired and forgotten is why writes felt broken rather than merely slow — see KD #186. `useWriteAction` awaits it and holds the pending toast across it. Two consequences for anything on this path:

- **A doubled refresh doubles real work.** `app/(markbook)/markbook/grading/[id]/page.tsx:156` performs a **service-role RPC write on every render**, so a component that refreshes while its parent also refreshes runs that write twice. The guard test forbids a bare `router.refresh()` beside `useWriteAction` for exactly this reason.
- **The component that owns the write owns the wait.** Passing an `onSaved`/`onDone` callback whose whole body is `router.refresh()` splits the responsibility and reliably goes stale — three components had this and one of them shipped the bug.

### Examined and deliberately NOT done — do not re-add these

Each was on the original list; each stopped making sense once the code was read.

- **Scoping the 13-tag cache invalidation.** The premise was that a write pays for invalidating 13 tags. It does not — `revalidateTag` only marks entries stale, and the rebuild is paid by whoever next opens that page. The 180k-row attendance scan only rebuilds if somebody opens the attendance dashboard. Meanwhile a roster change genuinely does affect all six modules' rolled-up counts, so narrowing trades a benefit that could not be measured for staleness — the exact bug class this work was spent hunting. Three of the nine call sites (`generate-index`, `level-aliases`, `sections` create) are plausibly narrower and are worth revisiting **only with a measurement in hand**.
- **The grading page's render-time RPC.** Idempotent and load-bearing: it is what picks up a late enrollee added after the sheet was generated, and the comment above it says so. Removing it trades a real self-healing behaviour for one round-trip, and it cannot simply move after the fetch because the entries query depends on it.
- **Caching `requireCurrentAyCode()`.** It would have to move off the cookie client onto the service client inside `unstable_cache`, and be invalidated wherever `is_current` flips. The benefit is one small indexed SELECT per write; the failure mode is the whole app operating on the wrong academic year. Wrong trade.

### Still open

**~~14 write routes invalidate nothing.~~ REDONE 2026-08-27, and the old claim was wrong in three separate ways.** It now lives in `__tests__/cache/write-route-invalidation.test.ts` rather than in prose, because that is the only form of this list that cannot rot: every write route must either bust a tag or appear on an exemption list **with the reason it is safe**, and an entry that has since gained a bust or been deleted fails the build.

🔴 **`change-requests/act` was the headline example and it had invalidated since 2026-06-05.** It delegates to `lib/change-requests/decide.ts`, which calls `invalidateDrillTags('markbook', …)` — the audit had grepped route files only, so a bust one module down was invisible to it. Same for `change-requests/[id]`.

🔴 **The "60-second TTL" on the queue and bell does not exist.** The change-request queue page reads the service client directly with no `unstable_cache`; `getSidebarChangeRequestCount` says in its own header that it is deliberately uncached; the bell's preview sets `staleTime: 0`; and the badge is a realtime subscription. The only real window is the 30s client Router Cache (`next.config.ts`), which `revalidateTag` cannot touch at all.

🔴 **Six of the fourteen are `410 Gone` tombstones** (the PTC/checklist routes, KD #114 / #110). They write nothing.

**What was actually fixed:** the subject catalogue POST and PATCH, the level-offerings PUT, and the subject resync POST — all four write behind cached markbook or SIS readiness loaders, and the two sibling routes beside them had been busting correctly all along.

🔴 **And a defect nobody had recorded: `lib/markbook/overview-data.ts` tagged its cache entry `markbook:${academicYearId}` — a uuid — while every invalidator passes an `ay_code`.** `markbook:AY2026` never matched `markbook:<uuid>`, so that entry **was never busted by anything** and only ever expired on its TTL. It was the only AY-scoped tag in the codebase built from an id. Now pinned by a test that fails on any tag template interpolating something ending in `Id`.

**Genuinely still open, and named in the test rather than hidden:** `grading-sheets/[id]/labels`, `sections/[id]/schedule`, and `sis/admin/users/[id]` — the last being a real asymmetry, since the sibling POST busts `teacher-emails` and the PATCH/DELETE does not.

---

## 13. The app-wide query/write pass (2026-08-30, branch `perf/app-wide-query-pass`)

Seven phases, **55 commits (`15022a26..a2abdf9c`)**. Not merged, not on production. ⚠ **This section was written at the 51st commit (`4d6feec8`) and four more landed after it, from a `/code-review high` round whose four findings were every one verified real before acting and every one fixed**: the register's untie-broken paginated reads (`5af08c39` — ~25 rows share a `recorded_at` because that is what one class submit writes, so a boundary tie drops or duplicates a mark; three instances, worst 5,925 rows), `logActions`' retry narrowed so an ambiguous **throw** can no longer double a class submit in an append-only log (`e6afad51`), the 133 deploy-ordering note below (`92dc2de0`), and all three cache-tag guards widened from `lib/` to `app/` + `lib/` (`a2abdf9c`, no offender surfaced). The standing rule for the whole pass was **measure, don't estimate** — a prior audit raised six HIGH findings and half of them evaporated against production — so every claim below carries a budget number, a production count, or an explicit note that it rests on inference. The working ledger the pass was run from lives under `.superpowers/`, which is **gitignored**; this section plus the two findings docs it cites are the surviving record.

### The instruments, and which one is authoritative

- **`__tests__/_utils/counting-supabase.ts`** — a counting Supabase client reporting **`roundTrips` _and_ `waves`**. Those are two different claims and are kept apart everywhere below: `roundTrips` is how many queries were issued, `waves` is how deep the serial chain is. **Collapsing a waterfall changes no round-trip count**, so a latency fix must show `waves` move while `roundTrips` stays still — and in that case `roundTrips` is the _control_: if it moves, something was dropped.
- **`__tests__/perf/query-budget.test.ts`** — seven real surfaces, each carrying the date it was measured and a note on what it covers. Re-baselined at the end of every phase, so each phase compares against the tree it started from rather than against phase 0.
- **`scripts/audit/*.ts`** — five static scanners (cache tags, sequential awaits, unindexed filters, row-at-a-time writes, duplicate queries). ⚠ **They are coverage listers, not verdicts.** `sequential-awaits.ts` first shipped reporting 73 confident findings **not one of which touched a database**, while missing the known classroom waterfall entirely — because this codebase's waterfalls run through named helpers (`getTermsForAy`, `loadClassroomAccess`, `getSectionAttendanceSummary`) that contain no literal `.from(` for a text scanner to match. It was downgraded to ranking every file with 2+ sequential awaits as a worklist to _measure_. **Coverage from the scanner, truth from the harness.**
- **`scripts/probe-*.ts`** — four read-only production probes (query cost, write-amplification scale, import rollup cost, `sis:` tag rebuild cost). Every exempt row in this pass cites one of these or a schema constraint, never an estimate.

### What was measured — the seven budget surfaces

Before = the phase 0 baseline (2026-08-29). After = `HEAD`. **Round trips and waves are separate columns because they are separate claims.**

| Surface                                        | roundTrips  | waves           | Where the change came from                             |
| ---------------------------------------------- | ----------- | --------------- | ------------------------------------------------------ |
| `buildReportCard` (1 student)                  | 12 → **11** | 12 → **8**      | phase 3 (merge), phase 6b (shallow re-wave)            |
| `loadAdviserAttendanceDashboard` (2 sections)  | 46 → **42** | 21 → **11**     | phase 3 (duplicate read), phase 4 (two re-waves)       |
| classroom section page                         | 11 → **11** | 8 → **2**       | phase 4 — one `Promise.all`; round trips are the proof |
| `recomputeSheetEntries` (25 entries)           | 26 → **26** | 26 → **5**      | phase 5 — bounded waves of 8                           |
| `logActions` (30 rows)                         | 30 → **1**  | 1 → **1**       | phase 5 — one array insert; waves never moved          |
| relief bulk book (10 classes)                  | 13 → **4**  | 13 → **4**      | phase 5 — one `.update().in('id', ids)`                |
| attendance student detail _(added in phase 4)_ | 20 → **20** | **16** → **12** | phase 4; the "before" of 16 was measured, not asserted |

Read the two columns separately. `logActions` is a pure round-trip win with no depth change. The classroom page, `recomputeSheetEntries` and the student detail page are pure depth wins with round trips deliberately pinned. Only the report card and the adviser dashboard moved both.

**Wins that no seeded surface can show**, each measured elsewhere:

- `getSchoolConfig` request-memoised with React `cache()` — ~49 reads saved on a 50-student batch print, none on a single card.
- `subject_report_map` request-memoised — the same shape phase 3 missed: an **unfiltered whole-table read issued once per card**, ~50 per section print.
- A term attendance import spent **~34.5s** waiting on 404 rollup RPCs one at a time (median 85 ms each) → bounded waves of four, ~8.6s.
- `scripts/backfill/ay2025-grades.ts` issued **638 single-row statements** where four chunked ones do the work (620 sheets, 18 configs — both counted, not estimated).
- `/attendance/sections` — 9 sequential steps → 3. ⚠ **This one is not harness-guarded**: its evidence is a scanner re-run (11 sequential awaits / 0 `Promise.all` → 4 / 3), a coverage number rather than a wave count, because a reproduction needs the cookie-scoped client, the approvals counter and an `unstable_cache` adviser loader.

### What was fixed, by defect class

#### A. Cached loaders that no write has ever invalidated (phase 1)

**Thirteen loaders, two causes, both enumerated app-wide rather than sampled.**

1. **A uuid in the tag.** `lib/markbook/dashboard.ts` built `markbook:${academicYearId}` from a uuid while the only emitter emits `markbook:AY2026`; the strings could never match. Three panels fixed (`706c3904`). ⚠ **The uuid stays as a cache KEY — the AY _code_ is what goes in the TAG.** ⚠ The scanner flagged five sites here and **two were already correct**: it reads the helper's parameter _name_, not the argument passed.
2. **A lone bare tag nothing emits.** Ten loaders carried a single bare literal (`'sis'` ×7, `'markbook'`, `'levels'`, `'dashboard-windows'`) with no AY-coded sibling. The **only** bare-literal emitter app-wide was `revalidateTag('teacher-emails')`; `invalidateDrillTags` emits AY-scoped tags exclusively. Resolution: **6 fixed, 4 exempt as recorded decisions.** Five SIS charts already took `input.ayCode` as a cache key and had simply omitted it from the tag, so adding `sis:${ayCode}` was free (29 emitters already exist). Term windows are config, where staleness is a visibly wrong answer, so the AY-setup write routes now emit `dashboard-windows`. The three activity feeds have no AY to scope to and keep their 120s TTL **by decision, not by accident**. And `'levels'` cannot be fixed because there is no write route and there cannot be one — `/sis/admin/levels` and its three API routes were removed wholesale by migration 086 (KD #153) and the catalogue is a fixed 10 rows managed by migration; its source comment claiming writes _"should"_ emit the tag was stale and misleading, and has been rewritten.
3. **The guard then found two the enumeration had missed** — which is the argument for a scanner over a list. `getActivityByActor` is a fourth `audit_log` feed, exempt like the other three. `getSystemHealth` (`lib/sis/health.ts`) was a **real staleness bug nobody chose**: creating an AY, switching the current one, deleting one, or assigning an approver was invisible on the `/sis` readiness strip for up to 60s. It now carries its own `sis-health` tag emitted from five routes. Its two dead tags were **removed, not exempted**, and ⚠ **`'sis'` must not start being emitted** — it is the exempt tag on two activity feeds that emitting it would bust as collateral. ⚠ `lastAdminActivityAt` cannot be tagged at all (every admin action appends to `audit_log`), so the 60s TTL stays the contract for that one field.

⚠ **The failure mode worth carrying: an exemption that names ONE consumer is only as good as the search behind it.** Both approver routes sat on an exemption reading _"lib/sis/approvers/queries.ts is uncached"_. That reader really is uncached — and `lib/sis/health.ts` had been counting the same table from inside an `unstable_cache` the whole time.

Guards left behind: `__tests__/cache/ay-tags-are-codes.test.ts` (a runtime guard for the uuid class, not just the literal it hid in) and a new describe block in `__tests__/cache/write-route-invalidation.test.ts` for the lone-bare-tag case, gated on `!hasAyColonTag` — the exact complement of the existing test's gate, so **every tag in `lib/` is now examined by exactly one of the two**. Both were demonstrated red before green. ⚠ Its exempt key is `file|loader|tag`, not `file|tag`: three fixed loaders share a file with two exempt feeds, and a file-level key would have let a regression on any of them match the feeds' exemption and pass. ⚠ Its produced-set scan **strips comments** — `lib/sis/levels.ts` and `lib/auth/permission-map.ts` both quote `revalidateTag(...)` in prose, and counting a comment as an emitter marks the real gap as covered.

#### B. Serial waterfalls in read paths (phase 4, 8 commits)

`roundTrips` unmoved on every surface; only depth moved. 🔴 **Three of the four named instances were understated, and naming a _site_ instead of a _class_ would have left work behind:** `lib/sis/dashboard.ts`'s "two serial waves of 6 counts" was **one of five instances of the same shape across four modules** — compare mode cost 2× the latency of not comparing, everywhere — found by grepping `cmpFrom == null` across all of `lib/` rather than the one named shape. Guard: `__tests__/perf/no-serial-comparison-range.test.ts`, verified against `git show HEAD:` to have matched **all five** pre-fix sites, so it is known to catch its target rather than merely to pass.

Examined here and left, with the search named: `describeNonSchoolDay` (only asked once the fan-out has already produced the fact), the T1 term fallback, `getVacationLeaveUsage`'s internal chain, `levels-awaiting-sections`' per-year two-step, and the `admissions`/`attendance`/`evaluation` `dashboard.ts` trio, which _look_ like the five serial range builders but load rows once and bucket both windows in memory.

⚠ **One behaviour change, stated rather than hidden** (`1e9bac58`): if `loadTermTripContext`'s current-term expansion throws, the prior-term lookup now fires where before it did not. Success paths are byte-identical.

#### C. Duplicate and over-wide reads (phases 3 and 6b)

Two identically-filtered `attendance_records` reads on the report card differing only in projection; `getSchoolConfig` read once per card; the adviser dashboard reading each class's day twice (`todayMarks` was a second `getDailyForSection` over a window `allMarks` already contained); `getRollupForSection` asked for twice in one render; one enrolment read per register submit, hoisted so all roles pay one where teachers previously paid two. ⚠ On that last one the **error is carried into the gate rather than swallowed** — a failed lookup and an empty result both still refuse, because "no section ids came back" is not "no section to object to"; four refusal cases were asserted green _before_ the read moved.

#### D. Row-at-a-time writes (phase 5)

Full reconciliation: **`docs/superpowers/specs/2026-08-29-phase-5-write-amplification-reconciliation.md`**. `scripts/audit/row-at-a-time-writes.ts` enumerates twelve sites; the doc resolves **every one** to fixed or exempt-with-a-measured-count. ⚠ **Two more sites live in `scripts/`, which the scanner does not walk** — and that is where the largest instance was.

⚠ **`recomputeSheetEntries` keeps its 26 round trips on purpose, and saying so is part of the fix.** Every entry takes a different patch, and the obvious one-upsert-per-sheet form is verified NOT to work: `grading_sheet_id` and `section_student_id` are `not null` with no default, and Postgres checks NOT NULL on the proposed tuple _before_ resolving the conflict. Only depth moved. **This did not make the write atomic.**

#### E. Reads that silently lose rows (phase 6a)

PostgREST returns the first 1,000 rows of an over-cap response **with no error and no flag**, so each of these was a wrong answer that announced nothing.

- 🔴 **`markbook/grading/page.tsx` has been a page short of its own totals.** It chunked `grade_entries` 50 sheets at a time and never paged _inside_ a chunk; the comment above it did the arithmetic and reached the wrong conclusion — "~4000 rows per request, still under cap because grade_entries rows are skinny". **The cap counts rows, not bytes.** Measured 2026-08-30: AY2026 holds 260 sheets and the first chunk of 50 returns **1,029 rows**, so 29 entries were dropped on every render and the Graded percentage on those sheets read low. Chunking answers the URL-length ceiling and says nothing about the response ceiling; only one of the two was being answered.
- 🔴 **`loadGradingSnapshot` read every student the school has ever had, one page of them.** No filter at all — **760 rows against the 1,000 cap** on 2026-08-30 — and `lib/sync/students.ts` turns that list into the map answering "do we already have this student", so a truncated snapshot makes an existing student read as new. All 22 `.from('students')` sites across `lib`, `app` **and `scripts`** were checked; this was the only unfiltered whole-table read of it anywhere. Its sibling `section_students` read is left unpaged **and measured rather than waved through**: 414 rows over 22 sections in AY2025, bounded by one year's roster instead of by the table.
- **The audit-log Actor dropdowns** — see the corrections below; the real defect was one page and one actor.

The general guard `__tests__/data/no-unpaginated-high-volume-reads.test.ts` now walks `lib` + **all of `app`**, not `lib` + `app/api`. A server component is not an API route but reads the database exactly like one, which is the whole reason the grading-list defect sat unseen.

#### F. Indexes on the per-AY admissions tables (phase 2)

Full classification: **`docs/superpowers/specs/2026-08-29-phase-2-unindexed-filter-classification.md`** — 484 call sites across **86 distinct (table, column) pairs, every one classified exactly once**: 13 INDEX labels collapsing to **5 real statements per AY**, 32 outright scanner false positives, 33 bounded by a documented fact, 8 other. ⚠ The 13-to-5 gap is not double counting — four files build the AY table name from a template literal four different ways, so one physical table appears under several labels. See "Corrections to this document" for what the migration actions.

### ⚠ Two migrations are written and NOT APPLIED

Both are on the branch, neither has been run against production, and applying production DDL is not this pass's to do. **They share no object and can land in either order, or independently.** ⚠ **That is independence from EACH OTHER, and only one of them is independent of the CODE: 133 must be applied BEFORE this branch deploys, 132 need not be.** See 133's paragraph below.

**`132_ay_enrolment_indexes.sql`** — five non-unique btree indexes per AY (`_enrolment_applications("enroleeNumber")`, `("studentNumber")`, `_enrolment_status("enroleeNumber")`, `("applicationStatus")`, `_enrolment_documents("enroleeNumber")`), delivered as an idempotent `attach_enrolment_indexes(slug)` helper + a backfill + **one `perform` line** added to `create_ay_admissions_tables`. **15 indexes after backfill** (5 × 3 AYs). Its three acceptance queries:

```sql
-- 1. Fifteen rows, five per AY.
select tablename, indexname from pg_indexes
 where schemaname = 'public' and indexname like 'ay%\_enrolment\_%\_idx'
 order by tablename, indexname;

-- 2. The index is usable, and correctly quoted.
set enable_seqscan = off;
explain select 1 from public.ay2025_enrolment_applications
 where "enroleeNumber" = 'x';
-- expect: Index Scan using ay2025_enrolment_applications_enrolee_idx
reset enable_seqscan;

-- 3. Idempotent, and the RPC still works.
select public.attach_enrolment_indexes('ay2025');    -- no-op, no error
select public.create_ay_admissions_tables('ay2027'); -- still succeeds
```

🔴 **A `Seq Scan` after applying 132 is the EXPECTED result, not a failure**, and expecting otherwise would produce a false alarm. At 822 rows the table fits in a handful of heap pages and the planner correctly keeps choosing a sequential scan. **Query 2 is the honest proof** — and it also proves the camelCase double-quoting held, because unquoted identifiers fold to lowercase and would have built the index on a column that never matches.

**`133_audit_actor_emails.sql`** — a `stable`, **SECURITY INVOKER** `audit_actor_emails(p_actions text[])` returning distinct actor emails, so a dropdown of nine names stops being derived by reading the log. Invoker, not definer: migration 006's `audit_log_registrar_read` policy still decides what the caller may see, which is exactly why it can carry `grant execute … to authenticated`. ⚠ **That grant is load-bearing** — migration 114 revoked this same class of grant on a policy helper and blanked every teacher's Teachers tab until 116 put it back, and the caller here is a server component on the user's cookie client. Until it is applied, `lib/audit/actor-emails.ts` logs which page lost its actor list rather than failing the page.

🔴 **Deploy ordering — 133 must be applied BEFORE this branch deploys.** All three Actor dropdowns now call `audit_actor_emails` and nothing else, so the same rule KD #153 states for migration 078 applies here: code calls the new object, therefore the migration lands first. **Deploy before applying and all three filters render with no options — a regression on the two that were previously accurate** (attendance and evaluation both listed their actors correctly; only markbook was short a name, per the table in 133's header). 132 carries no such constraint: nothing in the code names an index, so it can be applied whenever.

**There is deliberately NO fallback, and re-adding one would undo this pass.** The degrade is an empty list, and it was chosen over a fallback because it is **visible** (an empty `<Select>` is not a silent wrong answer), **logged by page naming the migration file** (`loadAuditActorEmails` prints "has migration 133*audit_actor_emails.sql been applied?"), **harmless** (the dropdown is a \_filter* — an actor missing from it cannot be picked, but every one of their rows still appears in the log, and no page fails to render), and **self-healing** the moment the migration is applied, with no cache to bust and no redeploy. A fallback would trade all four for the `.limit(200)` projection this very pass measured wrong on production data. **Do not add one; do not restore the limit query.**

### Examined and deliberately NOT done — with the number that would justify revisiting

**This is the deliverable that makes the next sweep cheap.** Each of these was read, sized and left. Do not reopen one without the measurement named.

- **The report-card deep re-wave.** `buildReportCard`'s residual 8 waves would go to roughly 4, and each of the 8 currently names its own dependency in the budget file. It is **saved once per page, not once per student** — the roster fans out inside a `Promise.all`, so the depth is paid concurrently — and collapsing it needs a committed golden-payload fixture, because the existing test pins five things and a real payload has far more. **Gate: measured batch-print wall-clock. Under ~1.5s for a 40-50 student print it does not pay for the fixture it needs.** ⚠ It also goes nowhere near `preloadedCalendar.byTermAndLevel`, the Map mutated inside a `Promise.all` across every concurrent card.
- **Batching the attendance grid client-side.** Per-cell optimistic write/revert means a failed batch reverts cells marked minutes apart, and **a lost attendance mark is a compliance defect**. ⚠ **The "375 cells per sitting" figure must not be used to argue otherwise** — 302 distinct (teacher, day) pairs at median 375 / max 401, against a ~400-student school where a class is ~25, are bulk-import rows attributed to a service account. Interactive marking of one class cannot produce 401. **Reopen only with a count that excludes import-attributed rows and still shows a real teacher marking in the hundreds per sitting.**
- **Splitting the `sis:${ayCode}` tag.** 29 emitters (plus `invalidateDrillTags('records', ay)`, which was not in that count) bust one tag carried by **49 loaders across 21 files**, so any one field edit drops all 49. **Closed at 1 ms, measured** — the loader that dominated the cold column (`getHubModuleOverview`, 3.8s) fans out over six modules whose children carry their own tags, so a `sis:` emit never evicts them and the parent is pure composition over six cache hits. Its multi-second figure is a **cold deploy, not a field edit**. Full reasoning and the re-measure command are in §2's bullet. **Reopen only if that probe shows a `sis:`-scoped rebuild materially above 1 ms.**
- **Tightening the pagination scanner's 900-char statement window to the first `;`.** It would surface **8 more files, and all 8 are correct** — they use the `let q = …; q = q.eq(…); await q.range(…)` builder shape, where the `.range()` legitimately sits past a semicolon. Allowlisting eight correct files to catch one wrong one trains the next reader to skim the list. The one shape the window structurally cannot catch (the actor dropdowns, where it over-reads into a neighbouring query's `.range(`) got a precise test of its own instead: `__tests__/data/audit-actor-dropdown-uses-rpc.test.ts`.
- 🔴 **`grade_audit_log` — the one exemption in the whole pass resting on inference rather than a count.** It carries **no index of any kind beyond its `id` primary key** (`001:167`), and `app/api/audit-log/route.ts` orders it `changed_at desc limit 500` with only optional narrowing by `sheet_id` / `entry_id`. It is exempt because it is bounded by grade-entry _edit_ volume and AY2026 holds 4,636 grade entries — but **its own row count was never probed**, and unlike every other table in the sweep it is append-only with no per-AY ceiling: a locked sheet reopened and re-edited appends rows without adding entries. ⚠ **Re-measure it first on the next sweep. Trigger: above ~50,000 rows, add `create index … on public.grade_audit_log (changed_at desc)` and `… (grading_sheet_id, changed_at desc)`.** Proposing them before somebody has counted would be guessing, and a false "missing index" finding is worse than none.
- **The declarations N+1 — dropped, with the number.** 0 pending declarations, so the loop cannot fire. Revisit only if that count grows.
- **`loadHubKpisUncached` counts `sections` it already fetched.** Real, but a _duplicate_-class finding whose fix would move `roundTrips`; left for a later pass rather than folded into a latency phase.
- **`attendance_daily` needs nothing.** Its two scanner hits are one statement, and `attendance_daily_compassionate_idx` (`015:204`) is a **partial** index whose predicate _is_ that query's `ex_reason` filter. It is the only genuinely large table in the app (102,510 rows) and migrations 014/015/048 already cover it.

§12's three "do not re-add" items (scoping the 13-tag invalidation, the grading page's render-time RPC, caching `requireCurrentAyCode()`) all still stand and were not revisited.

### The exempt lists — where they live

**Do not re-transcribe or re-litigate these here.** Each row carries its own reason and, where it is a size judgement, a production count.

- **86 unindexed-filter pairs / 484 call sites** — `docs/superpowers/specs/2026-08-29-phase-2-unindexed-filter-classification.md`. Re-run with `npx tsx scripts/audit/unindexed-filters.ts` (~40s, no DB credentials). Anything it surfaces that is not in that document is genuinely new; anything already in it is answered.
- **12 write-loop sites, the two the scanner cannot see, and the read-after-write sweep** — `docs/superpowers/specs/2026-08-29-phase-5-write-amplification-reconciliation.md`, with production scale from `scripts/probe-write-amplification-scale.ts`.
- **Cache-tag exemptions** — in `__tests__/cache/write-route-invalidation.test.ts`, not in prose, because that is the only form of the list that cannot rot: a stale exemption fails the build.
- **Pagination exemptions** — in `__tests__/data/no-unpaginated-high-volume-reads.test.ts`, same discipline. One entry was added and measured rather than inherited: `markbook/grading/[id]/page.tsx` reads one sheet by id, and across all 260 AY2026 sheets the worst holds 33 entries against Hard Rule #5's cap of 50.

⚠ **Findings that must survive go in `docs/superpowers/specs/`.** `.superpowers/` is gitignored (`.gitignore:46`) — no phase doc written there has ever been tracked.

### Corrections to this document

- 🔴 **§10's "one real, actionable gap" is ACTIONED by migration 132** (written 2026-08-29, **not yet applied**). Before: the three per-AY admissions tables carried no index beyond `constraint %I primary key (id)`. After: five non-unique btree indexes per AY, 15 rows after the backfill, and one `perform` line in the RPC so every future AY gets them for free. ⚠ **§10's own instructions would have caused a regression, and they are wrong in two places.** (1) It says the RPC's base lives in `012_ay_setup_helpers.sql` — **012 is the ORIGIN, not the current definition.** The function has been re-emitted eleven times (012, 025, 026, 033, 050, 067, 069, 075, 076, 087, 099) and **099 is current**; re-emitting from a stale body already caused a five-migration regression in this exact function, with 099's header recording the doc-revision trigger being _"silently dropped by migration 050's re-emit"_ and staying dropped through 067/069/075/076. 132 follows 098/099's idempotent `attach_*(slug)` pattern instead, and its function body was copied byte-for-byte (md5 verified) then edited in place, diffing to **one hunk, 8 additions, zero deletions, zero modifications**. (2) It says to mirror the `pg_tables` walk in `025` over `ay26`/`ay27` — **the slug has been four-digit (`ay{YYYY}`) since migration 026**, so 025's regex matches **zero** tables and reports success. 132 walks `academic_years` the way 098 does, which is correct by construction and cannot match an AY that does not exist.
- **§10's last bullet — "Not yet done: an audit of server-component pages/layouts for sequential await waterfalls" — is done** (phase 4), and the answer carries a warning: a static scanner cannot do that job alone, because this codebase's waterfalls run through named helpers. Coverage from `scripts/audit/sequential-awaits.ts`, truth from the counting harness.
- **§2's "hoist the `unstable_cache` wrapper to module scope" rule and its §9 checklist line were already rewritten** by phase 7 (`2b0dd62d`), and that amendment is recorded in this doc's header. Not repeated here. The short form: the code disagreed with the doc 110 to 8 and the code was right; the rule worth having is the inverse — **`keyParts` must name every variable the callback closes over**, or two callers collide and serve each other's answer.
- **§2 gained a bullet on `sis:${ayCode}`** (phase 7, `aecaeaa5`), with the re-measure command and the trap named: the 3.8s figure in it is a cold deploy and must not be read as the cost of invalidation.

### 🔴 Claims this pass killed — do not repeat them

Every one of these was written from reasoning rather than from reading, and several were the controller's own. They are kept because the failure mode will recur.

1. **"The evaluation audit-log dropdown silently truncates in production" — FALSE.** 29 rows against a 1,000 cap. The defect was latent, not live; severity-3 hygiene, not a wrong answer.
2. **The inverse was real, but far smaller than claimed.** Measured 2026-08-30: **exactly one page was wrong** — markbook, 306 rows / 9 actors, `.limit(200)` listing 8 — so **one actor was missing**, not "all four pages dropping most of theirs". ⚠ **And there were three dropdowns, not four**: seven audit-log pages exist, three build an Actor `<Select>` (markbook, attendance, evaluation) and four accept `?actor=` while building none. ⚠ **It is a filter** — a missing actor cannot be _selected_, but their rows still appear in the log. **Nothing was ever hidden from the record.** The trap is that two of the three were right, so the obvious "make them consistent" tidy-up — putting `.limit(200)` on evaluation — is the single change capable of breaking a correct page; there is a ⚠ comment at that site saying so.
3. **"Missing indexes are the single biggest win" — OVERSTATED.** The largest table in the sweep is 822 rows and a sequential scan of it is sub-millisecond. 132 is cheap future-proofing on the only tables in the app that grow without bound. Nothing on a screen gets faster today.
4. **"A second bug class: six school-wide loaders nothing can invalidate" — DOES NOT EXIST.** All six were correctly AY-code-tagged; the scanner simply cannot read `.map()` expressions, bare variables, module constants, or a `tags:` key positioned after `revalidate:`. **The sweep's value there was proving 115 of 118 sites clean, not finding more bugs.**
5. **"`ALLOWED_UNPRODUCED_BARE_TAGS` encodes a false premise" — WRONG, and it was the controller's own ⚠.** The whole check is gated on `hasAyColonTag`, so it only ever inspects tag arrays that already contain an AY-coded tag, and for those the allowlist's stated reason is true. The real defect was that **a lone bare tag with no sibling was never examined at all** — which is why a guard written for this bug class did not catch it. The fix was a new sibling test, not a shrunk allowlist.
6. **"Widening the pagination guard is why the audit-log defect was missed" — did not survive testing.** The guard **structurally cannot** catch that shape: its 900-char window over-reads from the actor query into the neighbouring log query's `.range(`. Widening it to all of `app` was worth doing anyway and found a live defect on the first pass (the grading list), but it is not the answer to this one.
7. **"The read-after-write category has one instance and is otherwise clean" — there were TWO, and the unnamed one runs on a schedule.** `syncOneStudent` inserted then re-read `students` by `student_number` for every fresh enrolment — the nightly auto-sync's unsynced queue, five students to a wave, paying the extra trip per newly-enrolled child. It was found by grepping the _shape_ across `app/`, `lib/` **and `scripts/`** (56 sites, each resolved to its table by the nearest preceding `.from(`) rather than opening the one site the brief named. **The search is the deliverable**, so a future pass can widen it instead of redoing it.
8. **"`lib/sync/students.ts` has three per-row write loops, with a chunked write 20 lines below" — wrong twice.** `syncOneStudent` is handed a **one-row roster**, so each loop runs ≤1 time (≤2 for the status-change loop after a mid-year transfer); and there is **no chunked write in that file at all** — the only chunk-shaped helper in it is `fetchAllPages`, a read.
9. **"~260 sheets and ~100 configs" in the AY2025 backfill — wrong in both directions.** Production holds **620 sheets and 18 configs**. The conclusion drawn from the wrong numbers happened to be right anyway.
10. **"Copy the sibling pages' `.limit(200)`" — no.** A row limit is not an actor limit; PostgREST has no `DISTINCT`, so every client-side alternative reads rows to derive a handful of names.
11. ⚠ **One tally in the working ledger does not match the findings doc, and the doc is right.** The ledger recorded phase 5 as "7 fixed, 1 retained-by-design, 8 exempt", which counts _commits_. The reconciliation doc counts _rows_: the scanner reports **twelve** sites → **1 fixed, 11 exempt**, plus four loops already fixed earlier in the phase (so gone from the enumeration), two sites in `scripts/` outside the scanner's walk, and two read-after-write instances. Use the doc.

### Still open

- **Applying 132 and 133.** Both written, neither run. 132's three acceptance queries are above. 🔴 **133 is a deploy BLOCKER and 132 is not** — three Actor dropdowns call its RPC and have no fallback, so shipping this branch before applying it empties all three filters. Ordering and the reasoning for having no fallback are in 133's paragraph above and in the migration's own header.
- **`grade_audit_log`'s row count.** The one number this pass never took, and the first thing the next sweep should measure.
- **`/attendance/sections` has no harness guard** — its fix is real, but its evidence is a scanner delta rather than a wave count.
- **The three write routes §12 names** (`grading-sheets/[id]/labels`, `sections/[id]/schedule`, `sis/admin/users/[id]`) were not part of this pass and remain as §12 left them.
- ⚠ **AY2026 still has 3 term rows, not 4.** Re-confirmed by two independent probes during this pass. Recorded, not fixed — the dates are the school's to supply.
