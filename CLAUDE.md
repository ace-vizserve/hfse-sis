# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, P-Files, Records, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

These two are `@`-imported so they're always in context. Do not edit without explicit user approval.

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

## Stable rules — on-demand (read with the Read tool when relevant)

Not `@`-imported. Each file carries YAML frontmatter (`description`, `load: on-demand`) explaining its trigger. Read before acting when any of the "Read when..." conditions apply.

| Rule | Read when... |
| --- | --- |
| `.claude/rules/tech-stack.md` | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha |
| `.claude/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives |
| `.claude/rules/env-vars.md` | Touching `.env.local`, Supabase/auth plumbing, or Resend emails |
| `.claude/rules/key-decisions.md` | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions |
| `.claude/rules/design-system.md` | Before any UI / frontend code; when choosing a shadcn primitive, token, color, or layout |
| `.claude/rules/workflow.md` | Finishing work — before reporting a task done, or at session wrap-up |

## Reference docs

| Doc | Read when... |
| --- | --- |
| `docs/sprints/development-plan.md` | Starting any task — status snapshot + current sprint |
| `docs/context/01-project-overview.md` | Onboarding |
| `docs/context/02-grading-system.md` | Grade computation / formula |
| `docs/context/03-workflow-and-roles.md` | Permissions, locking, workflow |
| `docs/context/04-database-schema.md` | DB tables / queries |
| `docs/context/05-report-card.md` | Report card UI / PDF |
| `docs/context/06-admissions-integration.md` | Admissions sync |
| `docs/context/07-api-routes.md` | API contracts |
| `docs/context/08-admission-dashboard.md` | Admissions analytics |
| `docs/context/09-design-system.md` | UI tokens, hard rules, page→component matrix, pre-delivery checklist |
| `docs/context/09a-design-patterns.md` | Craft standard, canonical patterns, semantic color discipline |
| `docs/context/10-parent-portal.md` | Parent identity, linkage, SSO handoff |
| `docs/context/10a-parent-portal-ddl.md` | Frozen admissions DDL (AY-prefixed tables) |
| `docs/context/11-performance-patterns.md` | Any new page — auth/cache/parallel/loading checklist |
| `docs/context/12-p-files-module.md` | P-Files module — document types, statuses, architecture |
| `docs/context/13-sis-module.md` | Records module |
| `docs/context/14-modules-overview.md` | Cross-module architecture, shared identity, navigation |
| `docs/context/15-markbook-module.md` | Markbook module scope |
| `docs/context/16-attendance-module.md` | Attendance module (Phase 1 + 1.1 shipped) |
| `docs/context/17-process-flow.md` | Cross-module lifecycle + soft gates |
| `docs/context/18-ay-setup.md` | Superadmin AY-rollover wizard |
| `docs/context/19-evaluation-module.md` | Student Evaluation module — FCA writeups + virtue theme (KD #49) |
| `docs/context/20-dashboards.md` | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts` |
| `docs/context/21-stp-application.md` | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` gating + STP-conditional doc slots (KD #61) |

## Session context

<!--
Scratch surface for session-learned context, temporary caveats, in-flight
investigations. Safe to edit; pruned periodically. Stable rules do NOT go
here — they live in `.claude/rules/*.md`.
-->

- **Sprint 27 — Lifecycle observability v1 + levels word-form migration + seeder realism — shipped 2026-04-27 on `feat/swap-sonner-sileo`** (multi-piece session). 4 new KDs (#59-#62). **Lifecycle observability**: per-student composite `<StudentLifecycleTimeline>` dual-mounted on `/admissions/applications/[enroleeNumber]?tab=lifecycle` + `/records/students/[studentNumber]`; aggregate per-blocker dashboard widget on `/sis` (`<LifecycleAggregateCard>` with 8 buckets including the new "Awaiting document validation" for `Uploaded` slots); soft-gate visual checklist on `EditStageDialog` (advisory; surfaces blockers before submit; server gate unchanged); per-stage sequential prerequisite chain (`PREREQ_STAGE_PREDECESSOR`) removed (was fake-serializing parallel admissions work); outer `ENROLLED_PREREQ_STAGES` flip gate preserved; new `<NewApplicationsPriority>` PriorityPanel on `/admissions` (operational top-of-fold per KD #57). New `lib/sis/process.ts` data layer (60s `unstable_cache`, tag `sis:${ayCode}`) + `lib/admissions/priority.ts`. Auto-completion deferred to v2 pending audit-log evidence per the binding "simplify, don't change business flow" principle. **Levels migration**: storage flipped from digit form (`"Primary 1"`) to word form (`"Primary One"`); 5 new levels added (3 Youngstarters preschool tiers `YS-L`/`YS-J`/`YS-S`; 2 Cambridge Secondary tracks `CS1`/`CS2`). New `lib/sis/levels.ts` constants module (LEVEL_LABELS / LEVEL_CODES / LEVEL_TYPE_BY_CODE / `compareLevelLabels` / `canonicalizeLevelLabel`). Migration `029_levels_to_words.sql` widens `level_type` CHECK to allow `'preschool'`, idempotently rewrites `levels.label` (10 rows) + iterates every `ay{YYYY}_*` admissions table to remap `classLevel` + `levelApplied`. `lib/sync/level-normalizer.ts` repurposed from word→digit converter to digit→word defensive identity. Code sweep collapsed bespoke comparators in `lib/sis/dashboard.ts` + `lib/p-files/dashboard.ts` to `compareLevelLabels`. **Seeder realism corrections** driven by real-DB sample rows (KD #59-#62 surfaced here): funnel uses canonical SIS pipeline statuses (was stale `Inquiry/Applied/Interviewed/Offered/Accepted`); apps row writes `applicationStatus: 'Registered'` (parent-portal-side) + `category` + `classType` + `paymentOption` + `contractSignatory` + `pass` + Yes/No string `avail*` flags; status row mirrors `enroleeType` to `category`; per-stage progression realism so the lifecycle aggregate widget shows non-zero buckets. New `seedAdmissionsDocuments` populates 233 docs rows with status mix per applicationStatus profile; STP-conditional slots only populated for `stpApplicationType`-set rows; non-expiring slots use `'Uploaded'` initial state, expiring slots go `null → 'Valid' → 'Expired'`. `DOCUMENT_SLOTS` expanded 12→16 (added `form12`, `icaPhoto`, `financialSupportDocs`, `vaccinationInformation`); new `STP_CONDITIONAL_SLOT_KEYS` export. `seedDiscountCodes` rewritten to real schema (`discountCode`/`details`/`enroleeType`; was `code`/`description`/`percentageDiscount`). New context doc `docs/context/21-stp-application.md` covers the Singapore ICA Student Pass workflow (HFSE Edutrust Certified). New `ENROLEE_CATEGORIES` enum + narrowed `ProfileUpdateSchema.category`. Migration `029_levels_to_words.sql` written but not yet applied to production. Build clean.
- **Sidebar consolidation + sonner→sileo shim + test-banner refresh — shipped 2026-04-26 on `feat/swap-sonner-sileo`** (33rd pass; PR not yet open against `main`). New KD #58. **Sidebar**: 8 per-module `<module>-sidebar.tsx` files + the topbar `<ModuleSwitcher>` are deleted; one `components/module-sidebar.tsx` (+ `module-sidebar/{sidebar-header,sidebar-nav-item,sidebar-profile,sidebar-quick-action}.tsx` parts) replaces them. Module identity moved into the sidebar header as a `Popover` switcher; active item adopts §9.3 non-flat (gradient wash + ring-inset); footer collapsed to a single profile pill with a popover for Account + Sign out; per-role quick action declared in `lib/sidebar/registry.ts::SIDEBAR_REGISTRY[module].quickActionByRole`. New `components/topbar-module-switcher.tsx` survives only on the neutral `/` + `/account` shells (no sidebar to host the switcher there). Realtime badges generalized via `lib/sidebar/use-realtime-badges.ts` — markbook-only `hooks/use-realtime-badge-count.ts` removed. One in-flight bug caught + fixed before merge: `useRealtimeBadges(badges ?? {})` was creating a fresh object every render → infinite recount loop on modules without badges. Stable `EMPTY_BADGES = {}` const added in `module-sidebar.tsx`. **Toast shim**: `sonner` dropped from `package.json`, `sileo ^0.1.5` added; `tsconfig.json::compilerOptions.paths` rewires `'sonner'` → `components/ui/sonner.tsx`, which is a sonner-shaped facade over `sileo`. Zero call-site touches across ~154 `toast.X(...)` invocations in 55 files. **Test-mode banner**: thin amber strip redesigned — gradient `AlertTriangle` tile + mono `Test environment · AYxxxx · Disposable data` + `Button variant="warning"` to `/sis/admin/settings`. Specs: `docs/superpowers/specs/2026-04-26-sidebar-redesign-design.md` + `2026-04-26-sonner-to-sileo-shim-design.md`. Build clean.
- **27th-pass dashboard + admin surface polish (2026-04-25)** — follow-ups after the school calendar landed. **`InsightsPanel` redesigned** (`components/dashboard/insights-panel.tsx`): editorial rows with size-10 §7.4 crafted gradient tiles per severity (good=mint→sky with `shadow-brand-tile-mint`, warn=amber, bad=destructive, info=indigo→navy), `ChartLegendChip` severity badges replace flat wash pills, new severity-breakdown strip under the header ("2 alerts · 1 watch · 3 info"), serif 15px titles, mono-uppercase CTA links. Auto-applies across all 7 module dashboards. **Subject weights matrix + dialog redesigned** (`components/sis/subject-config-matrix.tsx` + `subject-config-edit-dialog.tsx`): cells now color-coded by canonical profile (Primary = mint `40·40·20`, Secondary = indigo `30·50·20`, Custom = amber; invalid sum = destructive), alternate row striping, 2px brand-indigo left accent on subject column, `ChartLegendChip` legend strip under the table. Dialog got flat form hierarchy (dropped 3 nested cards), new `RatioBar` visual — thin 8px 3-segment rounded-full progress bar with tiny color swatches + live sum validator inline, §7.4 gradient icon tile in header, Save button uses `variant="destructive"` (per "are-you-sure" rule — changes apply across every grading sheet for the AY). Fixed Hard Rule #5 + #7 violations (`emerald-700 dark:emerald-200`) in the sum validator. **Staff accounts badges** (`components/sis/users-admin-client.tsx`) — Active/Disabled badges use Badge `default` variant (mono uppercase white text from the Badge base-class refresh). **`Badge` primitive** gained new `variant="muted"` and font-mono/uppercase/tracking-[0.14em] base typography + `text-white` on `variant="success"` (earlier `text-ink` was too subtle). **`/sis/ay-setup` status badges** migrated to new variants; Delete button is now `variant="destructive"`.
- **Design-system "non-flat" primitive refresh — fully shipped 2026-04-25 on `feat/dashboard-drilldowns`** (see 26th-pass dev-plan row). Three-tier primitive system codified: **T1 chip/CTA** (Button default+destructive, Badge success+blocked, Tabs default active chip) uses brand gradient + `shadow-button`/`shadow-brand-tile`; **T1 content surface** (Alert w/ 4 variants + AlertIcon slot, DropdownMenu, Popover, Tooltip, Dialog/Sheet/AlertDialog, Sonner toast) uses solid tint + `ring-1 ring-inset` + `shadow-md`/`shadow-lg` (never gradient backgrounds); **T2 fillable** (Input, Textarea, Select trigger, Checkbox) uses hairline + inset `shadow-input` + `ring-2 ring-brand-indigo/20` focus bloom + brand-gradient checked state. New tokens: `shadow-input` flipped from 4% drop → 6% inset; `shadow-brand-tile-mint`/`-destructive`/`-amber` added. New shared primitive `components/dashboard/chart-legend-chip.tsx` (ChartLegendChip + chartLegendContent factory) powers the legend sweep — 8 recharts `<Legend>` migrations (admissions/dashboard/markbook/p-files/sis charts) + 3 hand-rolled legend migrations (wide-grid day-type dots, calendar-admin legend strip, outdated-applications staleness tiers). `StatusLegendItem` in wide-grid intentionally untouched (true grid-cell visual key, not a flat chip). `Select` → full crafted treatment this pass (skipped 25th pass). Alert absorbs §9.4 status-panel pattern; existing hand-rolled panels migrate on touch. §9.3 wash recipes intentionally preserved alongside gradient variants (dual-tier: gradient for pill chips, wash for in-table state-as-metadata). `RadioGroup`/`Switch` NOT added (YAGNI). Spec: `docs/superpowers/specs/2026-04-25-non-flat-primitive-refresh-design.md`; plan: `docs/superpowers/plans/2026-04-25-non-flat-primitive-refresh.md`.
- **Drill-down sprint — fully shipped 2026-04-25 on `feat/dashboard-drilldowns`** (28th pass). New KD #56. Every aggregating dashboard surface across Admissions / Markbook / Attendance / Evaluation is now click-to-drill into underlying rows with a uniform toolkit (search · range scope `This range`/`Current AY`/`All time` · status multi-select · level multi-select · group-by `None`/`Level`/`Status`/`Stage` · density · column visibility · CSV with UTF-8 BOM · sortable headers). 38 drill targets total: Admissions 12 (4 MetricCards + 6 chart cards + 2 NEW cards `applications-by-level` + `doc-completion` closing deferred backlog + outdated CSV) · Markbook 9 (4 MetricCards + 3 chart-card drills; targets `sheet-readiness-section` + `teacher-entry-velocity` API-only, no surface card yet) · Attendance 10 (4 MetricCards + daily-trend day-picker + 2 donut drills + top-absent CSV; targets `attendance-by-section` + `compassionate-quota` API-only) · Evaluation 7 (4 MetricCards + submission velocity + 2 NEW cards `writeups-by-section` + `time-to-submit-bucket` closing deferred backlog). Critical Next 16 lesson encoded in KD #56: chart drill wrappers MUST be `'use client'` per-target named components — Server Components cannot pass render-prop functions as children of Client Components. KD #24 preserved (raw `fetch` + sonner; no React Query). Specs: `docs/superpowers/specs/2026-04-25-admissions-drill-downs-design.md` + `2026-04-25-drill-down-replication-design.md`.
- **Dashboard perf audit + Option B fixes — shipped 2026-04-26 on `feat/dashboard-drilldowns`** (29th pass). Drops the universal-row-set pre-fetch for the two high-volume modules. Per-module contract is now "rolled-up shapes server-fetched, raw rows lazy-fetched on drill open with `DrillSheetSkeleton` placeholder" — KD #56 updated. Attendance + Markbook `buildAllRowSets()` no longer ship `entries` through the RSC payload (was ~30-50 MB / ~10 MB respectively at 1000 students × 180 school days / × 10 subjects × 4 terms). Entry-kind drills lazy-fetch on mount via the existing API route + `useEffect` (no new code path; the `seedRows.length === 0` case naturally falls through to fetch). New `lib/auth/teacher-emails.ts::getTeacherEmailMap` (5-min cached) replaces 2 unbatched `auth.admin.listUsers({perPage:1000})` calls in Markbook + Evaluation drill loaders — returns `Array<[id,email]>` rather than Map because Next 16 `unstable_cache` JSON-serializes return values (Map round-trips as `{}`). Tactical wins: `bucketByDay` Map lookup (drops 90k Array.indexOf comparisons → 1k Map gets), single-pass status+level filter combos in Admissions + Markbook drill sheets, term-id filter pushdown in `loadSheetRowsUncached` + sheet-id filter on grade_entries (was unbounded scan), `rollupCompassionate` accepts entries parameter to skip redundant `loadEntryRows`, Admissions doc-fetch split (`enrichWithDocs` only called for the 5 of 12 targets that surface doc fields; `buildDrillRows({withDocs})` opt-in). Skipped: DB index audit (needs Supabase CLI access I don't have from here — flag for next session); `loadDailyRowsUncached` term filter (function is AY-scoped by contract); audit's "Attendance loadTopAbsent dedupe" (turned out to be 2 distinct types `TopAbsentRow` vs `TopAbsentDrillRow` with different consumers, no real drift). React Query discussed + rejected (KD #24 preserved). Spec: `docs/superpowers/specs/2026-04-25-dashboard-perf-audit-design.md`; plan: `docs/superpowers/plans/2026-04-25-dashboard-perf-audit.md`. 8 commits. Build clean.
- **Records + P-Files + SIS Admin drill-down sprint — shipped 2026-04-26 on `feat/dashboard-drilldowns`** (30th pass). Closes the drill-down framework rollout to all 7 module dashboards. New `lib/sis/drill.ts` (Records + SIS Admin targets in one module) + `lib/p-files/drill.ts` + 3 new API routes (`/api/{records,p-files,sis-admin}/drill/[target]`). Records pipeline-stage rebuilt as recharts Sankey (no new dep — Sankey ships in core; survey rated horizontal bar WEAK for 10-stage progression). 3 new cards (one per module): class-assignment readiness (Records — students enrolled but no section_id), revisions activity heatmap (P-Files — 12-week × 7-day calendar with intensity-scaled brand-indigo→navy gradient cells), activity by actor (SIS Admin — top 12 users by audit-event count). System-health strip on `/sis` becomes click-to-drill: AY indicator → academic-years; Approver coverage → approver-coverage drill; quick-link panel stays a Link. SIS Admin drill sheet handles 4 row shapes (audit / approver / AY / actor) via discriminated render. Audit-by-module bar chart now drills (range-aware via from/to threading). Activity-by-actor segment-click pivots to audit-events filtered by that actor. 23 drill targets across the 3 modules. KD #56 updated with full module list. Build clean. Spec: `docs/superpowers/specs/2026-04-26-records-pfiles-sis-drill-downs-design.md`; plan: `docs/superpowers/plans/2026-04-26-records-pfiles-sis-drill-downs.md`.
- **Task-oriented dashboard layouts — fully shipped 2026-04-26 on `feat/dashboard-drilldowns`** (31st pass). New KD #57. Adopted Stephen Few's **operational / analytical / hub** archetype taxonomy as the dashboard layout vocabulary; codified in `docs/context/20-dashboards.md` § Layout archetypes. New shared primitive `components/dashboard/priority-panel.tsx` + `lib/dashboard/priority.ts` (`PriorityPayload` type) — top-of-fold "what to act on right now?" banner reusing §7.4 craft (gradient icon tile + serif headline + ChartLegendChip severity tag + flex-wrap action chips + footer CTA). 5 modules reshaped: **P-Files** (operational — PriorityPanel of expiring docs + top 4 student chips, ActionList block removed); **Attendance** (operational — "X sections still need to mark today" + chips per unmarked section + compassionate over-quota chips; resolved via `terms.academic_year_id` join since `school_calendar` has no direct `ay_code`); **Markbook** (operational, **role-split** — teacher sees open subject sheets across assigned sections, registrar sees pending change requests as urgent headline + per-term unlocked-sheet chips); **Evaluation** (role-split — teacher PriorityPanel of writeups due in current term, registrar sees school-wide PriorityPanel + retains existing analytical layout, teacher view gates out school-wide KPIs via `canToggle &&`); **SIS Admin** (hub — AdminCards promoted to top-of-fold, audit-activity content moves behind `?view=audit` segmented Tabs, audit DB fetches gated on view). Records + Admissions confirmed analytical (no top-of-fold change). All 7 dashboards now classified. Per-module priority loaders live next to existing `dashboard.ts` (e.g. `getPFilesPriority`, `getMarkbookTeacherPriority` / `getMarkbookRegistrarPriority`); 60s `unstable_cache` + module tag (KD #54). Subagent caught + fixed `evaluation_writeups.submitted boolean` (no `status` column). KD #24 + #56 preserved. No new deps.
- **Polish pass + non-flat status-pill rollout — shipped 2026-04-26 on `feat/dashboard-drilldowns`** (32nd pass). Series of follow-up commits after Sprint 25 closed. **PriorityPanel hotfix**: `PriorityPayload.icon: LucideIcon` round-tripped as `{}` through `unstable_cache` (same JSON-serialization gotcha as the Sprint 23 Map → `{}` issue). Fixed by replacing with `iconKey: PriorityIconKey` string discriminator + an internal `ICON_BY_KEY` registry inside PriorityPanel. **2 new primitive variants**: `Badge variant="warning"` (amber gradient, white text, `shadow-sm`) + `Button variant="warning"` (amber gradient, `shadow-md`, hover-lighten + active-press — same craft as `destructive` in the warning palette). Completes the §9.3 healthy/warning/blocked trio across both primitives. **New design rule §10 in `09a-design-patterns.md`** — "Legends" formalises the gradient-pill-vs-tinted-swatch decision (was de facto across sweeps, now binding). Wide-grid attendance cells + headers + legend all upgraded to gradient pills using a shared `STATUS_CHIP_COLOR` + `DAY_TYPE_CHIP_COLOR` map; legend now shows all 7 dropdown letters (P/L/EM/EC/ES/A/NC), not the collapsed 5-letter status family. **Root `/` route rewrite**: tile-style module picker with all 7 modules in lifecycle order, role-gated via `isRouteAllowed()` (single source of truth with the top-bar ModuleSwitcher); disabled tiles dropped entirely (not dimmed); centered horizontally + vertically. Teacher + superadmin no longer auto-redirected — KD #43 updated inline. Mental model: "0 or 1 module accesses → forced redirect, ≥2 → picker." **Non-flat status-pill sweep**: change-requests (admin queue + teacher view) status column extracted to shared `lib/markbook/change-request-status.ts` with all 5 lifecycle states mapped to non-flat Badge variants (warning/default/success/blocked/muted — no className overrides). EnvironmentCard, term-dates-editor, AY-setup row actions, sis/approvers icon tile, attendance audit log all migrated to primitive variants — zero inline color recipes left in those surfaces. **§7.4 craft fixes** on sis/approvers icon tile (rounded-md→rounded-xl + shadow-brand-tile), Reset Test environment tile (gradient destructive + shadow-brand-tile-destructive), term-dates dialog header tile. New shared module `lib/markbook/change-request-status.ts` (CHANGE_REQUEST_STATUS_CONFIG + ChangeRequestStatus type). 16 commits (`ca98807` through `afad02d`). Build clean.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved; only the host file moved.
