# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, Evaluation, P-Files, Records, Admissions, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

**Everything inside `.claude/rules/` is read into context automatically, every session.** That is a Claude Code behaviour, not something a file can opt out of — a `load: on-demand` line in a file's frontmatter is descriptive only and does not stop it loading. So only four files live there, deliberately:

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

- `.claude/rules/design-system.md` — binding design pointer; too important to risk missing (it is small, and only points at the full docs).
- `.claude/rules/key-decisions.md` — the KD index, so a "KD #N" cite resolves with no extra read.

Do not edit any of the four without explicit user approval, and **do not add new files to `.claude/rules/`** — anything put there is paid for in every session. On-demand material goes in `docs/` (below).

## Stable rules — on-demand (read with the Read tool when relevant)

These live **outside** `.claude/rules/`, so they cost nothing until read. Read before acting when any of the "Read when..." conditions apply.

| Rule                           | Read when...                                                                                                                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/rules/tech-stack.md`     | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha                                                                                                                                                      |
| `docs/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives                                                                                                                                                            |
| `docs/rules/env-vars.md`       | Touching `.env.local`, Supabase/auth plumbing, or Resend emails                                                                                                                                                                                        |
| `docs/rules/workflow.md`       | Finishing work — before reporting a task done, or at session wrap-up                                                                                                                                                                                   |
| `docs/key-decisions/*.md`      | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions. Find the topic file via the always-loaded index at `.claude/rules/key-decisions.md`, then Read only that file. |

## Reference docs

| Doc                                         | Read when...                                                                                                                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/sprints/development-plan.md`          | Starting any task — status snapshot + current sprint                                                                                                                                                    |
| `docs/context/01-project-overview.md`       | Onboarding                                                                                                                                                                                              |
| `docs/context/02-grading-system.md`         | Grade computation / formula                                                                                                                                                                             |
| `docs/context/03-workflow-and-roles.md`     | Permissions, locking, workflow                                                                                                                                                                          |
| `docs/context/04-database-schema.md`        | DB tables / queries                                                                                                                                                                                     |
| `docs/context/05-report-card.md`            | Report card UI / PDF                                                                                                                                                                                    |
| `docs/context/06-admissions-integration.md` | Admissions sync                                                                                                                                                                                         |
| `docs/context/admission-process.md`         | HFSE's 13-step intake process (Open House → Orientation); maps ~1:1 to the 9-stage SIS pipeline (`STAGE_KEYS`, KD #51/#59)                                                                              |
| `docs/context/07-api-routes.md`             | API contracts                                                                                                                                                                                           |
| `docs/context/08-admission-dashboard.md`    | Admissions analytics                                                                                                                                                                                    |
| `docs/context/09-design-system.md`          | UI tokens, hard rules, page→component matrix, pre-delivery checklist                                                                                                                                    |
| `docs/context/09a-design-patterns.md`       | Craft standard, canonical patterns, semantic color discipline                                                                                                                                           |
| `docs/context/10-parent-portal.md`          | Parent identity, linkage, SSO handoff                                                                                                                                                                   |
| `docs/context/10a-parent-portal-ddl.md`     | Frozen admissions DDL (AY-prefixed tables)                                                                                                                                                              |
| `docs/context/11-performance-patterns.md`   | Any new page — auth/cache/parallel/loading checklist                                                                                                                                                    |
| `docs/context/12-p-files-module.md`         | P-Files module — document types, statuses, architecture                                                                                                                                                 |
| `docs/context/13-sis-module.md`             | Records module                                                                                                                                                                                          |
| `docs/context/14-modules-overview.md`       | Cross-module architecture, shared identity, navigation                                                                                                                                                  |
| `docs/context/15-markbook-module.md`        | Markbook module scope                                                                                                                                                                                   |
| `docs/context/16-attendance-module.md`      | Attendance module (Phase 1 + 1.1 shipped)                                                                                                                                                               |
| `docs/context/17-process-flow.md`           | Cross-module lifecycle + soft gates                                                                                                                                                                     |
| `docs/context/18-ay-setup.md`               | Superadmin AY-rollover wizard                                                                                                                                                                           |
| `docs/context/19-evaluation-module.md`      | Student Evaluation module — FCA writeups + virtue theme (KD #49)                                                                                                                                        |
| `docs/context/20-dashboards.md`             | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts`                                                                                                             |
| `docs/context/21-stp-application.md`        | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` + `stpApplicationStatus` phases (KD #61). ⚠ Its 3 STP document slots were removed by KD #96 — that half is history |
| `docs/context/23-session-log.md`            | Session history before the current sprint — shipped work, closed decisions, superseded claims, and the gotchas they left behind                                                                         |

## Session context

✅ **THE AY2026 SIS IS ALIGNED TO THE SCHOOL'S OWN CLASS LIST FOR P1–S4 — APPLIED 2026-09-17.** Mr Ace: _"the plan is to align our SIS to the master list."_ 21 sections matched; 3 form class advisers corrected (**P1 Patience had none recorded**); **18 students renumbered** across 7 sections; `sections.class_type` populated (7 Global, 2 Standard — the column had been NULL on all 21 since the year was created, and the school records the stream only in the section TITLE); "Respect" renamed **"Test Section"** so the one section that is not real stops reading as a discrepancy; and two children stored back-to-front fixed (`Sayeda, Toki` → `Toki, Sayeda`, `Taseen, Tayeb` → `Tayeb, Taseen` — the school writes surname first). Re-runnable: `scripts/audit-roster-vs-masterlist.ts` (read-only) and `scripts/backfill/apply-masterlist-alignment.ts` (dry-run default). ✅ **A TEACHER CONFIRMED THE RESULT INDEPENDENTLY, MID-WORK** — Ms. Sharon Menezes reported Fabre should be #14 and Toki #25; the list said exactly that, the SIS had them at #32/#33, and the SIS had holes at #14 and #25. 🔴 **Her section was being SILENTLY SKIPPED** because the list calls it "Sec 1 Discipline- Global" where the SIS says "Discipline 1" — **a section that fails to match reports no discrepancies, which is indistinguishable from being correct.** One alias fixed it and a third wrong number came with it. ⚠ **THE MASTERLIST IS NOT ALWAYS RIGHT**: P2 Honesty lists Felix at #11 where Yao already sits (he is row 4, and #4 is the only number their list is missing) — the script REFUSES a target held by a child who is not moving rather than colliding. 🔴 **STUDENT NUMBERS ARE DELIBERATELY UNTOUCHED — see [[project_student_number_rules_and_school_list]]: 200 differ, and adopting theirs would FUSE PAIRS OF CHILDREN.**

✅ **HOUSE COLOURS IMPORTED — 373 students, 2026-09-17.** The four houses have existed since 2026-08-03 with tokens, titles and core values, and **exactly one row carried a `house_id`: the `Test, Testing Two` record** — so the House column was an em-dash for every real child. After: Green 96, Blue 95, Orange 93, Yellow 90, 41 without (withdrawn + late arrivals the allocation predates). `scripts/backfill/apply-house-assignments.ts`. ⚠ **`XLSX.readFile` DECODES A .csv AS LATIN-1** — "CASIÑO" arrived as "CASIÃO" and five children matched nobody; the file was correct UTF-8 all along (0xC3 0x91 at the byte level). Read with `readFileSync(path,'utf8')` and hand XLSX a string. ⚠ **REPORT THE MIRROR SIDE OR THE AUDIT LIES**: "SIS children with no house" cannot distinguish "not on the list" from "name did not match" — reporting the CSV rows that matched NOBODY is what exposed the mojibake, and then turned up the withdrawn students' sections. ⚠ `/*.csv` added to `.gitignore`; the allocation sat unignored at the repo root with 389 children's names (the old rule was xlsx-only).

🟡 **WITHDRAWN AY2026 STUDENTS WITH NO CLASS ROW: 21 → 13 (2026-09-17). Plan, with the remaining 13 split by what each one needs: `docs/superpowers/plans/2026-09-16-withdrawn-students-without-class.md`.** ✅ **What unblocked it was the HOUSE-COLOUR CSV, not the masterlist** — it lists these children UNDER THEIR SECTION, which admissions never did (it records LEVEL only), and it independently reproduces the two placements Mr Ace had already approved by hand (Cama, Jannat Ajmal → S3 Consistency). Index numbers came from the alphabet: each section had exactly one hole the surname fits, the Cama precedent's own reasoning, re-checked against the live roster before writing (`scripts/backfill/apply-withdrawn-placements-from-house-list.ts`). Muhammad Ibrahim Ajmal is placed. ⚠ **The audit still says 15 — two are PHANTOMS.** It matches on the AY2026 `studentNumber`, and Santos Kairo Alonzo holds three: `H240037` (real, from AY2025 P3 Courageous), `H233489` (minted fresh on his AY2026 row) and `V260494` (a VizSchool mis-application — Mr Ace: _"maybe the parent mis appleid to vizschool and should be regular that simple"_). He is placed under `H240037`; the check looks for `H233489`. **The audit inherited the same wrong assumption that caused the bug** — that a Current student's AY2026 record carries their real number. Left loud deliberately; the fix belongs on the admissions row. 🔴 **The 13 need TWO ASKS of the school: 5 index numbers (section known, holes are in the non-alphabetical tail) and 6 children's class AND number (not on the house list at all).** The last 2 are Youngstarters, parked. ⚠ 7 of the 13 have no `students` row.

_Original entry:_ 🔴 **21 OF 23 WITHDRAWN AY2026 STUDENTS HAVE NO CLASS ROW AT ALL — found 2026-09-16, BLOCKED on Miss Jo's masterlist.** Mr Ace: _"you cant be an enrolled student and be withdrawn with no class assignment."_ Hard Rule #6 keeps a withdrawn student on the roster with their retired index number; these children vanished from theirs. Three were found ONE AT A TIME — Jannat Ajmal (15 Sep), then Ashley Rae Cama and Muhammad Ibrahim (16 Sep) — each looking like a one-off, until reading the withdrawal remarks turned up the rest in a single pass (`scripts/audit-withdrawn-without-class.ts`, read-only, re-runnable). ⚠ **10 of the 21 have no `students` row AT ALL**, so the SIS does not know those children exist in any module. ⚠ **`E260303` and `E260494` are the SAME child** (Santos, Kairo Alonzo, Primary Four) under two enrolee numbers — the duplicate-application signature again. ⚠ **21 missing against 27 index holes: the counts DIFFER**, so the holes are only partly these students — do not assume one explains the other. **Waiting on the masterlist because admissions records a child's LEVEL, not their class or index number, and a hole proves somebody is missing without saying who.** ✅ Cama is placed (S3 Consistency #5, withdrawn, last day 2026-04-26) and is the approved precedent to copy: `scripts/backfill/apply-cama-s3-consistency-placement.ts`.

✅ **A STUDENT IS ONLY ASKED FOR THE DOCUMENTS THEIR CATEGORY NEEDS — committed 2026-09-16 (`9d0d7440`), NO MIGRATION, NOT PUSHED, NOT BROWSER-VERIFIED. Full account: KD #219.** The school keeps two lists and the app knew neither, so every child was shown all of it. A **New** student gets the five extra school forms (Last School Recommendation, Assessment Result and Interview, New Student Checksheet, P-Files Checklist, Pre-Counselling Acknowledgement); a **Current** one does not. Verified on all three years: New sees 5 of 5, Current 0 of 5. ⚠ **Two of the school's rules needed no code and none was written** — "latest copy if the previous is expired" is what the expiry date already does (**246 of 501 AY2026 students hold an expired pass or passport today**), and "if there's a new medical condition to declare" has nothing behind it in the database, so Medical Exam is untouched and no staff toggle was invented. 🔴 **THE GATE IS DELIBERATELY NOT APPLIED TO THE FAMILY DOCUMENTS.** The Current list names what the office RE-COLLECTS, not what the file should hold: a returning student's row already carries them (**367 of 381 an ID picture, 379 a birth certificate**), so gating them would have marked real uploaded documents "not applicable" and hidden them from the cabinet. Same reason **a gated slot that already holds a file still shows**. 🔴 **READ THE CATEGORY VIA `resolveCategory`, WHICH TAKES `category` FIRST AND `enroleeType` SECOND — the other order is wrong and was shipped that way for an hour.** `enroleeType` is the tidier record, but **the Records profile sheet writes `category` and NOTHING writes `enroleeType`**, so a mis-tagged child could not have been corrected from anywhere in the product. The two disagree on **241 of 828 AY2025 rows, 4 of 495 AY2026, 3 of 290 AY2027**, and `category` is blank for 232 AY2025 students — hence the fallback. Where they disagree the rule decides; Mr Ace: _"i mean just follow the rules"_. 🔴 **THREE GATES WERE DEAD AND NOBODY COULD SEE IT.** The **Late Enrolment Form had never been visible to anyone** since it shipped 2026-08-31 — no loader ever supplied the late-enrollee fact, and "cannot tell" hides — while **21 AY2026 children are marked `late_enrollee`**; the drill sheet's Conditional Enrolment gate was declared, commented and never filled in; and the dashboard donut applied no gate at all, so this change would have overstated its "Missing" slice by **~1,900 documents** against the table beside it. ⚠ **`.maybeSingle()` IS WRONG FOR A ROSTER ROW** — a transfer (KD #67) leaves a withdrawn row with the same `enrolee_number`, and **`E260532` has two AY2026 rows**; two rows come back as PGRST116 in `error` with `null` in `data`, which reads as "not late". ⚠ **Form 12 moved to School Forms**: the parent portal stopped offering it (AY2025 **515**, AY2026 and AY2027 **zero** — Mr Ace: _"yes form 12 is not being collected on the parent portal thats correct"_), so as a family document it gave ~1,300 students a permanent "Remind parent" row nobody could clear. ⚠ **Gate columns are hand-listed in six loaders** — a condition keyed on a column missing from one reads blank and hides the slot on that surface only; that is what both dead gates were. Probes: `scripts/audit-document-needs-by-category.ts`, `scripts/verify-category-document-gate.ts`.

✅ **A WITHDRAWAL NOW RECORDS THE TWO DATES THE SCHOOL KEEPS — migration 163 APPLIED 2026-09-16 and verified (both columns live, 0 approved dates, 11 withdrawal dates).** Mr Ace: _"we should capture this in enrolled student being withdrawn too in SIS … we are not capturing this details."_ 🔴 **The SIS captured neither and invented a third date** — the withdraw path stamped `withdrawal_date = sgToday()`, so the stored value meant "the day a registrar opened the screen". Cama's real dates are 26 April (last day) and 11 May (approved), two weeks apart; that line would have matched neither, and the Withdrawals count, the permanent record and every attendance question read it as fact about a child. `withdrawal_date` now means **LAST DAY OF ATTENDANCE**, entered by the registrar and required on the boundary; `withdrawal_approved_date` is new. ⚠ **No CHECK that approved >= last day** — approval after departure is ordinary. ⚠ **The 11 pre-163 dates are admin-action dates and were NOT backfilled**; nothing says which were clicked on the day the child left. 🔴 **THE OFFICE'S REAL RECORDS LIVE IN THE REMARKS** — 477 of 495 AY2026 rows carry one, and Cama's last day existed only as prose in `applicationRemarks`. `scripts/audit-admissions-remarks.ts` surveys all of them; reviewed with Mr Ace and deliberately **not** acted on beyond the withdrawal dates. 🔴 **READ `ay{YYYY}_enrolment_status` FOR A STAGE, NEVER `_enrolment_applications`** — the latter still says "Registered" for students the office has withdrawn, and that cost two confidently wrong answers.

✅ **A TERM CAN SAY IT HAS NO EXAM — migration 159 APPLIED 2026-09-15, code NOT yet deployed. Full account: KD #218.** Miss Joann asked whether she has to untick something per term: S3 Filipino has an exam in some terms and not others, Global Perspectives had none in Term 3, _"those subjects have PT scores only, and the PT score effectively is the exam"_. 🔴 **The flag she was pointed at is the wrong one.** `subjects.is_examinable` means numeric-vs-LETTER grading (migration 049), has no AY dimension, and unticking it would have made Filipino letter-graded in every year at once. Mr Ace: _"our definition of examinable is wrong … its the grading sheet that needs it instead of the subject."_ 🔴 **And it was a live defect:** `(qa_ps ?? 0) * qa_weight` gives a missing component ZERO rather than handing its weight back, so a no-exam term graded Filipino out of 80 — full marks scored **87**, and Global Perspectives on performance tasks alone scored **72**. ⚠ **`lib/compute/quarterly.ts` is NOT modified and must not be** — with `qa_weight` at 0 the formula was already right, it was being fed the wrong weights. `grading_sheets` now carries its own weights (nullable = inherit), set for a whole term on **Subject setup** or for one class in **Edit totals & slots** (the per-section case Mr Ace asked for: _"currently its per level and there might be cases where its per section"_). ⚠ **Redistribution is integer percentage points with the tie broken deliberately** — the first version divided fractions and `0.3/0.8` is `0.37499999…`, so a real 37.5 tie was decided by representation error; Filipino lands on **37/63**. ✅ **Measured first and the live damage was 2 students, AY2025 clean across 348 sheets** (`scripts/audit-missing-component-weight.ts`) — **no `subject_configs` row anywhere has ever carried a zero weight**, so this is a **pre-import fix, not a cleanup**, and a prerequisite for the T3 grading import, which would otherwise overwrite the year's weights a third time. ⚠ **Slots were already per-term and needed nothing** (`ww_totals`/`pt_totals`/`qa_total` since migration 001 — Math already holds nine distinct T1 exam totals); only the weights were stuck at year level. 🔴 **DEPLOY ORDERING: 159 before this code**, same trap as 138. ⏳ **Not browser-verified yet** — the canonical check is an S3 Filipino T3 sheet with Exam unticked showing 100, not 87.

✅ **AN EVENT SAYS WHICH LEVELS IT IS FOR (migration 158).** Mr Ace: _"i have seen events named P6 Fieldtrip and its assigned to whole school which is wrong."_ `calendar_events.levels text[]` + `section_ids uuid[]`, mutually exclusive; `audience` stays and is **derived by trigger** because three readers branch on it. Measured after: a P1 register shows 7 July events and **none is the Leadership Camp**; P6 shows 10, camp 3/3. ⚠ **A multi-day event is not necessarily one row** — the T3 import wrote one row per tagged date, so matching a backfill on `start_date` scoped day 1 and left days 2–3 whole-school **while the count still read right**. ⚠ Closures are deliberately NOT level-scoped: `school_calendar` has no level column and all 28 labelled day rows are whole-school. ✅ **The source conflicts were settled by the sources, not by a judgement call** — Mr Ace: _"i mean the documents cant answer your concern?"_ They could. AY2025's register states _"Leadership Camp P4 - Sec 4"_ outright; the T3 date tags carry `SE` on 13 of 20 sheets for 14–16 Jul and none for the published 8–10 Jul (402 marks those days); 12 May holds 370 marks so Vesak's masthead date is AY2025's; and 5 Jul is a Sunday, so the Youth Day Celebration is the published 3 Jul. **Resolutions are ENCODED in `reconcile.ts::KNOWN_SOURCE_ERRORS` with their evidence**, so a re-run cannot undo them. ⚠ **Ask the documents before asking Mr Ace.** 🔴 **43 closures + 9 events fall outside every term window with nowhere to store them** (New Year, term breaks, the Nov–Dec block); both calendar tables are term-scoped. **Still open.**

⚠ **STILL OPEN, carried from 2026-09-03.** Only two accounts hold a role LIST (`kohsuat.hoon@`, `ace.guevarra@`); the other four teaching admins need the role granted before they see the switcher. Full account of that change: KD #206, archived in `docs/context/23-session-log.md`.

**Older entries have moved.** Everything before the current sprint now lives in `docs/context/23-session-log.md` — shipped features, closed decisions, superseded claims, and the gotchas they left behind. Read it when you pick up a quiet module, meet a reference you do not recognise, or are about to reopen something that may already have been measured and closed.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved across all moves. KDs were split into per-topic files under `docs/key-decisions/` (the root file is the index + KD-to-topic map); existing "KD #N" cites still resolve via the index, and global numbering is unchanged.

Historical plan and spec docs under `docs/superpowers/` cite the older `.claude/rules/key-decisions/<topic>.md` paths. Those are archival records of past work and were deliberately left as-written — read them as "the topic file for that KD", which now lives under `docs/key-decisions/`.
