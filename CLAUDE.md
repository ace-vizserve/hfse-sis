# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, Evaluation, P-Files, Records, Admissions, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

**Everything inside `.claude/rules/` is read into context automatically, every session.** That is a Claude Code behaviour, not something a file can opt out of — a `load: on-demand` line in a file's frontmatter is descriptive only and does not stop it loading. So only four files live there, deliberately:

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

- `.claude/rules/design-system.md` — binding design pointer; too important to risk missing (it is small, and only points at the full docs).
- `.claude/rules/key-decisions.md` — the KD index, so a "KD #N" cite resolves with no extra read.

Do not edit any of the four without explicit user approval, and **do not add new files to `.claude/rules/`** — anything put there is paid for in every session. On-demand material goes in `docs/` (below).

## Stable rules — on-demand (read with the Read tool when relevant)

These live **outside** `.claude/rules/`, so they cost nothing until read. Read before acting when any of the "Read when..." conditions apply.

| Rule                           | Read when...                                                                                                                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/rules/tech-stack.md`     | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha                                                                                                                                                      |
| `docs/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives                                                                                                                                                            |
| `docs/rules/env-vars.md`       | Touching `.env.local`, Supabase/auth plumbing, or Resend emails                                                                                                                                                                                        |
| `docs/rules/workflow.md`       | Finishing work — before reporting a task done, or at session wrap-up                                                                                                                                                                                   |
| `docs/key-decisions/*.md`      | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions. Find the topic file via the always-loaded index at `.claude/rules/key-decisions.md`, then Read only that file. |

## Reference docs

| Doc                                         | Read when...                                                                                                                     |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `docs/sprints/development-plan.md`          | Starting any task — status snapshot + current sprint                                                                             |
| `docs/context/01-project-overview.md`       | Onboarding                                                                                                                       |
| `docs/context/02-grading-system.md`         | Grade computation / formula                                                                                                      |
| `docs/context/03-workflow-and-roles.md`     | Permissions, locking, workflow                                                                                                   |
| `docs/context/04-database-schema.md`        | DB tables / queries                                                                                                              |
| `docs/context/05-report-card.md`            | Report card UI / PDF                                                                                                             |
| `docs/context/06-admissions-integration.md` | Admissions sync                                                                                                                  |
| `docs/context/admission-process.md`         | HFSE's 13-step intake process (Open House → Orientation); maps ~1:1 to the 9-stage SIS pipeline (`STAGE_KEYS`, KD #51/#59)       |
| `docs/context/07-api-routes.md`             | API contracts                                                                                                                    |
| `docs/context/08-admission-dashboard.md`    | Admissions analytics                                                                                                             |
| `docs/context/09-design-system.md`          | UI tokens, hard rules, page→component matrix, pre-delivery checklist                                                             |
| `docs/context/09a-design-patterns.md`       | Craft standard, canonical patterns, semantic color discipline                                                                    |
| `docs/context/10-parent-portal.md`          | Parent identity, linkage, SSO handoff                                                                                            |
| `docs/context/10a-parent-portal-ddl.md`     | Frozen admissions DDL (AY-prefixed tables)                                                                                       |
| `docs/context/11-performance-patterns.md`   | Any new page — auth/cache/parallel/loading checklist                                                                             |
| `docs/context/12-p-files-module.md`         | P-Files module — document types, statuses, architecture                                                                          |
| `docs/context/13-sis-module.md`             | Records module                                                                                                                   |
| `docs/context/14-modules-overview.md`       | Cross-module architecture, shared identity, navigation                                                                           |
| `docs/context/15-markbook-module.md`        | Markbook module scope                                                                                                            |
| `docs/context/16-attendance-module.md`      | Attendance module (Phase 1 + 1.1 shipped)                                                                                        |
| `docs/context/17-process-flow.md`           | Cross-module lifecycle + soft gates                                                                                              |
| `docs/context/18-ay-setup.md`               | Superadmin AY-rollover wizard                                                                                                    |
| `docs/context/19-evaluation-module.md`      | Student Evaluation module — FCA writeups + virtue theme (KD #49)                                                                 |
| `docs/context/20-dashboards.md`             | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts`                                      |
| `docs/context/21-stp-application.md`        | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` gating + STP-conditional doc slots (KD #61) |

## Session context

<!-- Scratch: session-learned context, caveats, in-flight work. Pruned periodically.
Stable rules live in `.claude/rules/*.md`; sprint history in `docs/sprints/development-plan.md` + `git log`. -->

**On prod and stable — KD #160/#161/#162 and #166–#175 all shipped; the index in `.claude/rules/key-decisions.md` resolves each one, so they are not re-listed here.** ⚠ Run **`scripts/audit-role-permissions.ts`** after any grant change: the live `role_permissions` table is authoritative over the code defaults, so a code-only edit is **inert**. **Four findings worth keeping resident, because each one silently produces a wrong answer:** `residenceHistory` is **heterogeneously typed in prod** (a JSON string from the portal/seeder, a real array from the SIS edit route), so any type probe must scan **every** value, never the first non-null; in `csv.extraColumns`, `defaultChecked: true` means always exported and omitting it means **never** exported; **the parent portal reads some tables DIRECTLY as the parent** — only `school_config` and `report_card_publications` are parent-readable and everything else silently returns zero rows (KD #165); and non-examinable subjects store a **band-representative integer** (95/87/82/70) in `quarterly_grade` standing in for a letter, so those values must never be compared against the formula. ⚠ **Still open (KD #174–179):** a students sync surviving a house is unexercised, and `scripts/audit-grade-recompute-drift.ts` reports AY2026 CLEAN (260 sheets / 4,636 entries) with a residual **58 entries across 50 sheets in AY2025** — closed year, all locked, undecided.

**Relief cover carries START and END dates — SHIPPED and browser-verified 2026-08-25 (migration 123 APPLIED; KD #191).** Relief teachers themselves and one-subject-teacher-per-class shipped 2026-08-13 (117/118; **KD #184 / #185**). ⚠ **123 is NOT that reversal being reversed** — still no `assignment_reliefs` table, just two nullable columns, and 117's own header had already named scheduled cover as the accepted cost this pays off. **Three things to carry without reading KD #191:** (1) **seeing a cover and acting on one are different questions** — the three `is_*_for_*` helpers carry the window, the `teacher_assignments_scoped_read` policy deliberately does NOT, so a substitute booked for next week sees it and opens nothing; **do not "fix" the policy to match.** (2) **The rule is written twice on purpose** (`relief_is_live` in SQL, `isReliefLive` in TS) because **five callers of `loadEffectiveAssignmentsForUser` pass the SERVICE client and bypass RLS** — a SQL-only window would be skipped by all five; `__tests__/auth/relief-window-parity.test.ts` pins them. (3) **A name on screen no longer means access** — filled amber is covering now, hollow is booked (`lib/relief/display.ts`). Surfaces: `/sis/admin/cover` (grouped by the ABSENT TEACHER, not by class) + a "You're covering" panel on home and the three module indexes. ⚠ **Date arithmetic bit once already** — `Date.UTC` takes a zero-indexed month and the cover board passed the real one, so finished cover vanished from the page entirely; everything else here compares `yyyy-MM-dd` as strings, which is safe. ✅ **No longer unproven at scale — the deployment landed 2026-08-26 (see the teacher-assignments entry below).** AY2026 now holds 115 assignments across 19 real teacher accounts. ⚠ **SOW is tied to this and deliberately not built:** Christina asked for relief monitoring and lesson plans in one breath (2026-08-21) — relief is her stated _reason_ for a teachers' dashboard. Mr Ace 2026-08-24: keep separate; the "You're covering" panel is the hook a lesson-plan link would hang on. **Her ask is not closed until she has seen the page.**

**AY2026 teacher assignments IMPORTED — 2026-08-26. AY2026 went from ZERO form advisers to 19 of 21 sections, which unblocks report-card publishing school-wide.** Source: `Teachers Deployment_Updated 29 Jun 26_Teacherscopy (1).xlsx` (repo root, gitignored — 11 sheets, and **all eleven matter**, Mr Ace's instruction: none supersedes another). Parser `lib/sis/backfill/deployment/workbook.ts` + generator `scripts/backfill/gen-teacher-assignments.ts` (preview/apply SQL, house-import pattern), 28 tests; **19 teacher accounts provisioned**, 115 rows applied. ⚠ **CO-ADVISERS AND CO-TEACHERS ARE NEW — migration 124 APPLIED, KD #192.** HFSE shares a subject between two teachers across different DAYS (Sec 3/Sec 4 Humanities, P2/P4 STAR) and shares Sec 4's form class between two advisers. **The workbook is not wrong and this is not a data-quality problem** — all three of its views agree; our schema was what could not express it. KD #185's rules are untouched (one owner per sheet, one adviser per class, same indexes); co roles sit outside them, so **no backfill**. An array of teacher ids was considered and REJECTED — relief and RLS are both per-row.

**Teaching assignments are PER ACADEMIC YEAR on screen as of 2026-08-27 — KD #193.** `?ay=` on `/sis/admin/staff` (both tabs) and the teacher page; a finished year is view-only, a future one stays editable; the year picker sits in the table card's header, filters below it. Same pass adopted migration 124's vocabulary end-to-end: **seven access gates compared the role literal while SQL had already widened**, so the database granted what the app refused (a co-teacher's sheet; a co-adviser's attendance, export, student summary, section list, markbook "My sheets"). ⚠ **The inverse is a DECISION, not an oversight** — coverage and name-of-record reads still count only `form_adviser`, because nothing stops a `co_adviser` row existing without one and that section would read green then fail at publish. Both halves are pinned in `__tests__/auth/assignment-read-classification.test.ts`; do not "fix" either direction.

⚠ **KD #194 — THE IMPORTER LOST ALL OF SECONDARY ONE AND SAID NOTHING. Fixed 2026-08-27; SQL REGENERATED the same day (123 rows).** `parseClassMajorSheet` bounded a band at the next header row ANYWHERE on the sheet, and a third Sec 1 timetable sitting three rows lower at a different column truncated the Sec 1 pair to one row: **both classes kept their adviser and lost every lesson, while the run reported "11 skipped" without naming them.** Now bounded on the band's own start column (secondary 120 → 176 lessons, primary byte-identical at 285). A second defect rode with it: the shared-subject check groups on RAW cell text, so one subject spelled two ways ("Arts and Design"/"Art and Design"; "STAR"/"STAR … [PE]") became two rows for one sheet, which the person-once index rejects — a post-pass now regroups on the resolved subject id. **The re-run was diffed tuple-by-tuple against the 2026-08-26 file before applying: ZERO removals, 9 additions, all of them Sec 1 Discipline 1 Global's subjects** — do the same on any future re-run, because `apply.sql` ends `on conflict do nothing` and therefore **deletes nothing**, so a row the new run stops producing survives in prod as a silent orphan. ⚠ **That diff also found what the 2026-08-26 import actually did: its file listed 115 rows but two were the SAME P6 Grit STAR row under two spellings, so 114 landed, not 115** — the spelling defect had already fired once and `on conflict do nothing` hid it. **Sec 1 Discipline 2 is STILL unstaffed and that is the Cambridge question, not a bug** (both colliding workbook classes are dropped by design).

⚠ **A THIRD IMPORTER DEFECT, found 2026-08-27 by asking "are those really the gaps" — and the answer was NO. THE SKIP LIST ONLY NAMES ROWS THE GENERATOR REFUSED TO WRITE, so it is silent about a sheet that ends up with nobody.** The real measure is every AY2026 grading sheet against its assignments: **125 sheets, 99 staffed, 26 unstaffed.** That exposed **PE living in the catalogue under TWO codes** — `PEH` "Physical Education and Health" (Global classes, and all of secondary in AY2025) and `PESTD` "Physical Education" (created 2026-07-16, the Standard classes). Both are PE; **the split is the school's and deliberate**. Mr Hanafi writes one phrase for all five classes and `SUBJECT_MAP` is flat, so every class resolved to `PEH` — right for two, **wrong for three, and silently**: the row inserted against a subject the class does not run while the sheet it should have claimed read unstaffed. Fixed by `EQUIVALENT_SUBJECT_CODES` + re-pointing to whichever code the SECTION actually holds a sheet for (the same "ask the data" move bare "Mother Tongue" already made), and the re-point is **reported**, not silent. ⚠ **A generator fix does not fix prod** — `apply.sql` only ever adds, so `scripts/backfill/teacher-assignments-pe-cleanup.sql` deletes the three stale rows, guarded by `not exists (a sheet for that code in that section)`; **that guard is the whole safety of the statement**, since without it the same delete takes the two legitimate Global rows. **Do NOT "clean up" the 5 `PMPD` rows the same way** — Pastoral Ministry has no sheet in EITHER year because it is taught and not graded, so those assignments are correct. **P1 Obedience's `CL` sheet is left alone** (the only Christian Living sheet in the year, on 1 of 21 classes, never timetabled in the workbook) — an unused empty sheet costs nothing and the AY2026 subject list is not ours to settle.

⚠ **What the importer genuinely will not decide — now WRITTEN AS A MESSAGE, drafted 2026-08-27 and NOT SENT** (`docs/training/sessions/…-Action-Items.md`, _To Hanafi — the AY2026 deployment_): seven items — (1) **"SECONDARY 1D2 (Cambridge)" and "Sec 1 Discipline 2 Standard" still resolve to ONE live section**, so both are dropped rather than let `on conflict do nothing` pick by row order. ⚠ **NEVER ask whether Discipline 2 is running** — full timetable, adviser Ms J, and **16 students / 14 active**. The Cambridge sheet is the odd one: adviser Ms Carl and **no teacher in any of its eight subject cells**. (2) **Sec 4 Excellence names two advisers.** (3) **Four shared class+subjects need a teacher of record.** (4) **Ms Jasmine Zhou Qi and Ms Li Qun have no email**, so no account — four Mother Tongue classes unstaffed. (5) **P5 Commitment's "Mother Tongue"** — ⚠ an earlier note called this resolved-as-Ms-Jasmine and that was OURS to unsay: the section holds **two** language sheets (MANDARIN, FIL), so the language itself is unknown, not just the person. (6) **Sec 3 Consistency English says "Relief Teacher"** — confirm Ms Fong Mei Yin Elaine. ✅ **CUT to six: P4 Trust and P2 Gentleness/Gratitude are OFF the list** — P4 Trust is staffed and correct (`radhika.putrevu@`), and the two P2 afternoon classes match no live section, so **neither costs a mark sheet**. They were drafted because they were true, which is not the test. ✅ **P1 Respect is OFF too — 2 students, 0 active**, an empty leftover and not, as this file previously said, "a live section the file never names." All checked 2026-08-27; **do not re-add any of the three.** ⚠ **Every existing source of "who teaches this" is worthless and all were checked** — `evaluation_writeups.created_by` is null on all 32 sections across both years; `grading_sheets.teacher_name` is blank for the Humanities pairs and carries two conflicting names for STAR, one an admissions officer; `report_card_comments` has **no author column at all**; `evaluation_subject_comments` is dormant (KD #114). **The cause is structural, so it will not improve: both years were backfilled by service-role scripts, so any author column records the operator.** `audit_log` is the only place a real actor survives. **Do not re-run that probe** (`scripts/probe-existing-teacher-evidence.ts`).

**ORDERED APPROVAL EXISTS — migrations 126 + 127 (KD #196), and the absence declaration is its first and only user.** A flow is an ordered list of stages; a stage holds one or more approvers and **the first to act carries it**; a rejection at any stage ends it; **the stages are configuration, not code** (`/sis/admin/approvers`, second section). Queue at `/attendance/declarations` + a "Waiting for you" panel on the Attendance index and section picker. ✅ **126 and 127 APPLIED 2026-08-27**, verified by `scripts/verify-approval-migrations.ts` — 10/10, including `42501 permission denied` from the ANON key on both `approval_advance` and `apply_change_request_atomic`. ⚠ **The browser pass has NOT happened**, and the first check is a teacher opening a class register (the `is_adviser_for_section` re-point). ✅ **The two steps are CONFIGURED and live: 1. Form class adviser (derived) → 2. Officer in charge.** ✅ **THE OFFICER IN CHARGE IS NOW PER HALF OF THE SCHOOL — migrations 128 + 129 APPLIED 2026-08-27; `verify-approval-migrations.ts` 13/14, the last one being the live rows themselves.** It shipped wrong first: both holders were seeded onto ONE step with first-to-act, reading "Primary or Secondary" as _either of two approvers_. **It is the two HALVES OF THE SCHOOL.** Mr Ace: _"the OIC is per year category hence Primary and Secondary — so if the submitted approval by the parent is a student from primary then use the OIC for Primary."_ **Ms Lhen (`lhen.mendoza@`) is OIC of PRIMARY; Ms Elaine Wee (`elaine.wee@`) of SECONDARY.** ⚠ **The evidence was already in the repo and I read past it**: `development-plan.md` has called the OIC a "school-half-scoped position" since 2026-08-19. **A tag on the PERSON (`applies_to_level_type`), not two steps** — two steps would send every filing through both officers and silently skip the one that did not apply. ⚠ **An uncovered half STALLS visibly and is never handed to the other officer**; the readiness badge names the half. ⚠ **THE APPROVERS ARE NOT A STATIC LIST (Mr Ace, and it took a second column):** editing a step re-points every request still WAITING at it (`repointWaitingStages`), because a step that had nobody on it at filing froze with an empty pool and stayed empty forever. Decided stages are never re-pointed. `approval_request_stages.level_type` exists to make that rebuild possible without the engine knowing its consumer. ⚠ **`scripts/seed-declaration-approval-steps.ts` argued the WRONG reasoning in its header** (it cited the AEB's "Gary or Nina", which really is two interchangeable people) — rewritten; it still no-ops when steps exist, so **production was fixed on the screen, not by re-running it.** ⚠ **Ms Elaine Wee holds `teacher`, not `school_admin`** — the old approver picker (gated on `grade_changes.approve`) would not have listed her, so widening eligibility to any staff account was load-bearing. ⚠ **TWO Elaines exist** (`elaine.fong@` is a different person, Sec 3 English) — never resolve "Ms Elaine" from a first name. **Six things to carry without reading KD #196:** (1) **this INVERTS the grade-change flow's assumption** — there, whoever acts first _becomes_ "primary", so the two slots are a pool and not an order; both flows now coexist and each screen has to say which one it is. The grade-change flow is **untouched** (`APPROVER_FLOWS`, the `>= 2` rule in five places, `lib/change-requests/`). (2) **The engine holds no key back to its consumer** — it points at `(subject_type, subject_id)`, and `student_declarations.status` is a **projection** the decide route refreshes. (3) **Named pools are FROZEN at filing; a derived (`form_adviser`) pool resolves LIVE**, because the adviser rule includes live relief cover and a frozen pool would route a sick child's absence to the teacher who is on leave. (4) ⚠ **The adviser rule was collapsed to ONE definition and this touched live RLS** — `is_section_adviser(section, user)` now carries it and `is_adviser_for_section(section)` is a one-line wrapper; **both keep their `execute` grant to `authenticated`**, because migration 114 revoked exactly this kind of helper and blanked every teacher's Teachers tab until 116. **Browser-verify as a teacher opening a class register before calling this done.** (5) **A step with nobody on it STALLS and is never skipped** — nobody holds the Officer in Charge post, so a filing will clear the adviser and stop; filing is deliberately not blocked by that, and `/sis/admin/approvers` says so in red. (6) 🔴 **127 also closes a year-old hole that is not ours**: `apply_change_request_atomic` still carried `grant execute … to authenticated` and is in **neither** 103's nor 104's lockdown list. ✅ **THE BELL NOW CARRIES DECLARATIONS (migration 129)** — Mr Ace: _"the whole UI flow is the same as grade change request."_ An earlier note here said the bell was "left alone on purpose"; that is superseded. It needed a **migration, not just a component**: the grade-change count reads `grade_change_requests` from the BROWSER under RLS, and declarations had no equivalent — the engine tables deny SELECT outright, `student_declarations` admits neither the OIC nor their role (Ms Elaine is a plain `teacher` who does not teach these children), and `audit_log` is registrar-and-above so the P-Files pattern never reaches an adviser. 129 admits a row only when you are **already on it**. ⚠ **The policy admits every status while the COUNT narrows to pending** — a `pending`-only policy makes a row vanish at the instant it is decided, so the UPDATE never arrives and a co-adviser's badge goes stale. ⚠ **`replica identity full` is required** or Realtime drops that UPDATE. ⚠ **The count hook carries NO per-role scope SQL** — RLS is the scope, so the badge cannot drift from the queue (the older hook restates its predicate in the browser, which is how that one reached six copies with three disagreeing). ✅ **A DECIDED FILING NO LONGER VANISHES** — `listDecidedStages` + a third **Decided** tab. It is a **sibling** of `listInboxStages`, not a flag: an adviser who approved step 1 is on neither the pool nor the section of step 2, so scoping history like the queue would hide a rejection from the person who acted on it. ⚠ **A rejected filing is represented by the step that REJECTED it, not the last step** — a rejection stops the ladder and later steps stay `waiting` forever. Ancillary: `scripts/verify-approval-migrations.ts` (read-only, **includes the anon-key call** 103 taught us to make) and `scripts/repair-declaration-approvals.ts` (now also stamps `level_type` on ladders built before 128 — without it, splitting the officers would empty those pools and strand them). ✅ **THE REGISTER WRITE IS BUILT — Phase 3, KD #197, no migration needed (125 already carried the three `register_*` columns and nothing had ever written them).** Approving the last step marks every school day of the filing `EX` / `mc` on the class register. **Four things to carry without reading KD #197:** (1) ⚠ **THE REASON FOLLOWS THE KIND OF FILING, NOT THE CERTIFICATE** — Mr Ace: _"it will be either MC or vacation leave depending on the type of declaration the parent has sent."_ **This corrects migration 125's own header**, which claimed `with_medical` picks the subtype; it does not change the mark at all, and 125 stays as history rather than being edited. (2) **Approval SUPERSEDES a day the teacher already marked `A`** — append-only, so the original mark and the moment it changed both survive. (3) ⚠ **`isNonSchoolDay` was a CLOSURE inside the daily PATCH handler and is now `lib/attendance/school-days.ts`** — one pure decision, two fetch strategies over it (`createNonSchoolDayChecker` per date for the daily route, `expandSchoolDays` one-query-per-window for a range); the legacy branch (**a term with zero calendar rows blocks nothing**) is load-bearing, and "has this term any rows" is asked of the WHOLE TERM, never of the window just read. `term_id` resolves **per day**, so a range crossing a term boundary is right for free. (4) ⚠ **A failed register write NEVER un-does the approval** — two people already decided and `approval_advance` would refuse a second click; the failure is stored on the filing, shown in the decision sheet, repaired by `scripts/repair-declaration-approvals.ts`. **Marks first, stamp second** — verified in prod at `10:01:19` / `10:01:20`. Non-school days inside a range are **skipped silently, not refused** (unlike the daily route's 409): a teacher clicking a holiday made a mistake, a date range has not. 🔴 **Found while verifying and NOT ours: AY2026 HAS NO TERM 4 ROW** (AY2025 has four, AY2027 has four undated; **T3 ends 2026-09-04**), so after that date attendance cannot be marked at all and a declaration expands to zero days **silently** — same shape as the earlier T2–T4 gap. The dates are the school's to supply. ✅ **PHASE 4 IS DONE TOO — travel marks the register, and the vacation count is now TRIPS (KD #199, no migration).** ⚠ **This CORRECTS KD #94**, which counted days: Mr Ace, asked whether one vacation leave is a day or a trip — _"who does vacation 1 day bruh its one trip"_ — so a five-day holiday had been reading as **5 used against an allowance of 1** on six screens (student card, Insights, drill, adviser dashboard, and the grid toast, which fired "over quota" on the second day of an ordinary trip). ⚠ **An earlier note in this file asserted "one allowance = one trip" with nothing behind it** — that was our own planning text quoted back as if it were the school's, and it only became a fact when he said it. **Four things to carry without reading KD #199:** (1) **a trip is a run of vacation-marked SCHOOL days** with no ordinary school day interrupting it — Fri/Mon/Tue is one trip because the weekend is not a school day, and two vacation days with an Absent between them are two. `lib/attendance/vacation-trips.ts` is the whole rule; **counting declarations was rejected** (it would miss a family who phoned, and would put a second source of truth beside the register). (2) **A trip crossing a term boundary counts ONLY in the term it STARTED in** (Mr Ace's call) — implemented as a `startedBeforeTerm` seed, exact because nothing between two terms is a school day. (3) ⚠ **`tallyVacationTripsByStudent` is a SIBLING of `tallyLeaveUsageByStudent`, never a flag on it** — the compassionate allowance really is 5 **days** per year, and sharing one function is how vacation inherited a day count. (4) ⚠ **The allowance WARNS and never BLOCKS** — the parent always files, the filing tells the approver _"This would be trip 2 of 1 allowed this term"_ in amber, and `used` excludes the filing being decided. ✅ **Measured against prod first: the correction moves NO live figure** (AY2026 has one student with vacation leave, a single day). ⚠ **Labels changed with the number** — the student card said "days remaining" over a day count. Nothing notifies the parent of the outcome. ✅ **TWO FILING GAPS CLOSED — migration 130 APPLIED 2026-08-27 (KD #195).** Both found by Mr Ace after Phase 4. (1) **A parent could file for days the school is shut** — now refused, and ⚠ **the test is "NO school day at all", never "contains a non-school day"** (Friday-to-Tuesday is a real filing; the weekend is already skipped), with **one child enough** since siblings can sit in different halves. (2) **The duplicate index caught one shape only** — it keys on `filed_by`, so **the OTHER parent could file the same absence**, and it matches exact dates, so **an overlapping range slipped through** (27–31 then 28–29). ⚠ **An identical re-send returns 200 and an overlapping one returns 409** — collapsing them would undo 125's deliberate "a double-tap is a SUCCESS" (a first cut did exactly that and was caught before shipping; `isExactMatch` keeps them apart). 🔴 **130 narrows the index to exclude `rejected` too**: it excluded only `cancelled`, so re-filing after the school asked for a certificate collided with the rejected row and handed the parent **a success carrying the rejection they were replacing**. ⚠ **Neither check turns its own failure into a refusal** — a lookup that throws lets the filing through.

✅ **THE BELL LEAD WAS RIGHT — CONFIRMED AND FIXED; migration 131 APPLIED 2026-08-28 (KD #196). ⚠ NOT YET BROWSER-VERIFIED** — the direct proof needs a signed-in approver with a filing actually waiting on them, which arrives with the travel pass. Mr Ace, 2026-08-27: _"i noticed declarations is not realtime in SIS"_. The browser recount `!inner` joins `approval_requests`, which sat at 126's `using (false)` while 129 opened `approval_request_stages` **only** — so the join drops every row. 131 mirrors 129's policy onto the requests table. **Three things to carry:** (1) 🔴 **it is NOT "0 for everybody in every module"** — that was our own overstatement; the grade-change half joins `grading_sheets` and `sections` (the latter has **no RLS at all**) and never broke, so the badge drops by _exactly_ the declaration count while the service-backed dropdown still lists the rows. (2) 🔴 **The failure is SILENT** — 126 denies via policy and never revoked the grant, so PostgREST returns `{ count: 0, error: null }` and the hook's "log it and freeze" branch, gated on `error`, never fires. (3) ⚠ **An anon-key call alone cannot prove it** — no session means `auth.uid()` is null, so 129's table returns zero too and the causes are indistinguishable; the proof is the policy text plus a service-vs-anon count with **no error**. `scripts/verify-bell-count.ts` does both and takes `--email`/`--password` for the direct version. Pinned by `__tests__/auth/browser-query-readable-joins.test.ts`.

✅ **THE ACTIVITY PANEL IS BUILT AND BROWSER-VERIFIED — 2026-08-28, NO MIGRATION, KD #200.** The bell is now an Activity side sheet: work waiting on you pinned above the tabs, then General / Mark changes / Declarations over a newest-first log that loads more as you scroll. Plus a **View history** timeline on both mark-change screens (`/markbook/change-requests`, `/markbook/grading/requests`). ⚠ **The declarations page did NOT need one** — its decision sheet has rendered the full ladder since it shipped; only the never-reached case was wrong. **Four things to carry without reading KD #200:** (1) **no table and no migration** — events are derived on read from `approval_request_stages` + `grade_change_requests`, so the log was full on day one rather than starting empty, and no future flow has to remember to write to it; (2) ⚠ **the two flows are guarded completely differently** — declarations are RLS-scoped to their ladder (129/131) while `grade_change_requests` is readable by **any account holding a role** (009), so `lib/activity/feed.ts`'s `.or(...)` is the ONLY thing standing between one teacher and the whole school's mark changes, and it must stay in the query, never a post-fetch `.filter()`; (3) ⚠ **oversight roles read everything, via the ONE `OVERSIGHT_ROLES` set the queue pages already share** — it shipped scoped to personal involvement for every role and a superadmin's panel was empty beside a full queue page; **the widening is the log only, never "Waiting for you"**; (4) **`audit_log` cannot back any of this** — registrar-and-above only, and it records who ACTED, not who should be TOLD. ⚠ **Two per-role arms were REMOVED as dead code** (a `school_admin` legacy-rows arm, an `academic_coordinator` approved-rows arm) — both only patched the missing oversight rule; do not reintroduce per-role exceptions in `markChangeScopeArms`.

**PARENTS CAN WRITE — the Student Absence and Travel Declaration, backend SHIPPED 2026-08-27 (migration 125 APPLIED; KD #195).** Action item #6. `student_declarations` + four parent endpoints; the portal UI is the other team's and its brief is `docs/handoff/2026-08-27-parent-portal-declarations.md`. ⚠ **This added NO parent→student link in Postgres and must not be read as having done so** — a parent is still role-less, the table denies `authenticated` every write, and the parent matches no arm of its read policy. Authorisation is the Bearer token + `getAllStudentsByParentEmail`, in the route, the only shape available. **Four things worth carrying:** (1) **one row per child** (`filing_group_id` groups a submission) because the first approver is THAT child's adviser and siblings are in different classes; (2) **`evidencePath` is authorised too, not just student numbers** — the upload writes to `declarations/<parent user id>/…` and a path outside the caller's folder is an attempt to attach someone else's MC to their own filing; (3) **`/enrolled-students` is a SECOND child list and picking the wrong one is silent** — `/students` is gated on PUBLICATION and correctly omits an enrolled child between publications, so using it for a picker hides a child from their own parent; (4) files go in the **existing public `parent-portal` bucket** under `declarations/` — an earlier plan made private storage its own phase citing migration 121, and that was wrong: P-Files already keeps passports, birth certificates and medical reports in that same bucket. ✅ **The approval itself is BUILT — see the block above (KD #196).** `status` now moves: `approved` when the last stage approves, `rejected` the moment anyone turns it down. The register write is still Phase 3. **Grade-change approval is explicitly NOT part of this** — his call: absence filing first.

**Write feedback — COMPLETE (2026-08-17).** All 80 write components share one lifecycle (`useWriteAction`); full account in **KD #186**, the defects it surfaced in **KD #190**. ✅ **The "14 write routes invalidate nothing" item is CLOSED and was wrong three ways (2026-08-27).** 🔴 **`change-requests/act` — the headline example — had invalidated since 2026-06-05** via `lib/change-requests/decide.ts`; the audit grepped route files and could not see a bust one module down. 🔴 **The "60-second TTL" on the queue and bell does not exist** — both are uncached by design and the badge is realtime; only the 30s Router Cache remains, which `revalidateTag` cannot touch. 🔴 **Six of the fourteen are `410 Gone` tombstones.** Four real hits were fixed (subject catalogue POST/PATCH, level-offerings, resync). 🔴 **Plus one nobody had recorded: `lib/markbook/overview-data.ts` tagged on `markbook:${academicYearId}` — a UUID — while every invalidator passes an ay_code, so that entry was never busted by anything.** The list now lives in `__tests__/cache/write-route-invalidation.test.ts`, where every exemption carries a reason and a stale entry fails the build. **Three remain genuinely open and are named there:** `grading-sheets/[id]/labels`, `sections/[id]/schedule`, and `sis/admin/users/[id]` (a real asymmetry — the sibling POST busts `teacher-emails`, this one does not).

⚠ **Test-flake triage — read before chasing a red suite.** ~4 files flake under full-suite load (`role-permissions-guardrails`, `student-lookup-sheet`, `grading-workbook-secondary-t2`, `data-table-export-sheet`) and this is pre-existing. **"They pass in isolation" stopped being reliable on 2026-08-18** — `role-permissions-guardrails` now times out alone too, because its one slow test `await import`s `role-permissions-editor` and transforming that React graph cold costs ~5.04s against a 5.00s limit. **Diagnose with `--testTimeout=30000` first**; if it passes with more time it is transform cost, not the code. And ⚠ **measure, don't estimate** — three agents raised six HIGH findings and half evaporated against prod.

**Disciplinary records (#7) — SHIPPED and browser-verified (2026-08-21 / 2026-08-24; migrations 120–122; KD #189).** Five screens, one table with a `record_type`, filing in Classroom and a school-wide register at `/records/discipline`. Full account in **KD #189** (`docs/key-decisions/records.md`). ⚠ **The one thing to carry without reading it: THE SYSTEM DECIDES NOTHING AND GENERATES NOTHING** — no threshold in `school_config`, no detection, no letter generation, `lib/compute/awards.ts` untouched, and the document is one optional **link** to the acknowledged copy, never an upload. Mr Ace killed automation explicitly (_"of course no automation you dummy thats too much"_); the handbook rule is the school's to maintain, not ours to encode. **Do not re-derive any of it from the sample documents.** ⚠ **Still unanswered after four asks: where the disciplinary _outcome_ is recorded.**

**Classroom Overview names the adviser and every subject teacher (2026-08-21)** — `lib/classroom/staff.ts`; recorded as an update to KD #160.

⚠ **`SheetContent` IS NOT A FLEX COLUMN, and this cost an afternoon.** Its variants are a plain block with `h-full`, so a body styled `flex-1 min-h-0 overflow-y-auto` — the standard way to scroll under a pinned header — is **inert**, and tall content silently overflows with no scrollbar. Five sheets already compensated by passing `flex flex-col` themselves; three in `components/classroom/` never did (`student-details-sheet`, `at-risk-lookup`, `file-discipline-record-button`) and had been broken since they were written — nothing in them was tall enough to prove it until the discipline form. All three now pass it. **The base was left alone deliberately:** `gap-4` in that class list is currently inert, and making it a flex column would make the gap real in every sheet that does not pass `gap-0`, shifting spacing app-wide. A note sits at the top of `components/ui/sheet.tsx`; fixing it properly is a sweep with its own test pass.

**MC upload (#6) RESHAPED 2026-08-17 — the parent files it, not the FCA.** **⚠ "Ms Tin" IS Christina — Tin is her nickname** (Mr Ace, 2026-08-18, correcting me). That matters twice over: it means **the AEB's "Ms Tin" is Christina**, so the board is Chandana + Christina + Norma + Gary + Nina and includes the Principal; and it means this is **not** two independent sources, as I first wrote. It is **Christina saying the same thing twice** — unprompted on 2026-08-12 (_"in my sons' schools, parents file a leave of absence online and attach the medical certificate"_), then showing Mr Ace the actual screen. Her sons being at another school is the same fact from both angles. **One source, but the most invested kind:** she raised it herself and then went and demonstrated it. His call: _"this is the best way since parents are the ones who initially have the doc."_ **Supersedes the 2026-08-12 FCA-uploads spec.** Parent portal → **Services** → **Student Absence and Travel Declaration**. Absence: multi-select student(s), start/end date, radio **with medical / without medical**, then an upload **or a `mc.gov.sg` link** (proof can be a URL, not a file), optional note to the teacher. Travel: student(s), duration yes/no → from/to dates, country, optional city. **What it is for, settled in conversation:** not a mailbox for MCs — today the reason for an absence is four disconnected things (a WhatsApp message, a paper MC in Hanafi's drawer, the teacher's guess between `A` and `EX`, and the mark), and this joins them so the teacher stops guessing and the proof sits on the day. The payoff is #7's warning letter, which lists absence dates as unexcused — whoever writes it must know which days carried an MC, and today that means asking Hanafi. **The register mark stays the record; the declaration is the evidence**, and `ex_note` (KD #177) keeps its place for when a parent files nothing. ⚠ **The travel declaration IS the LOA request the school already has** — the #7 letter states the rule (one vacation leave per term, filed five working days ahead) and we already model both the quota (KD #76) and the mark (`ex_reason='vacation'`, migration 070); the five-day rule stays **theirs to enforce, not ours to encode**. **Approvers answered 2026-08-19: "Form Class Adviser" then "Officer in Charge (Primary or Secondary)".** ✅ There IS an approval step, so a parent's declaration is a request rather than a fact — and **the FCA being first largely defuses the design worry**, since the FCA is also the person who marks the register and is therefore in the loop either way. ⚠ **What approval DOES is still unanswered** — the message asked whether there is a process and who approves, not what it does to the register; ask that plainly rather than inferring it. ✅ **The ORDER is SEQUENTIAL — FCA then OIC, in that order** (Mr Ace, 2026-08-21; see the #10/#11 line below, where the same ruling settles every flow). ⚠ **"Officer in Charge" exists nowhere in this system and is NOT new: OIC is the middle station of her pre-issuance grade-change chain**, so one unmodelled, school-half-scoped position now gates two unrelated flows. **This is the THIRD flow needing ordered approval** (grade changes, mid-year section moves, absence declarations) — past the point where each deserves bespoke logic. ⚠ **Scale:** a parent-facing form + an approval queue + a write into the attendance register is an order of magnitude past "an FCA upload button", and the external portal reads only `school_config` + `report_card_publications` today (KD #165).

**#10 and #11 are ONE feature — configurable approval steps (direction only, Mr Ace 2026-08-18; nothing designed or built).** The trigger is publication and Christina already said so (46:04): two approvers before the report book is issued, AEB after. **ORDERING SETTLED 2026-08-21 (Mr Ace): every approval flow is sequential — his frame is MS Teams Approvals — with exactly ONE exception, the pre-publication grade change, which keeps today's behaviour (a pool of approvers, first to act decides, no order).** ✅ **This kills the harder half of the design: unanimity stops being a second mechanism.** The earlier note here claimed a step is "either one named person in sequence, or a group where everyone signs, and the system needs BOTH" — but if every step is sequential then a five-member board where all five must sign is simply **five steps in a row**, so the model is ONE concept, an ordered list of steps. ⚠ **Correction, and it was ours: "the AEB is unanimous, order irrelevant" was an INFERENCE laid on top of Chandana, not her words.** She said only _"all the AEB members will review the request and only after the full approval, the changes can be made"_ — that states unanimity and says **nothing** about order, so sequential-and-unanimous is fully compatible with what she told us. ⚠ **The one real cost is operational, not modelling: a queue stalls where a pool does not** (member 3 away blocks 4 and 5). Do not reopen it as a discussion — **the "specific AEB approval form" she named was REQUESTED from Chandana 2026-08-21, awaiting reply**; numbered signature blocks vs a grid of five names answers it outright, the same anchor that worked for the incident report. The same message asks whether **AEB membership changes** — re-aimed before sending from the draft's "can it be updated in the system" (ours to decide) to a fact only she holds, because a rotating board means an old approval must keep who was on it **then** (KD #147). ⚠ **"Except" means Christina's five-station pre-issuance chain is NOT being built** — the cautious call and the right one, since it rests on a single message this file marks unverified and contradicts her own 46:04 "two approvers". **Configuration is the hedge, not a nicety:** the five-station chain rests on one message whose attribution the training file marks unverified and which contradicts Christina in the room — if steps are configurable that contradiction becomes a setting instead of a blocker, and nobody has to make her adjudicate her own two answers. **Two ordered flows are already on record** (grade changes here; mid-year section moves ending "Mr. Gary final approver", explicitly not AEB — admin session §C), which is what makes this config rather than a hardcoded chain. **Both ordering and unanimity are new** — today there are two approvers, first-to-act sets the status, and nothing reads `secondary_decision`. Home: `/sis/admin/approvers`, and `APPROVER_FLOWS` was already designed for a second route.

**Look up student — ONE MEASURE AT A TIME (2026-08-24; KD #188, browser-verified).** Full account + its two traps live in the KD; nothing extra to carry here.

**Feedback (2026-07-31): 5 of 11 shipped** (#1 excused note, #2 house, #3 whole-year view, #4 at-risk both halves, #5 teacher student view). The rest are blocked on answers, not code. **Christina replied 2026-08-12 — see _Answers received_ in the training file.** MC is spec'd and buildable (FCA uploads, FCA+ views, reason stays off the report card — she ratified the live behaviour). **The incident-report sample arrived 2026-08-13** (#7) and it moves the ask more than it settles it: these reports are **already computer-generated and sequentially numbered** (case 702, so ~701 precede it — a migration question nobody has asked), the **filer is an office, not the FCA** we assumed, and **the form carries no disciplinary outcome at all** — so the "warning letter or suspension" half of her 18:20 ask is still unanswered, and the picklist behind "Nature of incident" is unseen. The **awards sample has not arrived**; who records an award was decided by Mr Ace 2026-08-12 (FCA and up). ⚠ **Her approvals answer is a different design, not a widening:** a five-station sequential chain (subject monitor → subject head → OIC → Assistant Principal → Principal) before issuance, and the **AEB — a five-member board** — after. It contradicts her 46:04 "two approvers" and needs ordered approval, which the schema has no concept of. **Whether it belongs in the SIS is CLOSED — it does, and she said so in the room** (50:57: the same approvers already approve via MS Teams Approvals, "that will be superseded by this already"). So this replaces a running digital flow rather than inventing one; only _how_ is open, and ordered approval needs a position, a current holder and a rule for where a rejection returns to. Seven named bodies (incl. Discipline Committee, AEB) exist nowhere as roles. Third staff member on personal Gmail (Ms Norma), and she sits on the AEB. **The message to Christina was SENT 2026-08-11** (`docs/training/2026-08-11-questions-for-academic-head.md`) — 4 asks, consolidating what were five drafted messages and ~20 questions; Mr Ace answered the rest himself and the decisions table at the top of the feedback file is authoritative, **do not re-ask them**. **ONE send still outstanding: Koh** (asks nothing — a notification plus the exam-total correction; both its quoted facts re-verified 2026-08-21, `GRADE_ALERT_THRESHOLD = 5` and the A→C band display). **Two went out 2026-08-21 and are awaiting replies — Chandana** (the AEB approval form + does the board's membership change) **and Wynne**, hers carrying one item more than the draft: the TOR template **plus the additional list of documents to track for student P-Files** — new, not from this session, and it will change `DOCUMENT_SLOTS` if she names slots we do not hold. Hanafi's was dropped unsent. ⚠ **Christina reopened SOW on 2026-08-21** — unprompted, asking for a teachers' dashboard for "lesson planning, scheme of work and teaching and delivery matters", explicitly so a substitute can see the lesson topics when a teacher is absent. **SOW was built twice and removed both times** (migrations 058–066; KD #108 then KD #110), and the removal commit says why: _"zero real users — HFSE teachers maintain their SOW in external documents"_. **Her reason is new and is the first good one** — both prior builds were about authoring and spot-checking; neither ever had a **reader**. ⚠ **But the adoption trap is unchanged: the work falls on the subject teacher and the benefit falls on the substitute, only on days somebody is sick** — and a stale lesson plan is worse than none. **Proposed (ours, not her ask): a LINK per class/subject/term to where the SOW already lives**, exactly the discipline-document call — the ask is the record, not the file. **Show her Classroom (KD #160) before scoping anything**; it is already the per-class teacher workspace and lacks only lesson topics. Detail in the session file under _Answers received · 2026-08-21_; **nothing is scoped, costed or approved.** The **house import SQL (402 students, dry run clean) is still not run**. Re-read the drafted message blocks whenever something ships: twice a message has asked the school to decide something already built. **A third failure mode, found 2026-08-11:** the blocks accrete invented questions. The four to Christina carried ~20 and now carry 6 — cut were report-card/parent "reach" questions on features that do not exist, storage mechanics that are ours to decide, and one question she had already answered on the recording. **Ask only what the build needs, anchor every question to something they said, and save reach questions for features already live** (they exist because house shipped to prod unratified — an unbuilt feature has no default to ratify).

**Future work (deferred backlog) — the 2026-07-31 training items, the WW/PT max-score gap, and the open questions are tracked in `docs/sprints/development-plan.md`.** Also: late-semantics **C1** (derive the late label from earliest-enrollment across the ~8 enum sites — hardening, not a bug) + multi-interval re-enrol proration (known limitation); per-module compare trend charts (post-go-live); Sec 4 Economics card (no Sec 4 students yet); self-serve invite flow (KD #87, `/auth/setup`); attendance audit-log server-side pagination; VL bulk-import parser (KD #94); PTC digitization as a separate surface (DB tables intact, KD #114); **config-placement review** — operator-tuned settings → business module, structural config stays in SIS Admin (KD #48; clearest candidate discount-codes → Admissions; **do NOT reverse KD #118** — early-bird stays in SIS).

**Recent sprints:** full history (Sprint 28→67 + the 2026-07 bug-hunt, KDs #63–190, migrations 030–122) lives in `docs/sprints/development-plan.md` (status snapshot at top) + `git log`. Don't duplicate it here.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved across all moves. KDs were split into per-topic files under `docs/key-decisions/` (the root file is the index + KD-to-topic map); existing "KD #N" cites still resolve via the index, and global numbering is unchanged.

Historical plan and spec docs under `docs/superpowers/` cite the older `.claude/rules/key-decisions/<topic>.md` paths. Those are archival records of past work and were deliberately left as-written — read them as "the topic file for that KD", which now lives under `docs/key-decisions/`.
