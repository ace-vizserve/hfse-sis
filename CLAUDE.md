# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, Evaluation, P-Files, Records, Admissions, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

**Everything inside `.claude/rules/` is read into context automatically, every session.** That is a Claude Code behaviour, not something a file can opt out of — a `load: on-demand` line in a file's frontmatter is descriptive only and does not stop it loading. So only four files live there, deliberately:

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

- `.claude/rules/design-system.md` — binding design pointer; too important to risk missing (it is small, and only points at the full docs).
- `.claude/rules/key-decisions.md` — the KD index, so a "KD #N" cite resolves with no extra read.

Do not edit any of the four without explicit user approval, and **do not add new files to `.claude/rules/`** — anything put there is paid for in every session. On-demand material goes in `docs/` (below).

## Stable rules — on-demand (read with the Read tool when relevant)

These live **outside** `.claude/rules/`, so they cost nothing until read. Read before acting when any of the "Read when..." conditions apply.

| Rule                           | Read when...                                                                                                                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/rules/tech-stack.md`     | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha                                                                                                                                                      |
| `docs/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives                                                                                                                                                            |
| `docs/rules/env-vars.md`       | Touching `.env.local`, Supabase/auth plumbing, or Resend emails                                                                                                                                                                                        |
| `docs/rules/workflow.md`       | Finishing work — before reporting a task done, or at session wrap-up                                                                                                                                                                                   |
| `docs/key-decisions/*.md`      | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions. Find the topic file via the always-loaded index at `.claude/rules/key-decisions.md`, then Read only that file. |

## Reference docs

| Doc                                         | Read when...                                                                                                                     |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `docs/sprints/development-plan.md`          | Starting any task — status snapshot + current sprint                                                                             |
| `docs/context/01-project-overview.md`       | Onboarding                                                                                                                       |
| `docs/context/02-grading-system.md`         | Grade computation / formula                                                                                                      |
| `docs/context/03-workflow-and-roles.md`     | Permissions, locking, workflow                                                                                                   |
| `docs/context/04-database-schema.md`        | DB tables / queries                                                                                                              |
| `docs/context/05-report-card.md`            | Report card UI / PDF                                                                                                             |
| `docs/context/06-admissions-integration.md` | Admissions sync                                                                                                                  |
| `docs/context/admission-process.md`         | HFSE's 13-step intake process (Open House → Orientation); maps ~1:1 to the 9-stage SIS pipeline (`STAGE_KEYS`, KD #51/#59)       |
| `docs/context/07-api-routes.md`             | API contracts                                                                                                                    |
| `docs/context/08-admission-dashboard.md`    | Admissions analytics                                                                                                             |
| `docs/context/09-design-system.md`          | UI tokens, hard rules, page→component matrix, pre-delivery checklist                                                             |
| `docs/context/09a-design-patterns.md`       | Craft standard, canonical patterns, semantic color discipline                                                                    |
| `docs/context/10-parent-portal.md`          | Parent identity, linkage, SSO handoff                                                                                            |
| `docs/context/10a-parent-portal-ddl.md`     | Frozen admissions DDL (AY-prefixed tables)                                                                                       |
| `docs/context/11-performance-patterns.md`   | Any new page — auth/cache/parallel/loading checklist                                                                             |
| `docs/context/12-p-files-module.md`         | P-Files module — document types, statuses, architecture                                                                          |
| `docs/context/13-sis-module.md`             | Records module                                                                                                                   |
| `docs/context/14-modules-overview.md`       | Cross-module architecture, shared identity, navigation                                                                           |
| `docs/context/15-markbook-module.md`        | Markbook module scope                                                                                                            |
| `docs/context/16-attendance-module.md`      | Attendance module (Phase 1 + 1.1 shipped)                                                                                        |
| `docs/context/17-process-flow.md`           | Cross-module lifecycle + soft gates                                                                                              |
| `docs/context/18-ay-setup.md`               | Superadmin AY-rollover wizard                                                                                                    |
| `docs/context/19-evaluation-module.md`      | Student Evaluation module — FCA writeups + virtue theme (KD #49)                                                                 |
| `docs/context/20-dashboards.md`             | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts`                                      |
| `docs/context/21-stp-application.md`        | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` gating + STP-conditional doc slots (KD #61) |

## Session context

<!-- Scratch: session-learned context, caveats, in-flight work. Pruned periodically.
Stable rules live in `.claude/rules/*.md`; sprint history in `docs/sprints/development-plan.md` + `git log`. -->

**On prod, stable (detail in the KDs, not here):** capability layer + `SECURITY DEFINER` lockdown (KD #166/#167, migrations 101–104); Document Completeness table (KD #168); academic coordinator gains SIS Admin, loses document validation (KD #169 + #166 update, migrations 105/106); home offers work by JOB not role (KD #170); scoped FCA attendance dashboard (KD #171); Classroom timeline grouping (KD #172); link-layer/capability consistency (KD #173); Classroom module (KD #160), column labels (KD #161), CSV export (KD #162). Run **`scripts/audit-role-permissions.ts`** after any grant change — the live `role_permissions` table is authoritative over the code defaults, so a code-only edit is inert. **Findings worth keeping:** `residenceHistory` is **heterogeneously typed in prod** (JSON string from the portal/seeder, real array from the SIS edit route) so any type probe must scan every value, never the first non-null; `csv.extraColumns` means `defaultChecked:true` = always exported, omitted = **never**; **the parent portal reads some tables DIRECTLY as the parent** — only `school_config` + `report_card_publications` are parent-readable, everything else silently returns zero rows; and non-examinable subjects store a **band-representative integer** (95/87/82/70) in `quarterly_grade` standing in for a letter, so never compare those against the formula. **Still open from 2026-08-01→04 (KD #174–179):** a students sync surviving a house is unexercised, and `scripts/audit-grade-recompute-drift.ts` reports AY2026 CLEAN (260 sheets / 4,636 entries) with a residual **58 entries across 50 sheets in AY2025** (closed year, all locked) — undecided.

**On prod, applied + browser-verified (2026-08-13; migrations 117/118). Relief teachers — KD #184, rebuilt; one subject teacher per class — KD #185.** Cover is now **one nullable column**, `teacher_assignments.relief_teacher_user_id`: set it, the substitute gets the class; clear it, they lose it. 117 drops the `assignment_reliefs` table, the `has_active_relief_for_assignment` helper and the whole date window that 112–116 had built. ⚠ **The lesson is the reversal, not the feature.** All that machinery served a requirement nobody asked for — that cover keep its own history — when `audit_log` already had it, and the date window was a _second source of truth about who may act on a class_ (115 existed only because SQL and TypeScript disagreed about it). Mr Ace's whole spec was one sentence: _"it's like an enable and disable button."_ **A requirement you cannot attribute to the person asking is one you made up.** 118 closes a gap open since **003**, whose header says `subject_teacher — one per (section, subject) pair` while the index it created was on `(teacher_user_id, section_id, subject_id)` — which only stops the _same_ teacher twice, so two teachers could share one class's Filipino, and did; **when a constraint contradicts its own comment, the constraint is the bug**. **Verified:** `scripts/verify-relief-migrations.ts` (read-only, safe on prod) passes 7/7, and Mr Ace confirmed in a browser that a teacher can mark their own sheet, that adding a relief opens the cells for the substitute, and that clearing it closes them again. Still keep from the first build: **a policy helper is not an RPC** — 114 revoked `EXECUTE` from `authenticated` on a function an RLS policy calls, and policies run as the querying role, so every cookie-scoped read of `teacher_assignments` died while the service-client staff directory showed the same rows fine. That is why `verify-relief-migrations.ts` says in its own header that a service-role check **cannot** see RLS. **Only 3 teacher assignments exist in AY2026** (two test accounts), so nothing here — cover, the bulk save, the new subject rule — has met realistic scale; that waits on Hanafi's deployment (26 teachers, ~147 assignments).

**Write feedback — COMPLETE (2026-08-17; KD #186). All 80 write components converted; the guard test is green across every root.** Full detail in KD #186 (`docs/key-decisions/ui.md`); plans `squishy-popping-lagoon.md` + `warm-roaming-cupcake.md`. **New layer:** `lib/hooks/use-write-action.ts` (one lifecycle per write — pending toast → work → `onResolved` → **awaited** `router.refresh()` → terminal toast; never rejects) on `use-refresh-transition.ts`, plus `Button loading`/`loadingText` and a complete `__tests__/_utils/mock-toast.ts`. The audit that started it: `toast.error` was already in 78 of 78 write components and `toast.success` in 76 — **the in-flight phase was in exactly 1**. `__tests__/ui/write-feedback-coverage.test.ts` now enforces it; every exemption carries `{decided, why}`, stale entries fail, and positive assertions check the exempt files still do the thing that exempts them. ⚠ **Three rules the guard itself taught:** a write-detector cannot be `jsonInit(` alone (multipart uploads cannot use it, so the app's slowest write went unclassified); a source scan must **strip comments**, or it flags the very comments that explain the fix; and `useDebouncedRefresh(() => router.refresh())` is not a second refresh, so the sweep reads **statements, not files**. ⚠ **The helper's `success` resolver may return `null`** (mirroring `error`) — a write can succeed and still owe a WARNING, and 5 surfaces do. ⚠ **`loadingText` changes a button's accessible name in flight** (“Save” → “Saving…”), and the success toast now lands AFTER the awaited refresh — together these broke 6 existing tests that asserted the old ordering or selector. `toast.loading` had no `duration: null` and would vanish mid-write; nobody had noticed because nothing called it. **Four defects in one path (assigning a class):** the column is `classUpdatedby` — 8 of 9 `*Updatedby` columns lost the capital, `orientationUpdateby` also lost the "d", and the same key in `section-transfer.ts` failed SILENTLY so every transfer left `classSection` stale; a single-student sync could not see a section's other index numbers so it always asked for #1 (withdrawn students keep theirs); `transfer_student_section` always INSERTed so moving a student BACK collided (migration 119 reuses the row, keeping its number); and "Students needing setup" asked for a `students` row (the person) instead of an enrolment, so a failed assign hid the student from the one screen that could fix them. **Stale-surface audit — all three shapes closed.** Both Class B late-enrollee bugs (`edit-stage-dialog`, `enrolment-edit-sheet`) are fixed by construction. **Rule adopted: the component that owns the write owns the wait** — an `onSaved`/`onDone`/`onCreated` prop whose whole body was `router.refresh()` is deleted, not kept (4 were, and 2 parents had been double-refreshing). Three further defects surfaced by converting: `classroom-settings-form` never refreshed at all; `bulk-publish-dialog` reported total failure as a green success toast; and three writes (2 in `section-subjects-panel`, 1 in `staff-assignment-sheet`) reported nothing on success. ⚠ **Still open: 14 write routes invalidate nothing** (`change-requests/act` first — everyone else's queue and bell go stale). **Attendance Submit 90 → 6 round-trips** (`writeDailyBatch`, bounded waves of 8) and now all-or-nothing. ⚠ **`after()` was tried for audit rows and REVERTED** — an audit row is a compliance record and dropped post-response work leaves a hole nothing reports. **Three latency items were examined and deliberately dropped** (invalidation scoping, the grading page's render-time RPC, caching the AY lookup) — reasons in the plan; do not re-add them from the original list. **Profile/family sheets (browser-verified 2026-08-14):** the PATCH schema is now **partial**, and that is load-bearing both ways — required keys meant the sheet could never save (24 invisible medical fields), and the obvious fix would have written null over every one of them on the first save. `handleSubmit` now has an invalid handler, so a rejection is visible instead of a dead button. Full name is **derived and disabled** on the student and all three parent panels (`lib/sis/full-name.ts` — compose, never split: DELA CRUZ / SANTHOSH KUMAR). **Carried forward:** ⚠ measure, don't estimate (three agents' six HIGH findings, half evaporated against prod); ~4 files flake under full-suite load (`role-permissions-guardrails`, `student-lookup-sheet`, `grading-workbook-secondary-t2`, `data-table-export-sheet`) and pass in isolation — pre-existing. Earlier the same week: KD #181 (teacher student drawer), #182 (adviser at-risk list), #183 (silent-truncation guardrails).

**Disciplinary records (#7) — BACKEND ONLY, 2026-08-17. Migration 120 is NOT APPLIED and there is no UI.** Table `student_discipline_records`, `lib/discipline/{queries,mutations}.ts`, `lib/schemas/discipline.ts`, `canManageAnyDisciplineRecord` in `lib/classroom/scope.ts`, and POST/GET/PATCH under `app/api/classroom/[sectionId]/students/[studentNumber]/discipline/`. Full suite green (2878). Not reachable from any screen; the form and lists are next, and **Mr Ace wants a visual pass before any JSX**. ⚠ **THE SYSTEM DECIDES NOTHING AND GENERATES NOTHING, and that is the whole design.** Two school documents arrived 2026-08-14. The second was a **first warning letter on _attendance_** — a hand-typed Word template, signed Assistant Principal, noted by the Principal, listing absence dates and citing "below 80%" — so **#7 has two independent entry points**, incident-driven and register-driven, and the record had treated it as one thing. Mr Ace killed automation and killed encoding the rule: _"of course no automation you dummy thats too much"_, _"handbooks change let them do that themselves thats there role and respobsibility"_. So **no threshold in `school_config`, no detection, no letter generation**, and `lib/compute/awards.ts` is untouched even though that letter asserts an attendance shortfall forfeits academic awards — the rule lives in the Student Handbook (pp. 16–18), which the school revises on its own schedule, and a hardcoded copy goes stale **silently**. **Do not re-derive any of this from the samples.** Decisions: **one table with a `record_type`, not two** ("one list is fine for now its basically a type atp no?") — the attendance letter hangs off no incident, so they are siblings, never parent/child; **filing is open to any staff member** (Chandana: "the person in charge who is present at the venue"), **editing is the filer plus leadership**; **filing lives in Classroom, not Records**, because Records student pages are oversight-only; and **no new capability** — reach is gated by the section exactly as the Classroom drawer is (KD #181), which also sidesteps KD #166's "a code-only capability is inert". `nature` is **free text** until the school sends the picklist (one value seen); `student_id` is **required** though optional on their form; **`details`/`remarks` never reach `audit_log`** (append-only, coordinator-readable — the `ex_note` line, migration 109); **attachments deliberately NOT built** — the only bucket is public-by-URL with no signed-URL path anywhere, and a child's behavioural record is the wrong place to be the first exception; and **no sequence number**, since their cases run to 702 and nobody has decided whether those come across. `__tests__/auth/assignment-read-classification.test.ts` classifies both routes **`act`** — a relief teacher covering the class IS the person at the venue. **Still unanswered after four asks: where the disciplinary _outcome_ is recorded.** ⚠ **The awards sample (2026-08-14) is also not what was expected** — a **Principal's List** certificate, _"GPA of 97 during Term 1 & Term 2"_, i.e. an academic honour computed from grades, not the competition register #8/#9 describe. **"Term 1 & Term 2" has no implementation** (`computeGeneralAverage` is full-year, null until T4; the per-term mean is deliberately tier-less) and **"GPA" appears nowhere in the codebase**. Not scope until Christina says so — it answered "send me a sample", and _nobody asked for certificate generation_.

**MC upload (#6) RESHAPED 2026-08-17 — the parent files it, not the FCA.** Ms Tin showed Mr Ace her own child's school SIS; his call: _"this is the best way since parents are the ones who initially have the doc."_ **This supersedes the 2026-08-12 FCA-uploads spec and un-parks Christina's own version of the same idea** — two independent sources now, one with a working screen. Parent portal → **Services** → **Student Absence and Travel Declaration**. Absence: multi-select student(s), start/end date, radio **with medical / without medical**, then an upload **or a `mc.gov.sg` link** (proof can be a URL, not a file), optional note to the teacher. Travel: student(s), duration yes/no → from/to dates, country, optional city. **What it is for, settled in conversation:** not a mailbox for MCs — today the reason for an absence is four disconnected things (a WhatsApp message, a paper MC in Hanafi's drawer, the teacher's guess between `A` and `EX`, and the mark), and this joins them so the teacher stops guessing and the proof sits on the day. The payoff is #7's warning letter, which lists absence dates as unexcused — whoever writes it must know which days carried an MC, and today that means asking Hanafi. **The register mark stays the record; the declaration is the evidence**, and `ex_note` (KD #177) keeps its place for when a parent files nothing. ⚠ **The travel declaration IS the LOA request the school already has** — the #7 letter states the rule (one vacation leave per term, filed five working days ahead) and we already model both the quota (KD #76) and the mark (`ex_reason='vacation'`, migration 070); the five-day rule stays **theirs to enforce, not ours to encode**. ⚠ **Blocked on one answer, Mr Ace asking Ms Tin: what does approval DO** — register updates itself, or the adviser is merely told and still marks it? Also: who approves on their system. ⚠ **Scale:** a parent-facing form + an approval queue + a write into the attendance register is an order of magnitude past "an FCA upload button", and the external portal reads only `school_config` + `report_card_publications` today (KD #165).

**Feedback (2026-07-31): 5 of 11 shipped** (#1 excused note, #2 house, #3 whole-year view, #4 at-risk both halves, #5 teacher student view). The rest are blocked on answers, not code. **Christina replied 2026-08-12 — see _Answers received_ in the training file.** MC is spec'd and buildable (FCA uploads, FCA+ views, reason stays off the report card — she ratified the live behaviour). **The incident-report sample arrived 2026-08-13** (#7) and it moves the ask more than it settles it: these reports are **already computer-generated and sequentially numbered** (case 702, so ~701 precede it — a migration question nobody has asked), the **filer is an office, not the FCA** we assumed, and **the form carries no disciplinary outcome at all** — so the "warning letter or suspension" half of her 18:20 ask is still unanswered, and the picklist behind "Nature of incident" is unseen. The **awards sample has not arrived**; who records an award was decided by Mr Ace 2026-08-12 (FCA and up). ⚠ **Her approvals answer is a different design, not a widening:** a five-station sequential chain (subject monitor → subject head → OIC → Assistant Principal → Principal) before issuance, and the **AEB — a five-member board** — after. It contradicts her 46:04 "two approvers" and needs ordered approval, which the schema has no concept of. **Whether it belongs in the SIS is CLOSED — it does, and she said so in the room** (50:57: the same approvers already approve via MS Teams Approvals, "that will be superseded by this already"). So this replaces a running digital flow rather than inventing one; only _how_ is open, and ordered approval needs a position, a current holder and a rule for where a rejection returns to. Seven named bodies (incl. Discipline Committee, AEB) exist nowhere as roles. Third staff member on personal Gmail (Ms Norma), and she sits on the AEB. **The message to Christina was SENT 2026-08-11** (`docs/training/2026-08-11-questions-for-academic-head.md`) — 4 asks, consolidating what were five drafted messages and ~20 questions; Mr Ace answered the rest himself and the decisions table at the top of the feedback file is authoritative, **do not re-ask them**. **Two sends still outstanding**, one to Koh (asks nothing — a notification plus the exam-total correction), one to Wynne (the TOR template). Hanafi's was dropped unsent. The **house import SQL (402 students, dry run clean) is still not run**. Re-read the drafted message blocks whenever something ships: twice a message has asked the school to decide something already built. **A third failure mode, found 2026-08-11:** the blocks accrete invented questions. The four to Christina carried ~20 and now carry 6 — cut were report-card/parent "reach" questions on features that do not exist, storage mechanics that are ours to decide, and one question she had already answered on the recording. **Ask only what the build needs, anchor every question to something they said, and save reach questions for features already live** (they exist because house shipped to prod unratified — an unbuilt feature has no default to ratify).

**Future work (deferred backlog) — the 2026-07-31 training items, the WW/PT max-score gap, and the open questions are tracked in `docs/sprints/development-plan.md`.** Also: late-semantics **C1** (derive the late label from earliest-enrollment across the ~8 enum sites — hardening, not a bug) + multi-interval re-enrol proration (known limitation); per-module compare trend charts (post-go-live); Sec 4 Economics card (no Sec 4 students yet); self-serve invite flow (KD #87, `/auth/setup`); attendance audit-log server-side pagination; VL bulk-import parser (KD #94); PTC digitization as a separate surface (DB tables intact, KD #114); **config-placement review** — operator-tuned settings → business module, structural config stays in SIS Admin (KD #48; clearest candidate discount-codes → Admissions; **do NOT reverse KD #118** — early-bird stays in SIS).

**Recent sprints:** full history (Sprint 28→67 + the 2026-07 bug-hunt, KDs #63–185, migrations 030–119) lives in `docs/sprints/development-plan.md` (status snapshot at top) + `git log`. Don't duplicate it here.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved across all moves. KDs were split into per-topic files under `docs/key-decisions/` (the root file is the index + KD-to-topic map); existing "KD #N" cites still resolve via the index, and global numbering is unchanged.

Historical plan and spec docs under `docs/superpowers/` cite the older `.claude/rules/key-decisions/<topic>.md` paths. Those are archival records of past work and were deliberately left as-written — read them as "the topic file for that KD", which now lives under `docs/key-decisions/`.
