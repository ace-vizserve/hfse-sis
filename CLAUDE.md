# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, P-Files, Records, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

These two are `@`-imported so they're always in context. Do not edit without explicit user approval.

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

## Stable rules — on-demand (read with the Read tool when relevant)

Not `@`-imported. Each file carries YAML frontmatter (`description`, `load: on-demand`) explaining its trigger. Read before acting when any of the "Read when..." conditions apply.

| Rule | Read when... |
| --- | --- |
| `.claude/rules/tech-stack.md` | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha |
| `.claude/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives |
| `.claude/rules/env-vars.md` | Touching `.env.local`, Supabase/auth plumbing, or Resend emails |
| `.claude/rules/key-decisions.md` | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions |
| `.claude/rules/design-system.md` | Before any UI / frontend code; when choosing a shadcn primitive, token, color, or layout |
| `.claude/rules/workflow.md` | Finishing work — before reporting a task done, or at session wrap-up |

## Reference docs

| Doc | Read when... |
| --- | --- |
| `docs/sprints/development-plan.md` | Starting any task — status snapshot + current sprint |
| `docs/context/01-project-overview.md` | Onboarding |
| `docs/context/02-grading-system.md` | Grade computation / formula |
| `docs/context/03-workflow-and-roles.md` | Permissions, locking, workflow |
| `docs/context/04-database-schema.md` | DB tables / queries |
| `docs/context/05-report-card.md` | Report card UI / PDF |
| `docs/context/06-admissions-integration.md` | Admissions sync |
| `docs/context/07-api-routes.md` | API contracts |
| `docs/context/08-admission-dashboard.md` | Admissions analytics |
| `docs/context/09-design-system.md` | UI tokens, hard rules, page→component matrix, pre-delivery checklist |
| `docs/context/09a-design-patterns.md` | Craft standard, canonical patterns, semantic color discipline |
| `docs/context/10-parent-portal.md` | Parent identity, linkage, SSO handoff |
| `docs/context/10a-parent-portal-ddl.md` | Frozen admissions DDL (AY-prefixed tables) |
| `docs/context/11-performance-patterns.md` | Any new page — auth/cache/parallel/loading checklist |
| `docs/context/12-p-files-module.md` | P-Files module — document types, statuses, architecture |
| `docs/context/13-sis-module.md` | Records module |
| `docs/context/14-modules-overview.md` | Cross-module architecture, shared identity, navigation |
| `docs/context/15-markbook-module.md` | Markbook module scope |
| `docs/context/16-attendance-module.md` | Attendance module (Phase 1 + 1.1 shipped) |
| `docs/context/17-process-flow.md` | Cross-module lifecycle + soft gates |
| `docs/context/18-ay-setup.md` | Superadmin AY-rollover wizard |
| `docs/context/19-evaluation-module.md` | Student Evaluation module — FCA writeups + virtue theme (KD #49) |
| `docs/context/20-dashboards.md` | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts` |
| `docs/context/21-stp-application.md` | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` gating + STP-conditional doc slots (KD #61) |

## Session context

<!--
Scratch surface for session-learned context, temporary caveats, in-flight
investigations. Safe to edit; pruned periodically. Stable rules do NOT go
here — they live in `.claude/rules/*.md`.
-->

- **P-Files renewal lifecycle + parent-portal re-upload tracking + student-detail redesign — shipped 2026-04-29 on `feat/swap-sonner-sileo`** (uncommitted; 2 migrations applied via Supabase Studio). Two new KDs (#63 + #64). **Migration 033** adds DB trigger `capture_doc_revision()` on every `ay{YYYY}_enrolment_documents` table — fires AFTER UPDATE OF the 16 slot URL columns, gated on enrolled status, captures the parent-portal direct-write path that bypassed the SIS upload route. Schema changes on `p_file_revisions`: `archived_url`/`archived_path` → nullable, new `previous_url` + `source` columns; partial unique index `(ay_code, enrolee_number, slot_key, previous_url)` dedupes route's explicit insert vs trigger's after-update insert via ON CONFLICT DO NOTHING. `attach_doc_revision_trigger` introspects `information_schema.columns` so AY2025 (missing the 3 STP columns) attaches a 13-column trigger gracefully. **Migration 034** adds `p_file_outreach` (`kind='reminder' | 'promise'`, append-only RLS-locked) backing the renewal lifecycle. **3 new API routes**: `POST /api/p-files/[enroleeNumber]/notify` (single-slot reminder, 24h cooldown, 6 reasons surfaced), `POST /api/p-files/notify/bulk` (max 50, skipped/cooldown counts), `PATCH /api/p-files/[enroleeNumber]/promise` (sets `<slot>Status='To follow'` + revalidates `sis:${ayCode}`). **3 new audit actions**: `pfile.reminder.sent`, `pfile.reminder.bulk`, `pfile.mark.promised`. Resend template at `lib/notifications/email-pfile-reminder.ts` mirrors publication-email pattern (RESEND_API_KEY no-op fallback + NODE_ENV dev redirect per KD #16+#29); recipient resolution by slot prefix (mother*/father*/guardian*) with student slots → mother + father (CC), fallback guardian. **DocumentCard redesigned**: gradient badge variants (`success`/`warning`/`blocked`) replace outline+tint as the at-a-glance signal; card stays white in every state (only Missing keeps a dashed border); urgency line in mono uppercase ("EXPIRES IN 5 DAYS") only on actionable cards; Reminded badge=`warning`, Promised badge=`default` (indigo gradient — distinguishes from Reminded amber, was previously identical); action row pinned to bottom via flex spacer with History collapsed to icon-only, Notify/Promise/Upload/View ordered by primary action; classification via new `lib/p-files/urgency.ts::classifyUrgency` (10 kinds: expired/rejected/missing/expiring-30/60/90/uploaded/to-follow/valid/na). **Student-detail page (`/p-files/[enroleeNumber]`) redesigned**: hero replaces single status pill with horizontal multi-pill row (only > 0 counts render: success+pct, blocked expired/rejected, dashed missing, default promised, warning reminded); size-16 ring replaced with slim inline progress bar; **2-col operational row** — `<ActionQueueCard>` (top 5 actionable slots with inline Notify/Promise/Upload + `→` jump-to-slot link, "Nothing needs attention" empty state) + `<FamilyContactCard>` (mother/father/guardian rows + emails + role tags + STP application type as accent footer); `<RecentActivityStrip>` auto-hides when no events, otherwise horizontal scroll of latest 12 reminder/promise chips. Document groups reordered (`student-expiring → parent → student → stp`); slots within each group `compareSlotsByUrgency`-sorted. Tab-conversion explored but reverted on triage-vs-navigation grounds (registrar wants ⌘F + "see everything wrong at once"). **3 new sidebar quicklinks** under "Expiring soon" group: `/p-files?expiring=30|60|90`. CompletenessTable gets row-checkbox column + sticky `<BulkNotifyDialog>` footer when filter is `expired` or any `?expiring=N`. `getStudentDocumentDetail` enriched: per-slot outreach summary, parent display names, `stpApplicationType`, recent 12 events. **In-flight (NOT shipped — plan only at `~/.claude/plans/melodic-jingling-porcupine.md`)**: Markbook AY-scoping audit revealed 6 helpers/routes leaking cross-AY data — `loadMarkbookKpisForRange` / `loadGradeEntryVelocityRangeUncached` / `loadChangeRequestVelocityRangeUncached` ignore `RangeInput.ayCode`; `markbook/grading/page.tsx:106` + `change-requests/page.tsx:31` + `grading/requests/page.tsx:61` fetch unscoped; `loadStats` cache-key omits AY id. Fix pattern: nested `!inner` join + `.eq('section.ay_code', ayCode)` (or via `grading_sheet → section.ay_code` for grade_entries/CR). Build clean throughout shipped pieces.
- **P-Files refinements + sidebar active-state fix — shipped 2026-04-28 on `feat/swap-sonner-sileo`** (4 commits `d2006cb` → `a375018`). **Slot chips destructive**: `expiredSlots` / `uploadedSlots` / `promisedSlots` / `expiringSlots` columns in the lifecycle drill sheet all switched from their previous mixed palette to `'very-stale'` (destructive red) — every flagged slot in any drill now reads as "needs attention" with the same visual weight regardless of which severity bucket triggered the drill. **Chase strip card bg**: `<DocumentChaseQueueStrip>` cards dropped per-severity gradient washes for MetricCard's neutral `bg-gradient-to-t from-primary/5 to-card shadow-xs` pattern; severity stays visible via the gradient icon tile + ChartLegendChip. **P-Files KPI bug**: `loadPFilesKpisForRange` was comparing raw status strings to lowercase literals (`'pending' || 'uploaded'`), but the admissions DB stores PascalCase per KD #60 — every comparison returned false, so MetricCards showed 0 while drills returned hundreds of rows. Fixed by routing through the same `resolveStatus` helper that powers `getCompletionByLevel` + `getSlotStatusMix`. **P-Files focused operational view**: when sidebar Quicklink sets `?status=missing|expired|uploaded|complete`, the page renders a stripped-down list (hero + AY/range toolbar + filtered table + back link), skipping the dashboard KPIs/charts/heatmap/priority panel that would have shown AY-wide data anyway. Per-status hero copy via `STATUS_VIEW_META`. Side-benefit: focused branch skips ~9 DB roundtrips. CompletenessTable also gets a `key={selectedAy}:${initialStatusFilter}` so the local statusFilter state actually re-initialises on URL filter changes (was previously stuck at first-mount value). **Sidebar active-state for query-string Quicklinks**: `findActiveHref` rewritten to be query-aware — items with `?status=...` or `?term=...` in href now highlight when the URL matches both path AND every declared query param (extra params like `?ay=AY9999` ignored). Longest-href wins so the specific quicklink beats the parent path-only item when both match. Same fix benefits Evaluation sidebar's `?term=` Quicklinks. Build clean.
- **Document expiry auto-flip + 30-day "expiring soon" drill — shipped 2026-04-28 on `feat/swap-sonner-sileo`** (12 commits `0e36376` → `896b6d9`). KD #60's auto-flip half is now implemented. **Reactive auto-flip**: new `lib/sis/freshen-document-statuses.ts::freshenAyDocuments(ayCode)` runs 8 parallel idempotent `UPDATE`s (one per expiring slot) flipping `<slot>Status='Valid'` → `'Expired'` when `<slot>Expiry < today`. Wrapped in `unstable_cache` (60s TTL, tag `sis:${ayCode}`) so rapid refreshes dedupe and existing PATCH routes' `revalidateTag` calls naturally invalidate. Audit row added only when flips actually happened — system actor (`actor.id = null`, email `'(system:freshen)'`) per `audit_log` schema's null-actor allowance (migration 006); `lib/audit/log-action.ts` widened `LogActionParams.actor.id` to `string | null` and added `'sis.documents.auto-expire'` to the action enum. Mounted at the top of 6 page RSCs that read document status (`/admissions`, `/records`, `/p-files` dashboards + `/admissions/applications/[enroleeNumber]`, `/records/students/[studentNumber]`, `/p-files/[enroleeNumber]` detail pages). 7th page `/records/students/by-enrolee/[enroleeNumber]` is a redirect-only stub — freshen unnecessary. **Proactive expiring-soon drill**: `lib/sis/process.ts::scanDocStatusForActionFlags` extended with 4th boolean `hasExpiringSoon` (true when `Valid` + expiry in `[today, today + EXPIRING_SOON_THRESHOLD_DAYS]`); `EXPIRING_SOON_THRESHOLD_DAYS = 30` exported from `process.ts` (NOT `document-chase-queue.ts` — circular import risk). `getDocumentChaseQueueCounts` returns 4th counter `expiringSoon`; `<DocumentChaseQueueStrip>` adds 4th tile (severity `warn`, lucide `CalendarClock`); grid widens `md:grid-cols-2 lg:grid-cols-4`. New `LifecycleDrillTarget` `'awaiting-expiring-documents'` with `expiringSlots[]` + `daysLeft` row fields, "Expiring within 30 days" header, default columns `[name, level, expiringSlots, daysLeft, status, days-since-update]`. Read-time only — no DB writes for proactive signal; the auto-flip naturally evicts rows from this bucket when expiry passes (signals are mutually exclusive). Cron evaluated and rejected on observability grounds (silent failures in Vercel logs). Spec: `docs/superpowers/specs/2026-04-28-document-expiry-auto-flip-design.md`. Plan: `docs/superpowers/plans/2026-04-28-document-expiry-auto-flip.md`. Build clean.
- **Dashboard date-range picker fixes — shipped 2026-04-28 on `feat/swap-sonner-sileo`** (5 commits `e507e12` → `91f55e6`). Four bugs in the comparison toolbar's `<DateRangePicker>` flow. (1) **`<LifecycleDrillSheet>` Sheet/Trigger context**: chase strip mounted `<LifecycleDrillSheet>` bare in `<CardFooter>`, but the sheet only renders `<SheetContent>` and needs an enclosing `<Sheet>` for `DialogPortal` context — wrapped each tile in `<Sheet>` + `<SheetTrigger asChild>` mirroring `MetricCard.drillSheet`. Whole card now click-to-drill. (2) **`onChange` + `onComparisonChange` clobber**: picker fired both back-to-back synchronously; both `pushParams` reads hit the same stale `searchParams` snapshot from `useSearchParams()`, so the second `router.push` (with `cmpFrom/cmpTo`) wrote over the first's `from/to`. Net result: URL ended up with only comparison set, page fell back to default range. Fix: `onChange` now takes optional `autoComparison?: DateRange` second arg; toolbar merges both into one `updateParams` + one `pushParams` call. (3) **Calendar click thrash + impossible multi-day**: every click immediately committed `?from=A&to=A` (defaulting `to=from` for incomplete selections); next click started a fresh range from react-day-picker's perspective; multi-day was unreachable. Fix: stage selections in local `pendingRange` state, auto-commit on two distinct days, single-day requires explicit "Apply" button. Presets still commit immediately. (4) **`router.push` no RSC refresh**: same-path-different-searchParams could hit the prefetch cache; now `pushParams` calls `router.refresh()` after `router.push` inside the same `startTransition` to force RSC re-fetch. Bonus: removed the "Custom" row from the preset list (was inert — handler returned early on `if (p === 'custom')`). All four fixes sit in `components/ui/date-range-picker.tsx` + `components/dashboard/comparison-toolbar.tsx`. Build clean.
- **"To follow" document flag rollout — shipped 2026-04-28 on `feat/swap-sonner-sileo`** (11 commits ending in `5e2b3ce`). Closes the gap KD #60 documented but never implemented. **Cohort aggregate**: new bucket `'awaiting-promised-documents'` (severity `warn`, alongside revalidation + validation in the lifecycle widget). **Per-applicant timeline**: `'To follow'` slots rebucketed from `settled` to a new `promised` counter so single-student detail strings agree with the cohort widget (e.g. *"Status: Processing · 6/16 settled · 2 awaiting validation · 3 promised · 4 needs action"*). **Drill target** `'awaiting-promised-documents'` end-to-end (union, build switch, columns, header, sheet renderer, CSV cell). **Top-of-fold chase strip** mounted on all 3 dashboards (`/p-files`, `/admissions`, `/records`): 3-card grid (revalidation/validation/promised) opening the matching drill via shared `<Sheet>` wrappers. Lifecycle aggregate card's `BUCKET_ICON` + `REMAINDER_ORDER` registries got the new bucket entry (lucide `MailQuestion` + sort position immediately after document-validation) so the bucket sorts correctly + uses the right icon when surfaced via the widget. Pure-additive: parent portal's existing `'To follow'` writes stay untouched. Spec: `docs/superpowers/specs/2026-04-28-to-follow-document-flag-design.md`. Plan: `docs/superpowers/plans/2026-04-28-to-follow-document-flag.md`. Build clean.
- **List ordering wiring + command-palette sticky footer — shipped 2026-04-28 on `feat/swap-sonner-sileo`**. Three threads. **`lib/sis/queries.ts::listStudents`** gains `orderBy: 'created_at_desc' | 'name_asc'` (default `'name_asc'`); SQL `.order()` per mode with `enroleeNumber` as stable tiebreaker; `orderBy` threaded into the `unstable_cache` key tuple `['sis', 'list-students', ayCode, orderBy]` so admissions and records cache independently (tag stays `sis:${ayCode}` so existing invalidation hits both buckets). `searchStudentsAcrossAY` adds within-AY `.order('created_at', desc)` so the newest-matching application surfaces first inside the most-recent AY (cross-AY `ay_code desc` unchanged). `getSisDashboardSummary` calls with explicit `'name_asc'`. **`<StudentDataTable>`** gains two new props: `defaultSorting?: SortingState` (initial value for the table's `sorting` state, falls back to current `[level, section]` default) and `showSubmittedColumn?: boolean` (opt-in sortable "Submitted" column after Status, formatted `dd MMM yyyy` via `toLocaleDateString('en-SG')` per KD #32, hideable via the existing Columns dropdown). **Page wiring**: `/admissions/applications` → `listStudents(ay, 'created_at_desc')` + `<StudentDataTable showSubmittedColumn defaultSorting={[{id:'submitted',desc:true}]} />` so admissions team lands on newest-first; `/records/students` → `listStudents(ay, 'name_asc')` with no table-prop change (visible default stays level+section, server pre-sort just gives cache stability). **Command-palette sticky footer**: `components/sis/command-palette.tsx` footer pulled out of `CommandList` (which has `overflow-y-auto` and was scrolling the hint with the list); now sits as a flex sibling with `shrink-0` + tinted `bg-muted/40` strip + indigo gradient icon tile + two-line label + bordered ⌘K kbd. Plan: `~/.claude/plans/lucky-sparking-haven.md`. Build clean.
- **Sprint 27 follow-on — cohort views + global command palette + structured residence-history editor + calendar/subject-config polish — shipped 2026-04-27 on `feat/swap-sonner-sileo`** (post-snapshot work, 17 commits `b49156f` → `9b55ba9`). **Cohort views**: 3 new lifecycle observation lenses dual-mounted on `/admissions/cohorts/{stp,medical,pass-expiry}` + `/records/cohorts/{stp,medical,pass-expiry}` (single source from `lib/sis/cohorts.ts`, served by `app/api/sis/cohorts/[cohort]/route.ts`); shared `<CohortPageShell>` + 3 cohort-specific tables in `components/sis/cohorts/`. **Lifecycle drill API**: `app/api/sis/drill/[target]/route.ts` + `components/sis/drills/lifecycle-drill-sheet.tsx` — the lifecycle aggregate widget's 8 buckets are now click-to-drill into the underlying student rows (KD #56 framework). **STP UI**: `<StPApplicationCard>` surfaces `stpApplicationType` + ICA conditional slots on the applicant detail page; new `app/api/sis/students/[enroleeNumber]/residence-history/route.ts` + `<ResidenceHistoryEditor>` replaces the previous raw-JSON entry with a structured UI-friendly form (ICA min-1 row required). **Global Cmd+K command palette**: `cmdk` dep added; `components/ui/command.tsx` is the shadcn-style wrapper; `components/sis/command-palette.tsx` is the wired-up palette mounted in the root layout, offers fuzzy navigation across all 7 modules + live student search. Visible trigger lives in the module-sidebar header + topbar (Cmd+K is the shortcut, not the only access path — KD #58 sidebar pattern preserved). **P-Files slot config sync**: `lib/p-files/document-config.ts` extended to mirror Sprint 27's `DOCUMENT_SLOTS` 12→16 expansion + new `'stp'` slot group for ICA-conditional slots. **Calendar fixes** on `components/attendance/calendar-admin-client.tsx`: meta-strip dynamic (total-weeks + pre/in/post-term phase), cursor reset to term-start month on term switch, MonthView cells render badges by removing native `disabled` attribute (badges were getting suppressed on out-of-month days), Today button re-enabled. **Subject-config matrix**: cells + headers + sticky Subject column rendered as full chip-saturation gradients to match the legend chips (4 progressive fixes). All work on top of the 34th-pass Sprint 27 baseline (lifecycle observability v1 + levels word-form migration + seeder realism — see dev plan row for full coverage of KDs #59-#62, migration `029_levels_to_words.sql` not yet applied to production). Build clean.
- **Sidebar consolidation + sonner→sileo shim + test-banner refresh — shipped 2026-04-26 on `feat/swap-sonner-sileo`** (33rd pass; PR not yet open against `main`). New KD #58. **Sidebar**: 8 per-module `<module>-sidebar.tsx` files + the topbar `<ModuleSwitcher>` are deleted; one `components/module-sidebar.tsx` (+ `module-sidebar/{sidebar-header,sidebar-nav-item,sidebar-profile,sidebar-quick-action}.tsx` parts) replaces them. Module identity moved into the sidebar header as a `Popover` switcher; active item adopts §9.3 non-flat (gradient wash + ring-inset); footer collapsed to a single profile pill with a popover for Account + Sign out; per-role quick action declared in `lib/sidebar/registry.ts::SIDEBAR_REGISTRY[module].quickActionByRole`. New `components/topbar-module-switcher.tsx` survives only on the neutral `/` + `/account` shells (no sidebar to host the switcher there). Realtime badges generalized via `lib/sidebar/use-realtime-badges.ts` — markbook-only `hooks/use-realtime-badge-count.ts` removed. One in-flight bug caught + fixed before merge: `useRealtimeBadges(badges ?? {})` was creating a fresh object every render → infinite recount loop on modules without badges. Stable `EMPTY_BADGES = {}` const added in `module-sidebar.tsx`. **Toast shim**: `sonner` dropped from `package.json`, `sileo ^0.1.5` added; `tsconfig.json::compilerOptions.paths` rewires `'sonner'` → `components/ui/sonner.tsx`, which is a sonner-shaped facade over `sileo`. Zero call-site touches across ~154 `toast.X(...)` invocations in 55 files. **Test-mode banner**: thin amber strip redesigned — gradient `AlertTriangle` tile + mono `Test environment · AYxxxx · Disposable data` + `Button variant="warning"` to `/sis/admin/settings`. Specs: `docs/superpowers/specs/2026-04-26-sidebar-redesign-design.md` + `2026-04-26-sonner-to-sileo-shim-design.md`. Build clean.
- **Drill-down framework + 4-module replication + Records/P-Files/SIS rollout (2026-04-25 → 2026-04-26)** — codified into KD #56. Every aggregating dashboard across all 7 modules is click-to-drill via `lib/<module>/drill.ts` + `/api/<module>/drill/[target]/route.ts` + per-target `'use client'` chart wrappers. Per-module pre-fetch contract: rolled-up shapes server-fetched, raw rows lazy-fetched on drill open with `DrillSheetSkeleton`. New `lib/auth/teacher-emails.ts::getTeacherEmailMap` (5-min cached, returns `Array<[id,email]>` not Map — `unstable_cache` JSON-round-trips Map as `{}`). Migration `028_drill_loader_indexes.sql` ships 5 covering indexes for grade_entries / section_students / grading_sheets. KD #24 preserved (raw `fetch` + sonner). Records pipeline-stage uses recharts Sankey. SIS Admin drill sheet handles 4 row shapes (audit / approver / AY / actor) via discriminated render. Specs: `docs/superpowers/specs/2026-04-25-{admissions-drill-downs,drill-down-replication,dashboard-perf-audit}-design.md` + `2026-04-26-records-pfiles-sis-drill-downs-design.md`.
- **Design-system "non-flat" primitive refresh + 27th-pass polish (2026-04-25)** — three-tier primitive system codified in `09a-design-patterns.md`: T1 chip/CTA (gradient + tile glow), T1 content surface (solid tint + `ring-1 ring-inset`, never gradient bg), T2 fillable (hairline + inset `shadow-input` + indigo focus bloom). New tokens: `shadow-input` 4% drop → 6% inset, `shadow-brand-tile-{mint,destructive,amber}`. Shared `components/dashboard/chart-legend-chip.tsx` powers the legend sweep across 8 recharts + 3 hand-rolled legends. `Badge variant="muted"` + base mono-uppercase typography. Subject weights matrix + dialog redesigned with profile-coded cells + RatioBar. Specs: `docs/superpowers/specs/2026-04-25-non-flat-primitive-refresh-design.md`.
<!-- 31st + 32nd passes (Task-oriented dashboard layouts + non-flat status-pill rollout) pruned from this scratch on 2026-04-29 — codified in KD #57 + dev plan + `09a-design-patterns.md` §10. -->


## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved; only the host file moved.
