# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, P-Files, Records, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

These two are `@`-imported so they're always in context. Do not edit without explicit user approval.

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

## Stable rules — on-demand (read with the Read tool when relevant)

Not `@`-imported. Each file carries YAML frontmatter (`description`, `load: on-demand`) explaining its trigger. Read before acting when any of the "Read when..." conditions apply.

| Rule                              | Read when...                                                                                                                                                                                                                                                                       |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.claude/rules/tech-stack.md`     | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha                                                                                                                                                                                  |
| `.claude/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives                                                                                                                                                                                        |
| `.claude/rules/env-vars.md`       | Touching `.env.local`, Supabase/auth plumbing, or Resend emails                                                                                                                                                                                                                    |
| `.claude/rules/key-decisions.md`  | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions. The file is a thin index — open it to find the topic file under `.claude/rules/key-decisions/` that holds the KD you need, then Read that. |
| `.claude/rules/design-system.md`  | Before any UI / frontend code; when choosing a shadcn primitive, token, color, or layout                                                                                                                                                                                           |
| `.claude/rules/workflow.md`       | Finishing work — before reporting a task done, or at session wrap-up                                                                                                                                                                                                               |

## Reference docs

| Doc                                         | Read when...                                                                                                                     |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `docs/sprints/development-plan.md`          | Starting any task — status snapshot + current sprint                                                                             |
| `docs/context/01-project-overview.md`       | Onboarding                                                                                                                       |
| `docs/context/02-grading-system.md`         | Grade computation / formula                                                                                                      |
| `docs/context/03-workflow-and-roles.md`     | Permissions, locking, workflow                                                                                                   |
| `docs/context/04-database-schema.md`        | DB tables / queries                                                                                                              |
| `docs/context/05-report-card.md`            | Report card UI / PDF                                                                                                             |
| `docs/context/06-admissions-integration.md` | Admissions sync                                                                                                                  |
| `docs/context/admission-process.md`         | HFSE's 13-step intake process (Open House → Orientation); maps ~1:1 to the 9-stage SIS pipeline (`STAGE_KEYS`, KD #51/#59)       |
| `docs/context/07-api-routes.md`             | API contracts                                                                                                                    |
| `docs/context/08-admission-dashboard.md`    | Admissions analytics                                                                                                             |
| `docs/context/09-design-system.md`          | UI tokens, hard rules, page→component matrix, pre-delivery checklist                                                             |
| `docs/context/09a-design-patterns.md`       | Craft standard, canonical patterns, semantic color discipline                                                                    |
| `docs/context/10-parent-portal.md`          | Parent identity, linkage, SSO handoff                                                                                            |
| `docs/context/10a-parent-portal-ddl.md`     | Frozen admissions DDL (AY-prefixed tables)                                                                                       |
| `docs/context/11-performance-patterns.md`   | Any new page — auth/cache/parallel/loading checklist                                                                             |
| `docs/context/12-p-files-module.md`         | P-Files module — document types, statuses, architecture                                                                          |
| `docs/context/13-sis-module.md`             | Records module                                                                                                                   |
| `docs/context/14-modules-overview.md`       | Cross-module architecture, shared identity, navigation                                                                           |
| `docs/context/15-markbook-module.md`        | Markbook module scope                                                                                                            |
| `docs/context/16-attendance-module.md`      | Attendance module (Phase 1 + 1.1 shipped)                                                                                        |
| `docs/context/17-process-flow.md`           | Cross-module lifecycle + soft gates                                                                                              |
| `docs/context/18-ay-setup.md`               | Superadmin AY-rollover wizard                                                                                                    |
| `docs/context/19-evaluation-module.md`      | Student Evaluation module — FCA writeups + virtue theme (KD #49)                                                                 |
| `docs/context/20-dashboards.md`             | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts`                                      |
| `docs/context/21-stp-application.md`        | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` gating + STP-conditional doc slots (KD #61) |

## Session context

<!--
Scratch surface for session-learned context, temporary caveats, in-flight
investigations. Safe to edit; pruned periodically. Stable rules do NOT go
here — they live in `.claude/rules/*.md`. Sprint-by-sprint history lives in
`docs/sprints/development-plan.md` + `git log`.
-->

**Current state (2026-06-26 — on shared branch `feat/attendance-template-fidelity`, interleaved with parallel work, not yet PR'd):** **Application OUTCOME vs current state** (KD #150, amends KD #147): `applicationStatus` is now the append-only application **outcome** — the post-enrolment withdrawal route stops cascading it to `'Withdrawn'`; the **current** state lives on `section_students.enrollment_status`. An enrolled-then-withdrawn student keeps `Application Outcome: Enrolled` + `Current Status: Withdrawn` (read-only two-badge `components/sis/student-status-badges.tsx`; Records detail full, Admissions detail outcome-only) and no longer double-lists in **both** the Admissions closed archive + Records withdrawn (closed list → cleanly pre-enrolment-only). Audited all 17 terminal `applicationStatus` readers: outcome-readers self-correct, current-state readers switched to `enrollment_status` — `fetchAdmissionsRoster` excludes currently-withdrawn (**critical: stops the bulk "Sync from Admissions" reactivating them**), `resolveIsWithdrawn` dual-signal + transfer-aware (KD #67). Parent portal (`/api/parent/v2/*` `getAllStudentsByParentEmail`) **unchanged** — include withdrawn (cards stay window-gated, KD #10). Seeder: enrolled-then-withdrawn keep `Enrolled`. **No migration, no backfill** (0 corrupted prod rows — the corruption was latent, prevented go-forward). 630 tests, tsc clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-26-application-outcome-vs-current-state*`.

**Current state (2026-06-26 — on shared branch `feat/attendance-template-fidelity`, interleaved with parallel work, not yet PR'd):** **Attendance-sheet template fidelity + Term-sheet redesign** (KD #151). The in-system attendance Term sheet now mirrors HFSE's `AY2026 Term 3 Attendance.xlsx` workbook (recognition without losing modern UX), plus an `.xlsx` export reproducing it. **No migration** — all from existing tables (Bus/Student Care = existing `bus_no` + `classroom_officer_role`; `Academics`/`Admin` are read-only placeholders pending HFSE). Pure unit-tested engines `lib/attendance/sheet-{summary,columns}.ts` (HFSE % = `(P+L+EX)/marked-days`, **intentionally ≠ dashboard's** Present÷school-days; SE/EX column-tag resolver). Live sheet (`wide-grid.tsx` + new `sheet-context.tsx`): register **masthead** + SE/EX date tags + **Details**/**Summary** toggles. **Cell control redesigned**: per-cell native `<select>` → plain button + **one shared "marking palette" popover** (`cell-mark-popover.tsx`; perf invariant changed to single-portal) whose excuse rows show Vacation 1/term + Compassionate 5/year **used/allowance inline**. Export: `GET /api/attendance/[sectionId]/export` (`sheet-export.ts`, SheetJS). Shared single-source maps `column-tags.ts`/`status-wash.ts` (§10.2); installed `ui/collapsible.tsx`. 81 attendance tests, build clean. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-25-attendance-template-fidelity*`.

**Current state (2026-06-22 — on branch `feat/tables-filters-sorting`, not yet PR'd):** **Admissions application staleness filter + comprehensive tables filters/sorting remediation** (KD #149 + KD #84 update). Admissions: opt-in **Staleness** badge column + facet on the applications table (Fresh<7d/Warning 7-13d/Critical≥14d/Never updated from `applicationUpdatedDate`; `lib/admissions/staleness.ts` + `staleness-badge.tsx`); the "needs follow-up" insight now deep-links into it pre-filtered (`students.staleness=Warning,Critical`) and the redundant "Follow up today" card was removed. Tables sweep (audit of all 31 `<DataTable>` consumers): the shell only renders a clickable sort when a column's `header` is a `<SortableHeader>` — converted ~11 tables' date/number/urgency columns; fixed the shell `tabCountData` to resolve facet values via the column **accessor** (was raw `row[id]` → zeroed status-tab badges under accessor-backed facets); added `schedule`/`reason`/`term` facets, dropped redundant status-as-both-facet-and-tabs + a broken consent facet + the `outdated` status/tab id collision; converted users/discount-codes/attendance-readonly status→tabs; report-cards **Closed** tab now reachable; unsynced-queue gapReason tabs + Level facet; markbook audit-log action/actor filtering moved **server-side**; migrated `staff-table` onto the shell; added in-place sorting to the bespoke `document-completeness-table`. **Then a row links + per-row actions pass** (same KD #84 update + KD #81): cross-module `⋯` menus on the action-less tables, surfaced hidden actions (grading per-row lock, change-request open-sheet on all statuses), per-row "Send reminder" (cohort/doc-completeness/p-files-expiring, reuse existing dialogs/routes), `mailto:` chase links, `IdentifierLink` consolidation, staff section links. Code-only, no migration; 373 tests, build clean. Blow-by-blow: `git log` + dev-plan snapshot.

**Current state (2026-06-15 — PR'd to `main` on `feat/report-card-na-and-seeder-fidelity`, not yet deployed):** **TanStack Query v5 is now the client data layer** (KD #24, reverses the old "no React Query" rule): all **80** client components moved off raw `fetch()` → `useQuery` (lazy reads) / `useMutation` (writes). **Model A (RSC-first):** mutations still `router.refresh()` on success — RSC data is not moved into the client cache. Infra: `lib/query/{fetcher (apiFetch + typed ApiError + jsonInit), keys}` + `components/providers/query-provider.tsx` mounted in `app/layout.tsx` (queries `staleTime 60s`/`retry 2`/no focus-refetch; **mutations `retry 0`**). Every `onError` preserves the route's specific 422/409 code (no generic flattening). Component-test capability added (Vitest → jsdom + `@testing-library/react`). **Drill-seed bug found + fixed** (the migration's one regression): drills seed from a broad server-prefetched array but per-`(target,segment)` narrowing is server-side (KD #82) — `initialData` + `initialDataUpdatedAt: Date.now()` marked the broad seed _fresh_ under the 60s staleTime → the narrowing fetch was skipped → every drill showed the same unfiltered set. Fixed: the 6 broad/kind-seed drills use **`placeholderData`** (paint seed, but ALWAYS fetch + replace); markbook keeps `initialData` (its seed is client-narrowed to equal the fetch). Reproduced + regression-tested (`__tests__/query/drill-seed.test.tsx`). **Report-card N.A. for non-enrolled terms** (KD #148): enrolment-coverage proration (term outside `[enrollment_date, withdrawal_date]` → N.A.; annual/GA prorate over enrolled terms only; `lib/report-card/enrolment-coverage.ts`). All **code-only** — no migrations/seeders/data touched, so it applies to prod + test equally (takes effect on deploy). 363 tests, build clean. Blow-by-blow: dev-plan snapshot + `git log`.

**Current state (2026-06-12 — all on prod):** **Late-enrollee → normal convert** (KD #146): a **T1** late enrollee can be reverted to `active` via a reasoned, tracked confirm ("Convert to normal enrollee?"); **T2–T4 disabled** (unambiguously late); `enrollment_date` is never touched (proration is date-derived + unchanged) — the only real change is the cosmetic classification; tracked via `lateEnrolleeReverted` on the `enrolment.metadata.update` audit. **Structured section `schedule`** (morning/afternoon/whole_day) + the official HFSE **virtue-section** template list (migration 074, KD #144). **DataTable pagination fixed** — next-page no longer reverts to page 1; rows-per-page no longer renders blank (KD #84). **Correctness sweep (surfaced by a real parent-portal E2E + a 3-agent audit):** the sync now stamps `section_students.enrolee_number` (per-student **and** bulk — it was NULL, which silently broke the late-enrollee prompt + the Records index/status maps); **5 mutation routes gained no-op idempotency guards** (dup parent-reject email, stage/lock/publish/section-students audit dups). **Seeder fidelity batch** (test-env only): EC12 transfer + EC1–EC4 now mirror the real routes' audit shapes / cascades / destination attendance, `seedMovements` withdrawal reasons, VL entries wired, referral list aligned to the parent-portal dropdown — triage seeder-vs-system before "fixing" a feature on seeded data. **Bulk-publish dialog** rows deep-link per-concern to the right fix surface (KD #145). Blow-by-blow: `git log`.
**Future work (deferred backlog):** late-semantics **C1** (derive the late label from earliest-enrollment across the ~8 enum sites — hardening, not a bug) + multi-interval re-enrol proration (known limitation); per-module compare trend charts (post-go-live); Sec 4 Economics card (no Sec 4 students yet); self-serve invite flow (KD #87, `/auth/setup`); attendance audit-log server-side pagination; cron auto-sync trigger (KD #90); VL bulk-import parser (KD #94); PTC digitization as a separate surface (DB tables intact, KD #114); **config-placement review** — operator-tuned settings → business module, structural config stays in SIS Admin (KD #48; clearest candidate discount-codes → Admissions; **do NOT reverse KD #118** — early-bird stays in SIS).

**Recent sprints:** full sprint-by-sprint history (Sprint 28→59, KDs #63–150, migrations 030–075) lives in `docs/sprints/development-plan.md` (status snapshot at top) + `git log`. Don't duplicate it here.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved across all moves. KDs were split into per-topic files under `.claude/rules/key-decisions/` (the root file is the index + KD-to-topic map); existing "KD #N" cites still resolve via the index, and global numbering is unchanged.
