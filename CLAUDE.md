# HFSE SIS — Claude Instructions

A Student Information System for HFSE International School, Singapore. Modules — Markbook, Attendance, Evaluation, P-Files, Records, Admissions, SIS Admin — are surfaces of one student record, not sibling apps. The module switcher moves between them; `studentNumber` is the backbone.

## Stable rules — auto-loaded (every session)

**Everything inside `.claude/rules/` is read into context automatically, every session.** That is a Claude Code behaviour, not something a file can opt out of — a `load: on-demand` line in a file's frontmatter is descriptive only and does not stop it loading. So only four files live there, deliberately:

@.claude/rules/always-do-first.md
@.claude/rules/hard-rules.md

- `.claude/rules/design-system.md` — binding design pointer; too important to risk missing (it is small, and only points at the full docs).
- `.claude/rules/key-decisions.md` — the KD index, so a "KD #N" cite resolves with no extra read.

Do not edit any of the four without explicit user approval, and **do not add new files to `.claude/rules/`** — anything put there is paid for in every session. On-demand material goes in `docs/` (below).

## Stable rules — on-demand (read with the Read tool when relevant)

These live **outside** `.claude/rules/`, so they cost nothing until read. Read before acting when any of the "Read when..." conditions apply.

| Rule                           | Read when...                                                                                                                                                                                                                                           |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/rules/tech-stack.md`     | Touching code, installing/upgrading a dep, debugging a framework behavior, or a Next.js 16 gotcha                                                                                                                                                      |
| `docs/rules/project-layout.md` | Creating new files, moving code between modules, or deciding where a new route or lib lives                                                                                                                                                            |
| `docs/rules/env-vars.md`       | Touching `.env.local`, Supabase/auth plumbing, or Resend emails                                                                                                                                                                                        |
| `docs/rules/workflow.md`       | Finishing work — before reporting a task done, or at session wrap-up                                                                                                                                                                                   |
| `docs/key-decisions/*.md`      | A "KD #N" reference appears in code or docs; cross-cutting architectural choices; doubt about module boundaries, roles, or conventions. Find the topic file via the always-loaded index at `.claude/rules/key-decisions.md`, then Read only that file. |

## Reference docs

| Doc                                         | Read when...                                                                                                                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/sprints/development-plan.md`          | Starting any task — status snapshot + current sprint                                                                                                                                                    |
| `docs/context/01-project-overview.md`       | Onboarding                                                                                                                                                                                              |
| `docs/context/02-grading-system.md`         | Grade computation / formula                                                                                                                                                                             |
| `docs/context/03-workflow-and-roles.md`     | Permissions, locking, workflow                                                                                                                                                                          |
| `docs/context/04-database-schema.md`        | DB tables / queries                                                                                                                                                                                     |
| `docs/context/05-report-card.md`            | Report card UI / PDF                                                                                                                                                                                    |
| `docs/context/06-admissions-integration.md` | Admissions sync                                                                                                                                                                                         |
| `docs/context/admission-process.md`         | HFSE's 13-step intake process (Open House → Orientation); maps ~1:1 to the 9-stage SIS pipeline (`STAGE_KEYS`, KD #51/#59)                                                                              |
| `docs/context/07-api-routes.md`             | API contracts                                                                                                                                                                                           |
| `docs/context/08-admission-dashboard.md`    | Admissions analytics                                                                                                                                                                                    |
| `docs/context/09-design-system.md`          | UI tokens, hard rules, page→component matrix, pre-delivery checklist                                                                                                                                    |
| `docs/context/09a-design-patterns.md`       | Craft standard, canonical patterns, semantic color discipline                                                                                                                                           |
| `docs/context/10-parent-portal.md`          | Parent identity, linkage, SSO handoff                                                                                                                                                                   |
| `docs/context/10a-parent-portal-ddl.md`     | Frozen admissions DDL (AY-prefixed tables)                                                                                                                                                              |
| `docs/context/11-performance-patterns.md`   | Any new page — auth/cache/parallel/loading checklist                                                                                                                                                    |
| `docs/context/12-p-files-module.md`         | P-Files module — document types, statuses, architecture                                                                                                                                                 |
| `docs/context/13-sis-module.md`             | Records module                                                                                                                                                                                          |
| `docs/context/14-modules-overview.md`       | Cross-module architecture, shared identity, navigation                                                                                                                                                  |
| `docs/context/15-markbook-module.md`        | Markbook module scope                                                                                                                                                                                   |
| `docs/context/16-attendance-module.md`      | Attendance module (Phase 1 + 1.1 shipped)                                                                                                                                                               |
| `docs/context/17-process-flow.md`           | Cross-module lifecycle + soft gates                                                                                                                                                                     |
| `docs/context/18-ay-setup.md`               | Superadmin AY-rollover wizard                                                                                                                                                                           |
| `docs/context/19-evaluation-module.md`      | Student Evaluation module — FCA writeups + virtue theme (KD #49)                                                                                                                                        |
| `docs/context/20-dashboards.md`             | Any dashboard work — before touching a module's landing page or `lib/<module>/dashboard.ts`                                                                                                             |
| `docs/context/21-stp-application.md`        | Singapore ICA Student Pass workflow (HFSE Edutrust Certified) — `stpApplicationType` + `stpApplicationStatus` phases (KD #61). ⚠ Its 3 STP document slots were removed by KD #96 — that half is history |
| `docs/context/23-session-log.md`            | Session history before the current sprint — shipped work, closed decisions, superseded claims, and the gotchas they left behind                                                                         |

## Session context

✅ **THE P-FILES OFFICER ROLE IS GONE, AND ADMISSIONS DOES THAT JOB NOW — 2026-09-10. `ROLES.length === 5`. Migration 143 is WRITTEN AND NOT YET APPLIED. Full account: KD #207.** Exactly one account held `p_file_officer`, and the job it named — chase a parent for a passport, approve what arrives, upload a replacement — is the job admissions already did on the applicant side. Two roles for one lifecycle meant every document rule was written twice and kept in step by hand. **`admissions` absorbed all eight `documents_*` capabilities, both sides of enrolment**, reaches `/p-files` and `/records`, and **may now place students in classes and withdraw them** (`ENROLMENT_PLACEMENT_WRITERS` and `STUDENT_RECORD_WRITERS` are the same four roles — this supersedes KD #180's `placement_forbidden` 403 and KD #51's exclusion). Nobody's rights shrank: `school_admin` and `superadmin` already held all eight.

🔴 **APPLY 143 AND MOVE THE ACCOUNT BEFORE THE CODE DEPLOYS — the wrong order is a LOCKOUT, not an error.** A role that no longer resolves is read by this app as a **PARENT**. Migration 143 alone is safe in either order (it changes what the role MAY do, not who holds it); the deploy is the irreversible half. Already resolved for this change: Louilyn is on `admissions` and **zero accounts hold `p_file_officer`**, verified by direct query. ⚠ **The string SURVIVES in `audit_log.actor_role` on historical rows and must never be back-filled away** (append-only, Hard Rule #6) — `lib/audit/humanize.ts::RETIRED_ROLE_LABELS` is the one thing keeping those rows rendering "P-File Officer" instead of a raw identifier. **Any future role retirement adds a row there in the same commit.**

✅ **AY2026 T3 ATTENDANCE WAS RE-IMPORTED FROM THE FINISHED REGISTER AND IS APPLIED TO PRODUCTION — 2026-09-11.** The July import ran off a register filled in only to 21 July, so the term was stored as **30 school days**; the finished register has **50**. Re-import wrote **11,743 rows** (11,652 days never recorded + 91 corrections, mostly `A`→`EX`) and fixed **22 calendar day types**. Generators: `scripts/backfill/gen-ay2026-t3-attendance-reimport.ts` (+ `reconcile-ay2026-t3-by-name.ts`, `verify-t3-*.ts`). ⚠ **THE SHIPPED T3 APPLY FILES CANNOT DO THIS AND MUST NOT BE RE-RUN AS THE FIX** — `school_calendar` is `on conflict do nothing`, marks are `where not exists`, and they resolve a child by **(section, index_number)**, which has since moved for **18 students**. **Match the register by NAME.** Verified first that nothing was cross-filed: every stored mark matched the register row with the same _name_, 100%, never the index. ⚠ `attendance_daily` is an **append-only ledger** — a correction is a new row superseding by `recorded_at desc`; read it with `distinct on (section_student_id, date)` or you compare against dead history. ⚠ **The classifier calls a date a teaching day if ANY child anywhere is marked on it**, so five weekends are school days off a handful of cells (S1 Discipline 2, 1/8/15 Aug). Mr Ace: _"if theie excel has weekend attendance records then we just adopt it"_ — **adopted, do not re-litigate.** ⚠ A rollup's `school_days` is **the days that child has a mark for**, not 50 (migration 068) — Blank ≠ Zero, so blanks are neutral, not a scoring bias. 🔴 **`recompute_attendance_rollup` NEVER JOINS `school_calendar`** — it counts every dated row in the term with a non-null status, so a mark left on a `no_class` date **still inflates that child's `school_days`**; it does NOT "fall out of the totals", and assuming it does was wrong. Moving a date to `no_class` therefore means **clearing its marks too** (`apply-clear-non-teaching-marks.ts` — an append of `status = null`, migration 134, never a delete) and recomputing. Five P5 Perseverance children were marked **absent on a Saturday** this way until caught by `scripts/audit-post-merge-health.ts`, the cross-module check to re-run after any bulk data change.

✅ **37 CHILDREN EXISTED TWICE; THE MERGE IS APPLIED TO PRODUCTION — 2026-09-11.** Each had one record for AY2025 and another for AY2026 under a **different `student_number`**, breaking Hard Rule #4's cross-year link and stranding **923 grade entries + 5,960 attendance marks**. `scripts/backfill/apply-merge-duplicate-students.ts` (dry-run by default, `--apply` to write) repointed **36 roster rows + 105 evaluation write-ups**; **grades and attendance needed no statement — they hang off `section_students.id`, which does not change**. Verified after: **0 split records, 295/295 linked, admissions still 406/406**. **The AY2026 record survives** because admissions points at it. ⏳ **The 37 emptied student rows are deliberately NOT deleted** — unreferenced and harmless; `gen-merge-duplicate-students.ts`'s `02-remove-leftovers.sql` is the irreversible step, and Pangilinan's trailing-space `student_number` can only be trimmed **after** it, or it collides with its own orphan. ✅ **Admissions↔Records was never the problem: 406/406 agree on level and section** (`scripts/audit-admissions-records-alignment.ts`) — Mr Ace's fear of widespread drift was measured and disproven. ✅ **9 mid-year withdrawals applied** (`apply-ay2026-t3-withdrawals.ts`; 9/9 verified, every mark intact — dates are the register's last mark, except Lagman's 31 Jul from the admissions remarks). ⚠ **Admissions still says "Enrolled" for all nine** — it uses "Withdrawn" only for applicants who never started, so a mid-year leaver is recorded **nowhere but Records**. ✅ **Karl Miguel Cacao was FORCE-ENROLLED into S4 Excellence #39 — 2026-09-12** (`apply-force-enrol-cacao.ts`; 34 marks, 100%, admissions now 407/407). Mr Ace: _"i think we should force enroll cacao since i dont have details regarding his registration, assessment, supplies and orientation"_. He had been attending since day one with **contract Signed and fees Paid** (HFSE052023), but his application never left "Submitted", so he had no AY2026 placement — the S3 Consistency that Records showed was **AY2025**. Only `applicationStatus`/`classLevel`/`classSection` and a roster row were written; the four empty stages stay null (283 of 406 enrolled children have a null `classStatus` anyway). ⚠ **It bypassed the Admissions module, so no `audit_log` row exists for that stage change** — the reasoning lives in the script's header instead. ✅ Akter's spelling + 45 marks: applied (45 days, 95.56%).

⚠ **GENERATED SQL NOW HAS A LINTER, BECAUSE TWO REAL BUGS REACHED A FILE.** A row comment placed _after_ a `VALUES` row swallowed the separating comma (silently truncating the list), and a `DO` block was emitted with repeated `begin/end` pairs instead of one (would not parse). `scripts/backfill/lint-generated-sql.ts` checks balanced transactions, one-`begin` DO blocks, the comma trap, and quote/paren balance across every generated `.sql`. **Run it before handing any generated SQL to Mr Ace** — and it was itself proven against a deliberately broken file, not just a clean pass.

⚠ **STILL OPEN, carried from 2026-09-03.** Only two accounts hold a role LIST (`kohsuat.hoon@`, `ace.guevarra@`); the other four teaching admins need the role granted before they see the switcher. Full account of that change: KD #206, archived in `docs/context/23-session-log.md`.

**Older entries have moved.** Everything before the current sprint now lives in `docs/context/23-session-log.md` — shipped features, closed decisions, superseded claims, and the gotchas they left behind. Read it when you pick up a quiet module, meet a reference you do not recognise, or are about to reopen something that may already have been measured and closed.

## Cross-reference note

Cross-references elsewhere in the repo such as "CLAUDE.md Hard Rule #N" or "CLAUDE.md KD #N" now resolve to `.claude/rules/hard-rules.md` and `.claude/rules/key-decisions.md` respectively. Numbering is preserved across all moves. KDs were split into per-topic files under `docs/key-decisions/` (the root file is the index + KD-to-topic map); existing "KD #N" cites still resolve via the index, and global numbering is unchanged.

Historical plan and spec docs under `docs/superpowers/` cite the older `.claude/rules/key-decisions/<topic>.md` paths. Those are archival records of past work and were deliberately left as-written — read them as "the topic file for that KD", which now lives under `docs/key-decisions/`.
